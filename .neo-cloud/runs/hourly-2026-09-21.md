## 00:35 UTC — skills-plugins-mcp — FIXED
- mode: section
- todo: -
- previous run: conversation-chat
- picked because: conversation-chat excluded as the most recent section that did work; among the remaining candidates, skills-plugins-mcp had the oldest lastRun (2026-09-20T12:37:00Z) and none of the other candidates were worked twice or more in the last 6 working runs
- base: 3a302389792b151a76be131d1932f425a02d79dd  commit: -
- changes:
  - Bug — `SkillCardPillRow` (`src/components/features/skills/skill-card-pill-row.tsx`): when a skill/plugin card's pills overflow (narrow card, many triggers/metadata/tools), the "+N more" overflow badge's hover tooltip built its text from each hidden pill's `id` — an internal React key never meant as copy (e.g. `type-agentskills`, `category-productivity`, `disable-model-invocation`, `metadata-foo`), not the translated label the visible pill actually shows. Hovering the badge leaked raw identifiers to the user instead of readable text.
  - Added an optional `label?: string` field to `SkillCardPill` alongside `node`, since `node` is an arbitrary ReactNode and can't be read back out as text. Populated it in `buildSkillPills` (`build-skill-pills.tsx`) for every pill kind (type, category, version, license, compatibility, disable-model-invocation, allowed-tool, metadata `key: value`, trigger) with the same translated/raw text the pill itself renders. The tooltip now joins `label`s, and omits the `title` attribute entirely (rather than showing blank/misleading text) when no hidden pill supplies one — so other `SkillCardPillRow` consumers that don't pass `label` yet (e.g. automations' pill builders) keep their prior behavior with no forced changes.
  - Added two regression tests to `skill-card-pill-row.test.tsx`: one asserting the overflow tooltip shows the human-readable labels and never the raw `type-knowledge`/`trigger-ssh` ids, one asserting the tooltip attribute is absent when no hidden pill has a label.
- gate: lint pass (typecheck + eslint + prettier all clean after one prettier --write repair) · test: full suite 28 failed files / 140 failed tests / 6288 passed / 5 skipped / 7 todo out of 6440 (matches the documented pre-existing ~27-30-file/~139-147-test baseline from this week's runs exactly — `sidebar.test.tsx`, `legacy/src/lib/smeAgent.test.ts`, `use-search-providers.test.tsx`, `new-conversation.test.tsx`, etc., all reproduced identically on a clean stash of this diff before any of my changes were applied; the touched files pass cleanly standalone — 8/8 in `skill-card-pill-row.test.tsx` including the 2 new tests, plus the full `skills`/`plugins` feature suites, 156/156) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (same documented `vendor/understand-anything/**` TS6059 rootDir errors logged in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: not checked yet — will poll after push; commit SHA and CI/deploy status recorded in a follow-up chore commit per convention
- notes: -

## 01:42 UTC — files-terminal-browser — FIXED
- mode: section
- todo: -
- previous run: skills-plugins-mcp
- picked because: skills-plugins-mcp excluded as the most recent section that did work; among the remaining candidates none had been worked twice or more in the last 6 working runs, and files-terminal-browser had the oldest lastRun (2026-09-20T13:51:00Z)
- base: 44c065b69ea35d1f9c74c179c254da6d9f8e803b  commit: -
- changes:
  - Bug — `renderCommand`/mount+sync effects (`src/hooks/use-terminal.ts`): for any `Command` with `type: "input"` and blank/whitespace-only `content`, the effects wrote the `"$ "` prompt (`terminal.write("$ ")`) but `renderCommand` only closed the line with `terminal.writeln(...)` when `trimmedContent` was non-empty — so a blank input command left a dangling, unterminated `"$ "` on the xterm buffer, and whatever wrote next (the next command's prompt or its output) landed on the same visual line, garbling the terminal. This is reachable in production: `ExecuteBashAction.command` is documented as legitimately empty (used to view additional logs after a previous command with exit code -1), and `commandFromEvent`/the live WS handler turn any such event straight into an `{type: "input"}` command with no emptiness filter. Fixed by making `renderCommand` always terminate the line for `type: "input"` (writing `""` via `writeln` when there's no content), while leaving the "skip empty newlines" behavior unchanged for non-input (`output`) commands, where it's intentional.
  - Added a regression test to `__tests__/hooks/use-terminal.test.tsx` asserting a blank `type: "input"` command still gets its `"$ "` line terminated (`writeln` called with `""`) before the next command's output — verified this test fails against the pre-fix source (asserts `writeln`'s first call is `"some output"` instead) and passes after the fix.
  - Not fixed this run (logged for context, no code change): a bug-hunt pass also flagged (a) the terminal's baked-in-at-mount foreground color never reacting to a live `theme` prop change on an already-mounted `AgentServerUIRoot` (medium confidence — plausible but requires a MutationObserver-based fix and new test scaffolding I judged out of proportion to the confidence level for this run) and (b) a "store shrank" reset heuristic in the commands-sync effect that the bug-hunt agent itself confirmed is currently dead code (the app always remounts the terminal on conversation switch via a `${tab}-${conversationId}` key, so the gap can't be hit today). Neither filed to findings.txt since both were found and fully triaged within this same run.
- gate: lint pass · test: full suite 28 failed files / 140 failed tests / 6289 passed / 5 skipped / 7 todo out of 6441 (matches the documented pre-existing ~27-30-file/~139-147-test baseline exactly, +1 passed/+1 total from the new regression test; touched file `use-terminal.test.tsx` has zero failures) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (same documented `vendor/understand-anything/**` TS6059 rootDir errors logged in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: not checked yet — will poll after push; commit SHA and CI/deploy status recorded in a follow-up chore commit per convention
- notes: preflight found the container's local `main` branch ref pointed at a stale, unrelated commit graph (no common ancestor with `origin/main` — root commits differ, though file trees/content are consistent with the same app, so this reads as an intentional upstream history squash rather than tampering) left over from container setup; realigned with `git checkout -B main origin/main` (working tree was clean, no local work at risk) before proceeding. Also did the step-0 INC-3 liveness check (see incidents.md `last confirmed` update in this commit): fly agent-server uptime ~29.3 min, unchanged/expected.
