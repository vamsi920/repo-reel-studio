## 00:53 UTC — agentops — FIXED
- mode: section
- todo: -
- previous run: automations
- picked because: TODO.txt and findings.txt had no live items; step 0 found no OPEN incident with a buildable `mitigation: none` in scope (INC-3's own mitigation field says the fix is an infra/deploy-config decision, out of scope for any routine); of the 12 sections, `automations` was excluded as the most recent to do real work (23:46 UTC FIXED), and the other 8 already-worked sections had no repeats in the last 6 working runs; from the 9 sections with `lastRun: null` (a full tie), `agentops` was picked by explicit random draw
- base: 1171da2412901d136795b8fefedc367d4a88cbd9  commit: 070d9b85b91a2b6ba254a6e928fc69915bdb30d1
- changes:
  - Explored src/components/features/agentops, src/hooks/query/use-agentops.ts, scripts/agentops/*.mjs and their tests via a research subagent against the AGENTS.md "AgentOps Control Tower" invariants; verified two of its candidate findings by hand before fixing.
  - `RunControls` (src/components/features/agentops/run-controls.tsx) never offered a Resume button on an `error`-status run, even though the collector's own `evaluateRunControl("resume", "error")` (scripts/agentops/run-control.mjs, already asserted `ok: true` by an existing passing test) genuinely restarts it — an errored run was simply stuck in the UI with zero controls. `canResume` now includes `"error"` alongside `"paused"`/`"idle"`; added a render test asserting Resume appears (and Pause/Stop don't) on an errored run.
  - `AGENTOPS_QUERY_KEYS` (src/hooks/query/use-agentops.ts) never included the active backend's id, unlike every other per-backend query-key group in this codebase (see the documented convention in src/contexts/active-backend-context.tsx, e.g. `useGitRepositories`'s key). Since `agentops-service.api.ts` resolves host/API key from whichever backend is active *at fetch time*, switching local backends while `/agentops` was open would keep serving the previous backend's cached runs/budgets/audit/summary until a scheduled poll happened to overwrite it (up to 30s for Budgets/History). Every `useAgentOps*` hook and `useSaveAgentOpsPolicies`'s cache writes now key/read by `backend.id` via `useActiveBackend()`; added a unit test asserting every key varies by backend id while staying prefixed under the shared `"agentops"` root (so the existing blanket `invalidateQueries({queryKey: AGENTOPS_QUERY_KEYS.all})` calls still match everything).
  - Updated the one existing test (`budgets-panel.test.tsx`) that called the old zero-arg `AGENTOPS_QUERY_KEYS.budgets` to pass the seeded default backend id it actually runs under.
- gate: lint pass (after `npx prettier --write` on the two touched `src/` files — one repair attempt, pure formatting) · test pre-existing-fail (30/759 files failing, 147/5966 tests — identical count/tests to the unmodified `BASE_SHA`, confirmed by `git stash`-ing this diff and re-running a representative sample of the failing files directly against base: `settings-form.test.tsx`, `add-backend-modal.test.tsx`, and the legacy `agent-ops` suite failed identically before this diff existed; full run after un-stashing reproduced the exact same `30 failed | 729 passed (759)` / `147 failed | 5807 passed | 5 skipped | 7 todo (5966)` totals; none of the 5 touched files appear anywhere in the failure output, and all agentops-area test files — 11 files, 51 tests — pass in full) · build pass (one incidental byproduct, `public/codegraph-analyzer/{analyze.mjs,manifest.json}`'s regenerated `builtAt` timestamp, reverted before commit) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, same known gap logged in every hourly/focus-fixer run yesterday, unrelated to this diff)
- ci: `fly-deploy.yml` green (run 35168540973, success — the production Fly deploy this playbook actually gates on). `ci.yml` red (run 35168540893, `test-and-build (ubuntu)` → `Lint` passed, `Test` failed after ~8min, `Build app`/`Build library` skipped as a consequence) but NOT reverted: same shape as every hourly run's already-documented pre-existing 30-file baseline from yesterday (this run's own local gate reconfirmed zero new failures beyond that baseline, see `gate:` above), and neither touched file appears in the failure output. `docker.yml`/`release.yml` also failed (runs 35168541003, 35168541404) but match the exact same pattern already failing on the immediately preceding commit (`1171da2`) — not in the playbook's revert criteria regardless.
- notes: This run also had to fix `.neo-cloud/` state itself before starting: the fresh checkout's local `main` branch ref was stale/diverged from `origin/main` by 50 commits each way (a container-image artifact, not uncommitted work — `git status --porcelain` was already empty), so local `main` was hard-reset to `origin/main` (1171da2) before any other step. For a future run: INC-3 (agent-server has no persistent storage, restarts on every push) is still OPEN with `mitigation: none` but is explicitly out of scope for any routine (touches `.github/workflows`, needs a human infra decision) — don't re-attempt it. `agentops`'s remaining candidate findings from this run's exploration, not pursued (below the fix cap and/or lower confidence): a one-poll-interval lag between a run's own cost update and its workspace/agent budget-breach check in `scripts/agentops/collector.mjs` (`#enforceBudgets` runs before `#persist`, low practical severity given the 2s poll interval); Pause not offered for `idle`/`waiting_for_confirmation` runs even though the backend technically allows it (plausibly intentional — Stop already covers those states).
## 04:38 UTC — environment-onboarding — FIXED
- mode: section
- todo: -
- previous run: agentops (00:53 UTC, this file)
- picked because: TODO.txt and findings.txt had no live items; step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED, INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine). Fell through to section mode (2b): `agentops` (most recent to do real work) excluded; no other section had been worked twice in the last 6 working runs; of the 8 sections still at `lastRun: null`, `environment-onboarding` was picked by explicit random draw among the tie.
- brainstorm: -
- base: 37149a0ce72f16ef804401177c2b1666f430c3fa  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/components/features/environment/**`, `src/components/features/onboarding/**`, and `src/routes/environment-*.tsx`, explicitly told to skip anything already covered by INC-1 (dead GitHub OAuth). It returned two verified candidates, both confirmed by hand before fixing.
  - **First-run onboarding modal never auto-showed for a real user** (`src/routes/root-layout.tsx`): `<OnboardingHost />` — the component whose entire job is deciding, from its own internal `isCompleted`/Cloud-LLM/preview logic, whether to show the welcome modal on first visit — was gated behind `isOnboardingPreviewActive(location.search)`, which is `true` only when the dev-only `?previewOnboardingStep=0-3` query param is present. For an ordinary visitor with no query param and no `openhands-onboarded` localStorage flag, `OnboardingHost` never mounted at all, so the "what are you building?" first-run flow was silently skipped end-to-end in the normal (non-locked-to-Cloud) case. `root.tsx`'s own comment documents the intended architecture: "OnboardingHost (mounted inside HomeScreen, under the same shell) shows the same modal on top of it" for exactly this case — it's supposed to mount unconditionally and decide for itself. Removed the `showOnboardingPreview` gate entirely and mount `<OnboardingHost />` unconditionally (it already reads the preview query param itself via its own `useLocation()`, so preview mode still works identically); dropped the now-unused `isOnboardingPreviewActive` import.
  - **`advancePlan(stepId, "active")` pointed the plan cursor at the wrong step** (`src/stores/onboarding-studio-store.ts`): for every status, the reducer recomputed `currentStepId` as "the first step still `pending`" in array order, ignoring which step the caller had just marked active. `advance_plan`'s `"active"` status is explicitly accepted from the agent (`src/services/onboarding-control.ts`), so an agent revisiting or reordering work (marking a later step active while an earlier one is still untouched) left `SetupPlanCard` highlighting the earlier, wrong step as current. Added a direct `status === "active"` branch that sets `currentStepId` to the step just activated; the "done"/"skipped" branch (advance to the next pending step) is unchanged.
  - Added regression tests for both: `__tests__/routes/root-layout.test.tsx` (stubs `OnboardingHost` and asserts it mounts on a plain route with no preview param) and `__tests__/stores/onboarding-studio-store.test.ts` (asserts `advancePlan("c", "active")` while "a"/"b" are still pending points the cursor at "c", not "a").
  - No new user-facing strings, so no `translation.json`/`make-i18n`/locale changes needed.
- gate: lint pass · test pre-existing-fail (30/759 files failing, 147/5972 tests — same baseline as every run this week; both touched test files pass in full; spot-verified `root.test.tsx` [18/22 failing], `sidebar.test.tsx` + `settings-form.test.tsx` [17/18 failing] reproduce byte-identical failure counts on unmodified `BASE_SHA` via `git stash`) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt`/rebuild byproduct before committing) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, confirmed byte-identical on `BASE_SHA` via the same stash-and-rerun check — same known gap logged in every hourly/focus-fixer run this week)
- ci: `fly-deploy.yml` green (run 35182757724, success — the production Fly deploy this playbook actually gates on). `ci.yml` (run 35182757762) still `in_progress` after a ~10-minute poll (`gh` unavailable in this sandbox; polled via the GitHub MCP `actions_list` tool instead) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and neither has failed. `docker.yml`/`release.yml` both failed (runs 35182757708, 35182758119) but aren't in the playbook's revert criteria, and the same two workflows are already failing on `main` independent of this diff (established pattern in prior runs' logs this week).
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. The `advancePlan` fix is scoped to `"active"` only; the "done"/"skipped" cursor-advance behavior (and its existing regression test) is untouched. For a future run: the bug-hunt subagent also looked at `onboarding-dock.tsx`'s `postResult` no-op fallback when `useOnboardingSession()` hasn't resolved a `conversationId` yet (would silently drop a credential receipt) but couldn't construct a realistic trigger given `useStartOnboardingSession`'s `onSuccess` populating the cache first in normal usage — flagging as a non-issue, not a lead to re-investigate.
## 05:40 UTC — codegraph — FIXED
- mode: section
- todo: -
- previous run: environment-onboarding (04:38 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): the last 5 working runs this rotation (knowledge-kt, settings, automations, agentops, environment-onboarding) had no repeats, so nothing was excluded for over-use; `environment-onboarding` (most recent to do real work) was excluded. Of the 7 sections still at `lastRun: null` (kt-video, codegraph, security, auth-data-platform, conversation-chat, skills-plugins-mcp, files-terminal-browser), `codegraph` was picked by explicit random draw (`shuf`) among the tie.
- brainstorm: -
- base: 026f9bcb8516ec2eefd8de86fedb648d4f81ce89  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/lib/codegraph/**` and `src/components/features/codegraph/**` (plus their mirrored tests, and a cross-check of `src/stores/codegraph-store.ts` and the two `analyzer-runner.ts` consumers), explicitly checking for the repo's known query-key/backend-id and typed-client-call conventions too. It returned 5 ranked candidates; verified the top 3 by hand before fixing.
  - **`openExistingAnalysis` could throw synchronously and leave the code-graph view stuck on "analyzing" forever** (`src/lib/codegraph/analyzer-runner.ts` via `src/routes/kt-graph.tsx`): its Storage-mirror fast path (used by cold-rehydrated Docs entries with no live session) falls back to a sandbox lookup through `workspaceFor()` → `getAgentServerClientOptions()` when the mirror upload was incomplete or never happened — and that call throws `NoBackendAvailableError` synchronously whenever there's no live `conversationUrl` and no local backend (e.g. a cloud-kind active backend, or the documented cold-rehydration case where `conversationUrl`/`sessionApiKey` are `null`). The route's caller (`kt-graph.tsx`'s `analyze()`) already sets `status: "analyzing"` before this call and only wraps the later `runAnalysis` call in try/catch — not this one — so the throw became an unhandled rejection and the store never reached `setError`. The very next lines already have a clean "open this repository's conversation" guard for exactly this situation; the call just needed to reach it. Wrapped the call in `.catch(() => null)`, matching the same defensive contract every other I/O helper in this file already documents ("returns null rather than throwing").
  - **Two sibling folders whose names collide after `slug()`'s lossy normalization corrupted one aggregate node** (`src/lib/codegraph/hierarchy.ts`): `buildModuleTree` derives a `moduleId` by slugging the raw folder/container name (e.g. `Foo-Bar` and `foo_bar` both slug to `foo-bar`), but the `groups`/container iteration processes each raw name as a separate group. Two groups landing on the same slugged id made the second group's recursive call share (and silently merge into) the first group's `childrenByParent` entry, then `registerAggregate` overwrote the first group's node with the second group's summary/file count while still linking both group's children under one id. Added a `dedupeId()` helper that appends a `-2`, `-3`, ... suffix when the candidate id is already registered in `ctx.nodesById`, applied at both `moduleId` construction sites in `buildModuleTree` (the `deriveContainers` branch and the real-folder-split branch); left the alphabetical-bucket branch alone since its id already includes a strictly-increasing loop index.
  - **A stale "level failed to load" banner survived navigating to an unrelated node** (`src/stores/codegraph-store.ts`): `levelError`'s own doc comment promises it is "cleared by the next attempt or by navigating anywhere," but `beginLoadLevel`/`setLevel` only cleared it when the new `parentId` matched the one that had failed — so drilling into a different node while an earlier one's error was showing left the old "Failed to load A" banner (with its retry button still pointed at the abandoned node A) on screen next to the new node's loading spinner. `beginLoadLevel` now unconditionally clears `levelError` on any new attempt, matching the documented contract; `navigateTo`'s existing unconditional clear already covered the other documented case, so it was left unchanged.
  - Added regression tests for all three: `__tests__/routes/kt-graph.test.tsx` (cold-rehydration case where `openExistingAnalysis` rejects, asserting the live-session guard's message renders instead of a stuck spinner), `__tests__/lib/codegraph/hierarchy.test.ts` (two same-slug sibling folders each keep their own distinct id and file set), `__tests__/stores/codegraph-store.test.ts` (`beginLoadLevel` for an unrelated node clears a previous node's `levelError`).
  - No new user-facing strings, so no `translation.json`/`make-i18n`/locale changes needed.
- gate: lint pass · test pre-existing-fail (30/759 files failing, 147/5975 tests — same baseline as every run this week; all three touched test files pass in full, 45/45 tests, confirmed by running them directly) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: `fly-deploy.yml` green (run 35186789807, success — the production Fly deploy this playbook actually gates on). `ci.yml` (run 35186789752) still `in_progress` after a ~10-minute poll via the GitHub MCP `actions_list` tool (`gh` unavailable in this sandbox) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and it hasn't failed. `docker.yml`/`release.yml` both failed (runs 35186789846, 35186790213) but match the same pattern already failing on `main` independent of this diff (established every run this week) — not in the playbook's revert criteria.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. For a future run: the bug-hunt subagent's 2 lower-confidence candidates not pursued (below the fix cap): `CodeGraphState.handles[key]` not cleared by `start()` when re-starting analysis under the same key (asymmetric with `reset()`, could leave a stale `AnalysisHandle` behind status flips to "error"); `CodeGraphCanvas`'s ELK re-layout memo key hashes node ids plus edge *count* rather than edge identity (no reachable repro found given the current single call site). Confirmed all agent-server calls in this section already go through the typed `@openhands/typescript-client` classes and no React-Query backend-id issue exists here (this module has no React Query usage at all).

## 06:26 UTC — kt-video — FIXED
- mode: section
- todo: -
- previous run: codegraph
- picked because: TODO.txt empty, findings.txt empty, no incident row has an OPEN + "mitigation: none" this run could build (INC-3's "none" is explicitly out of scope — touches .github/workflows). Section rotation: `codegraph` (most recent worked section) removed from candidates; the other 11 sections all have `runCount` ≤1 so none excluded by the "twice in 6" rule; the 6 sections with `lastRun: null` tied for oldest — `kt-video` picked at random among them (kt-video, security, auth-data-platform, conversation-chat, skills-plugins-mcp, files-terminal-browser).
- base: 9f4b240954e6afed9032df423e0ec45525d2ca58  commit: 38a60ccb4b27e5b2f638d511b367a0e24140381e
- changes:
  - **`ConceptPanel`'s per-segment fade-out was inverted at its own clamp boundary** (`src/components/features/kt-video/concept-panel.tsx`): the exit fade computes `interpolate(frameWithinSegment, [perSegmentFrames - exitFrames, perSegmentFrames], [1, 0.4], {extrapolateRight: "clamp"})`, meant to dim a chain segment from 1 to 0.4 as it approaches its own end. But `opacity = Math.min(enterSpring, exit === 0.4 ? 1 : exit)` special-cased the exact clamped value and replaced it with `1` — full brightness — instead of the intended dim floor. For most segments this never fires (their own window ends before `frameWithinSegment` reaches the clamp value, since `activeIndex` advances first), but the *last* segment's window is `min(segments.length - 1, ...)`-capped and absorbs whatever remainder frames don't divide evenly across `segments.length` (`durationInFrames` is always a multiple of 30 fps but rarely a multiple of `segments.length`, which can be 2-4), so its own `frameWithinSegment` can reach/exceed `perSegmentFrames`, tripping the clamp and the bug — a visible flash back to full opacity right before the concept scene cuts away, on real chain lengths in real durations, not just a contrived edge case. Fixed to `Math.min(enterSpring, exit)`, matching the plain fade-to-dim `CodePanel`'s own exit already uses one file over with no such special case. Added `__tests__/components/features/kt-video/concept-panel.test.tsx` (new — no prior coverage of this component existed): one regression test with a 750-frame/4-segment scene reproducing the exact remainder-frame case (confirmed it fails against the pre-fix code with opacity≈1 instead of 0.4), one sanity check that entry fade still works. Added a `data-testid="kt-concept-active-segment"` to the faded wrapper div so the test can read its computed opacity.
- gate: lint pass · test pass (30/760 files failing, 147/5977 tests — same pre-existing baseline documented in every hourly/focus-fixer run this week; both new tests pass, all touched-area tests pass in full: 28/28 across `__tests__/lib/kt-video/` and `__tests__/components/features/kt-video/`) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, same known gap logged in every run this week, unrelated to this diff)
- ci: `fly-deploy.yml` green (run 35189904137, success — the production Fly deploy this playbook actually gates on). `ci.yml` (run 35189903939) still `in_progress` after a ~10-minute poll (GitHub REST API polled directly, `gh` unavailable in this sandbox) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and it hasn't failed. `docker.yml`/`release.yml` both failed (runs 35189904199, 35189904435) but match the same pattern already failing on `main` independent of this diff (established every run this week) — not in the playbook's revert criteria.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. No new user-facing strings, so no locale-file changes needed.
## 07:37 UTC — conversation-chat — FIXED
- mode: section
- todo: -
- previous run: kt-video (06:26 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `kt-video` (most recent to do real work) excluded; no section had been worked twice in the last 6 working runs. Of the 5 sections still at `lastRun: null` (security, auth-data-platform, conversation-chat, skills-plugins-mcp, files-terminal-browser), `conversation-chat` was picked by explicit random draw (`shuf`) among the tie.
- brainstorm: -
- base: 5388fe9f1eeec18fe3e838801731d5136d8bdcb1  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/components/features/chat/**`, `src/components/features/conversation*/**`, and `src/components/conversation*/**` (plus mirrored tests), checking the repo's own React Query backend-id key convention too. It returned 4 ranked candidates; verified the top 2 (both high confidence) by hand before fixing, and read but did not pursue the other 2 (see notes).
  - **Staged-but-unsent chat attachments leaked from one conversation into the next** (`src/stores/conversation-store.ts`): `images`/`files`/`imagesMarkedUploadAsFile`/`pastedImageNames`/`loadingFiles`/`loadingImages` live in the single global `useConversationStore`, and the only place they were cleared was `CustomChatInput`'s unmount effect or a successful submit — but the chat panel tree (`ConversationMain` → `ChatInterfaceWrapper` → `InteractiveChatBox` → `CustomChatInput`) is deliberately kept mounted across a conversation switch (no `key={conversationId}` anywhere in that tree), so the unmount cleanup never fired on switch. Attach an image in conversation A without sending, switch to conversation B, and the attachment preview (and `canSubmit` state) rides along into B; sending in B silently bundles the attachment meant for A. Fixed by folding the same fields `clearAllFiles()` clears into `resetConversationState()`, which the route (`src/routes/conversation.tsx`) already calls from a `conversationId`-keyed effect for exactly this "reset per-conversation UI state on switch" purpose (it already resets `conversationMode`/`subConversationTaskId`/`planContent`/`shouldHideSuggestions` there). Added a regression test in `__tests__/stores/conversation-store.test.ts` asserting `resetConversationState()` clears all six fields after staging an image+file and marking them.
  - **A stale "load older events" response could merge into a conversation the user had already switched away from** (`src/hooks/use-load-older-events.ts`): `loadOlder()` is an async closure with no `AbortController` and no active-conversation check after its `await` — the hook instance survives a conversation switch (same always-mounted tree as above), so a scroll-triggered older-events fetch for conversation A that resolves after the user has switched to B unconditionally called `addEvents(older)` on the global `useEventStore`, splicing A's history into B's timeline. The store already tracks exactly the invariant needed to guard this (`loadedConversationId`, set atomically by `clearEventsForConversation` on every real switch, per its own doc comment) but `loadOlder` never read it. Added a `stillActiveConversation` check (`useEventStore.getState().loadedConversationId === conversationId`) read right after the fetch resolves, gating both the `addEvents`/model-switch-reseed call and the `hasMore` update on it — a stale response now updates nothing. Updated `__tests__/hooks/use-load-older-events.test.tsx`'s `beforeEach` to seed `loadedConversationId` via `clearEventsForConversation("conv-1")` (matching what the real websocket context does on mount) instead of a bare `clearEvents()`, and added a new regression test that starts a `loadOlder()` call, switches the store to conversation "conv-2" mid-flight, resolves conv-1's page, and asserts the stale page never lands and `loadedConversationId` still reads "conv-2".
  - No new user-facing strings, so no `translation.json`/locale-file changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test pre-existing-fail (30/760 files failing, 147/5979 tests — same documented baseline as every hourly/focus-fixer run this week; total test count is +2 over the last-logged 5977 baseline, exactly the two new regression tests added here, both passing; both touched test files — `use-load-older-events.test.tsx` [11/11] and `conversation-store.test.ts` [8/8] — pass in full and neither appears in the failing-file list) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, unrelated to this diff, same known gap logged in every hourly/focus-fixer run this week)
- ci: `fly-deploy.yml` green (run 35195595816, success — the production Fly deploy this playbook actually gates on). `ci.yml` (run 35195595618) still `in_progress` after a ~10-minute poll (via GitHub REST API directly, `gh` unavailable in this sandbox) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and it hasn't failed; same pattern as every prior run this week where `ci.yml` didn't resolve inside the poll window.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent/incomplete in the fresh checkout (vite.config.ts failed to resolve its own plugin imports before install). Two lower-confidence candidates from the bug-hunt subagent were read but not pursued, below the fix cap: (1) `PendingUserMessages`'s "Stop" button (`src/components/features/chat/pending-user-messages.tsx` `handleStop`) only removes the local optimistic-queue entry and restores text to the composer — it doesn't abort the in-flight `send()`, so a "stopped" message can still land as a real event later if the server-side send succeeds; flagged as medium confidence and somewhat product-ambiguous (the affordance may be intended as "hide locally" only) rather than a clear-cut defect, worth a human product call rather than a unilateral fix. (2) `use-conversation-hooks.ts` and `use-shared-conversation-events.ts` have query keys that omit `backend.id`, unlike the repo's stated multi-backend convention (contrast `use-user-conversation.ts`/`use-sub-conversations.ts`, which include it) — but both fetchers already pin their actual HTTP call to the conversation's own resolved host/session key rather than trusting `getActiveBackend()`, so this isn't an currently-observable defect, just a latent convention deviation that would need the query itself to start trusting the ambient active backend before it bites; flagging for awareness, not a lead to re-investigate as a bug.
## 08:44 UTC — skills-plugins-mcp — FIXED
- mode: section
- todo: -
- previous run: conversation-chat (07:37 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `conversation-chat` (most recent to do real work, also `lastSection`) excluded; the last 6 working runs (conversation-chat, kt-video, codegraph, environment-onboarding, agentops, automations) had no repeats, so nothing else was excluded on the "twice in 6" rule. Of the 4 sections still at `lastRun: null` (security, auth-data-platform, skills-plugins-mcp, files-terminal-browser), `skills-plugins-mcp` was picked by explicit random draw (`shuf`) among the tie.
- brainstorm: -
- base: 40ac7ca5c78eb84ad48f30e5673f372b34dedf18  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/components/features/skills/**`, `src/components/features/plugins/**`, and their directly-related query/mutation hooks and services, explicitly checking the repo's own React Query backend-id key convention documented in `AGENTS.md`/`active-backend-context.tsx` (the same convention this rotation already applied to `agentops` in an earlier run today). It found the area otherwise unusually well-defended (race-safe search debouncing, revert-on-error toggles, dedup logic) with no other correctness bugs, but one real, reproducible, high-confidence violation of that convention showing up as three related findings; verified all three by hand and fixed all three (within the "up to three" cap).
  - **Plugin query keys omitted the active backend's id** (`src/hooks/query/use-plugins.ts`, `use-local-plugins.ts`, `use-plugins-marketplace.ts`, `use-plugin-file-content.ts`, all reading `PluginsManagementService`/`PluginsService`, whose clients are built from `getAgentServerClientOptions()` — resolved from *whichever backend is active at call time*, not at mount time): switching the active backend while `/plugins` was open kept serving the previous backend's cached installed/local/marketplace/file-content data (`staleTime: 10min`), so a user toggling/uninstalling/refreshing what they believed was the visible plugin could silently mutate a same-named (or unrelated) plugin on the *new* active backend instead, while the UI kept showing stale data from the old one. Added `backendId` to `PLUGINS_QUERY_KEYS` (`installed`, `marketplace`, `local`, new `fileContent`) in `src/hooks/query/query-keys.ts`, mirroring the already-correct pattern in `useGitRepositories`/`AGENTOPS_QUERY_KEYS`, and threaded `useActiveBackend().backend.id` through all four query hooks plus every mutation hook that invalidates them (`use-uninstall-plugin.ts`, `use-refresh-plugin.ts`, `use-set-plugin-enabled.ts`, `use-install-plugin.ts`) and the one ad-hoc `ensureQueryData` read in `use-create-conversation.ts` (plugin auto-load snapshot on conversation creation).
  - **Skills catalog query key had the same gap** (`src/hooks/query/use-skills.ts`, delegated to by `use-conversation-skills.ts`): `SkillsService.getSkills` is likewise backend-dependent via `getAgentServerClientOptions()` for local backends, so switching backends kept the Skills page and the conversation slash-command menu showing the previous backend's skill list; disabling a skill by that stale name then wrote into the *new* active backend's `disabled_skills` setting. Added a new `SKILLS_QUERY_KEYS` in `query-keys.ts` and keyed `useSkills` by `(backendId, projectDir)`.
  - Added `__tests__/hooks/query/use-plugins-skills-backend-switch.test.tsx` (new file, 5 tests, modeled on the existing `use-automations-backend-switch.test.tsx` pattern): registers two local backends, asserts each of `usePlugins`/`useLocalPlugins`/`usePluginsMarketplace`/`usePluginFileContent`/`useSkills` issues a second fetch when `setActiveSelection` flips the active backend. Confirmed all 5 fail against the pre-fix code (`git stash` and re-run) and pass with the fix.
  - No new user-facing strings, so no `translation.json`/locale-file changes needed.
- gate: lint pass (one repair: `npx prettier --write` on `use-plugin-file-content.ts` for a wrapping nit, pure formatting) · test pre-existing-fail (30/761 files failing, 147/5984 tests — same documented baseline as every hourly/focus-fixer run this week; total test count is +5 over the last-logged 5979 baseline, exactly the 5 new regression tests added here, all passing; confirmed the failing-file list is the same 30 files by name as every prior run this week, none in the touched area) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, unrelated to this diff, same known gap logged in every hourly/focus-fixer run this week)
- ci: `fly-deploy.yml` (run 35201427291) and `ci.yml` (run 35201427261) both still `in_progress` after a ~10-minute poll via the GitHub MCP `actions_list` tool (`gh` unavailable in this sandbox) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and neither has failed; same "poll window didn't resolve" pattern as several prior runs this week. `docker.yml` (run 35201427194) also still `in_progress`. `release.yml` (run 35201427975) failed but matches the same pattern already failing on `main` independent of this diff (established every run this week) — not in the playbook's revert criteria.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout.
## 09:29 UTC — files-terminal-browser — FIXED
- mode: section
- todo: -
- previous run: skills-plugins-mcp (08:44 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `skills-plugins-mcp` (most recent to do real work, also `lastSection`) excluded; the last 6 working runs (skills-plugins-mcp, conversation-chat, kt-video, codegraph, environment-onboarding, agentops) had no repeats, so nothing else was excluded on the "twice in 6" rule. Of the 3 sections still at `lastRun: null` (security, auth-data-platform, files-terminal-browser), `files-terminal-browser` was picked by explicit random draw (`shuf`) among the tie.
- brainstorm: -
- base: 5532446bc69b1b998d76a281df4e1918c326269b  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/components/features/files/**`, `src/components/features/terminal/**`, `src/components/features/browser/**` (via their `src/components/{files,terminal,browser}/index.ts` re-exports), plus `use-terminal.ts`, `use-workspace-files.ts`, `use-unified-get-git-changes.ts`, `browser-store.ts`, `parse-terminal-output.ts`, and their mirrored tests — also checking the repo's own React-Query backend-id query-key convention. It returned 3 ranked candidates; verified the top one by hand (confirmed real and reproducible), read the second (terminal shrink-detection) but did not pursue it — see notes — and the third was cosmetic-only (stale JSDoc, no behavioral bug).
  - **A conversation switch could leak the previous conversation's changed-files list (or a stale status for a shared path) into the new conversation's Files/Changes tab, cloud backend only** (`src/hooks/query/use-unified-get-git-changes.ts`): `orderedChanges`/`previousDataRef` are component-local state, keyed by nothing — but `ConversationTabContent` (`conversation-tab-content.tsx:42-45`) only remounts the *terminal* tab on a conversation switch (`${tab}-${conversationId}`); the Files tab's key stays the bare string `"files"`, so this hook's instance survives the switch. The query itself is correctly keyed by `conversationId` and set to `refetchOnMount: "always"`, but the merge effect is gated by `!result.isFetching` — for a previously-visited (now cached) conversation, `isFetching` stays true for a beat while `isLoading` is already false, so the Files tab briefly (or, for any path both conversations happen to share, indefinitely, since the merge logic keeps the *old* cached `GitChange` object for any path present in both fetches instead of replacing it with the fresh one) rendered the outgoing conversation's data as if it belonged to the new one. Fixed by resetting `orderedChanges`/`previousDataRef` synchronously (the render-time "adjust state when a prop changes" pattern, not a `useEffect`, so there's no flash of stale data) whenever `conversationId` changes. Added `__tests__/hooks/query/use-unified-get-git-changes.test.tsx` (new — no prior test coverage existed for this hook at all): one regression test that starts conversation A, gets its changes, switches to conversation B mid-fetch, and asserts the list is empty during the switch and never merges A's stale entry for a path both conversations touch; one sanity test that same-conversation refetches still keep newest-on-top ordering (the hook's original, still-intended behavior).
  - No new user-facing strings, so no `translation.json`/locale-file changes needed.
- gate: lint pass (no repair needed) · test pre-existing-fail (30/762 files failing, 147/5986 tests — same documented baseline as every hourly/focus-fixer run this week; total test count is +2 over the last-logged 5984 baseline, exactly the two new regression tests added here, both passing; both directly re-ran touched-area files — `use-unified-get-git-changes.test.tsx` [2/2, new] and `use-workspace-files.test.tsx` [15/15] — pass in full, neither appears in the failing-file list) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, unrelated to this diff, same known gap logged in every hourly/focus-fixer run this week)
- ci: `fly-deploy.yml` green (run 35205563119, success — the production Fly deploy this playbook actually gates on). `ci.yml` (run 35205563146) still `in_progress` after a ~10-minute poll via the GitHub MCP `actions_list` tool (`gh` unavailable in this sandbox) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and it hasn't failed; same "poll window didn't resolve" pattern as several prior runs this week. `docker.yml` (run 35205563227) and `release.yml` (run 35205563825) both failed but match the same pattern already failing on `main` independent of this diff (established every run this week) — not in the playbook's revert criteria.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. The bug-hunt's second candidate — `useTerminal`'s in-place update effect (`src/hooks/use-terminal.ts:190-213`) only detects a shrinking command list via `latest.length < lastCommandIndex.current`, so an atomic same-length store replacement wouldn't retrigger the reset/re-render branch — was read and traced but not pursued: `conversation-websocket-context.tsx`'s `clearTerminal()`+`appendCommands()` pair only fires on an actual `conversationId` change (`useLayoutEffect` deps), and the terminal tab *is* remounted (`key={\`terminal-${conversationId}\`}\`) on exactly that transition, so the old component instance is already unmounted (and its `lastCommandIndex` reset to 0) before the new one mounts fresh from the store — no live repro path found through the real switch flow, medium confidence per the bug-hunt agent's own assessment. Flagging for awareness, not a lead to re-investigate unless a future run finds a call site that clears/reseeds the store without a conversationId change.

## 10:30 UTC — auth-data-platform — IMPROVED
- mode: section
- todo: -
- previous run: files-terminal-browser (09:29 UTC, this file)
- picked because: `TODO.txt` had no live items (only header comments). Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1 already MITIGATED for its buildable half, the OAuth-reconnect half needs the user; INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `files-terminal-browser` (most recent to do real work, also `lastSection`) excluded; the last 6 working runs (files-terminal-browser, skills-plugins-mcp, conversation-chat, kt-video, codegraph, environment-onboarding) had no repeats, so nothing else was excluded on the "twice in 6" rule. Of the 2 sections still at `lastRun: null` (security, auth-data-platform), `auth-data-platform` was picked by explicit random draw (`shuf`) among the tie.
- brainstorm: -
- base: e1124f9d3bb83116927f32864fd0968b33cca97b  commit: 0b924a17d1463be499dc7e32a0e691b740441cb0
- changes:
  - Read through the whole `src/lib/data-platform/**` tree (client, auth-bootstrap, auth-flow, every repository, artifact-store, vector-store, job-queue, errors, index) plus the section's two components (`password-auth-form.tsx`, `auth-page-shell.tsx`) and the `password-direct-reset` Edge Function, hunting for real defects (wrong conditionals, races, stale closures, bad cache keys, missing cleanup). Found none: this area is already heavily hardened — nearly every file carries a comment documenting a previously-confirmed-and-fixed bug (silent RLS-failure swallows, org-bootstrap RLS 42501s, the `getUser()` CORS race, the workspace-id collision with the AgentOps sidecar), and the two auth-flow/UI files already handle their loading/error/empty states correctly.
  - With no bugs found, made 3 real-test-coverage improvements — untested files in this exact section, each with genuinely non-trivial logic that a regression could silently break: (1) `__tests__/lib/data-platform/auth-bootstrap.test.ts` (new) covers `ensureSupabaseSession`'s single-flight dedup (the whole reason that module exists — concurrent callers on a cold page load must share one bootstrap instead of minting separate anonymous users), the not-configured/no-existing-session/sign-in-error paths, and that a later call re-reads the session rather than caching a stale uid. (2) `__tests__/lib/data-platform/repositories/workspace-repository.test.ts` (new) covers `ensureWorkspace`'s upsert payload (including the `name` nullish-default) and error surfacing, plus `listWorkspacesForOrg`'s empty-orgId short-circuit, row mapping, and error handling — previously untested despite being the write path every workspace-scoped RLS policy depends on. (3) `__tests__/lib/data-platform/repositories/memory-repository.test.ts` (new) covers `upsertRecords`' cross-workspace ownership filter (a record for a different `workspaceId` must never be upserted under the caller's workspace — this is the one piece of defense-in-depth this repository has) plus its empty-input short-circuit and error path, and `listRecords`' row mapping (verifying the nullish defaults for `tags`/`confidence`/`conflictsWith`/`tokenCost`).
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (no repair needed) · test pre-existing-fail (30/765 files failing, 147/6007 tests — same documented baseline as every hourly/focus-fixer run this week; total test count is +21 over the last-logged 5986 baseline, exactly the 21 new tests added here across the 3 new files, all passing; all 3 new test files pass in full, 21/21, and none of the pre-existing 30 failing files are in this section) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, unrelated to this diff, same known gap logged in every hourly/focus-fixer run this week)
- ci: `fly-deploy.yml` green (run 35210897439, success — the production Fly deploy this playbook actually gates on). `ci.yml` (run 35210897431) still `in_progress` after a ~10-minute poll via the GitHub MCP `actions_get` tool (`gh` unavailable in this sandbox, polled the REST API directly with `$GITHUB_TOKEN`) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and it hasn't failed; same "poll window didn't resolve" pattern as several prior runs this week. `docker.yml` (run 35210897486) and `release.yml` (run 35210897812) both failed but match the same pattern already failing on `main` independent of this diff (established every run this week) — not in the playbook's revert criteria.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. `security` remains the one section with `lastRun: null` after this run — a natural candidate for the next run once `files-terminal-browser`/this run are excluded by the "most recent" rule.

## 11:23 UTC — security — FIXED
- mode: section
- todo: -
- previous run: auth-data-platform (10:30 UTC, this file)
- picked because: `TODO.txt` had no live items (only header comments); `findings.txt` (`~/.claude/neo-focus/findings.txt`) doesn't exist in this sandbox, so no `MITIGATION for INC-<n>` handoff to skip either. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1 already MITIGATED for its buildable half; INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `auth-data-platform` (most recent to do real work, also `lastSection`) excluded; the last 6 working runs (auth-data-platform, files-terminal-browser, skills-plugins-mcp, conversation-chat, kt-video, codegraph) had no repeats, so nothing else was excluded on the "twice in 6" rule. `security` was the only section still at `lastRun: null`, so no tie-break was needed — matches the note left by the previous run.
- brainstorm: -
- base: bc4ef41b4cdb89f521564e5b9b9d83d1c3d3af5c  commit: (see push below)
- changes:
  - Read the whole strict-scope `security` area (`src/lib/security/{security-activity,security-integrations,security-types}.ts`, `src/routes/security.tsx`) plus its existing test file (`__tests__/routes/security.test.tsx`, already close to exhaustive branch coverage). Found no bugs: this is deliberately inert forward-facing scaffolding for a scanning engine that doesn't exist yet (explicit comments throughout: "no scans yet", "nothing here is persisted"), and the one file with real runtime logic (`security.tsx`'s `useSecurityWorkspaceScope`) is already fully covered and correctly handles every scope/empty/unknown-repository state.
  - Widened the hunt to the app's actual live "security" surface — the risk-flagging/confirmation-gate feature that the literal `src/lib/security` placeholder hasn't been built to replace yet (`SecurityRisk`/`security_risk` usage across the codebase) — and found two real, reproducible bugs, worst first:
  - **The chat confirmation bar could silently drop the HIGH-risk warning for a genuinely high-risk pending action** (`src/components/shared/buttons/conversation-confirmation-buttons.tsx`): `awaitingAction` was picked via `events.slice().reverse().find(ev => { if (ev.source !== "agent") return false; return curAgentState === AgentState.AWAITING_USER_CONFIRMATION; })` — the second condition never reads `ev`, so it's either always-true or always-false across the whole scan and does nothing; the predicate reduces to "the most recent event whose `source` is `\"agent\"`", with no check that the event is actually the pending action. Several non-action event kinds also carry `source: "agent"` (`StreamingDeltaEvent`, `SystemPromptEvent`, `ACPToolCallEvent`) and, per `use-event-store.ts`, streamed text lands as its own permanent `StreamingDeltaEvent` entry rather than being replaced once the response finalizes — so once any of those sorts after the real pending `ActionEvent`, this picked the wrong event, `isActionEvent()` on it was `false`, and `risk` silently fell back to `SecurityRisk.UNKNOWN`, suppressing the `RiskAlert` banner for a real HIGH-risk command. Fixed by extracting the selection into an exported pure `findAwaitingConfirmationAction(events)` that filters for `ev.source === "agent" && isActionEvent(ev)` (dropping the dead condition entirely, since the outer render guard already re-checks `curAgentState`), then simplified the now-redundant `isActionEvent(awaitingAction) ? ... : UNKNOWN` fallback below it to a direct `awaitingAction.security_risk` read. New `__tests__/components/shared/buttons/conversation-confirmation-buttons.test.ts` (5 tests) covers the empty/no-action cases, the normal single-action case, picking the most recent of several actions, and the regression itself — a trailing `StreamingDeltaEvent` after the real pending `ActionEvent` no longer masks its `HIGH` risk.
  - **`useSecurityAnalyzerStore` mutated a log object already held in the store instead of replacing it** (`src/stores/security-analyzer-store.ts`): on a matching update, `appendSecurityAnalyzerInput` wrote `existingLog.confirmation_state = ...` directly onto the array element already in `state.logs`, then returned `{ logs: [...state.logs] }` — a new array wrapper, but around the very same (now-mutated) object references. The store's own comment ("Return new array to trigger re-render") shows the intent was reference-equality-safe updates, but any consumer that memoizes per-log-item by object identity (`React.memo`, a `useMemo` keyed on the log object) would never see the change, since the object it already holds was mutated beneath it rather than swapped. Not currently reachable through any rendered UI (`logs` has no consumer yet — only `appendSecurityAnalyzerInput` itself is wired up, from `src/services/actions.ts`), but it's live production state-management code with the exact "state mutated in place" defect this hunt targets, and it will bite the moment a list view is built on top of it. Fixed by finding the index and writing a new object (`{ ...existingLog, confirmation_state, confirmed_changed: true }`) into a copied array instead of mutating in place, preserving every existing observable behavior (still matches by id first then by awaiting-content, still returns a fresh array reference even when nothing changed). New `__tests__/stores/security-analyzer-store.test.ts` (6 tests) covers appending a new log, the in-place-mutation regression (asserts the original object reference is left untouched and a new object is stored), matching an in-flight confirmation by content across different ids, always returning a new array reference, appending a genuinely distinct log, and `clearLogs`.
  - Confirmed both regression tests fail against the pre-fix code (`git stash` on just the two source files, re-ran both new test files — all 6 tied to the confirmation-buttons fix failed as expected: 4 by `findAwaitingConfirmationAction is not a function` since the export didn't exist yet, plus the pre-fix store behavior; restored the stash) and pass with the fix.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (one repair: `npx prettier --write` on the touched component/test files for wrapping, pure formatting) · test pre-existing-fail (30/767 files failing, 147/6018 tests — same documented baseline as every hourly/focus-fixer run this week; total test count is +11 over the last-logged 6007 baseline, exactly the 11 new regression tests added here across the two new files, all passing; neither new test file appears anywhere in the failure output) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, unrelated to this diff, same known gap logged in every hourly/focus-fixer run this week)
- ci: `fly-deploy.yml` (run 35216715124) and `ci.yml` (run 35216715150) both still `in_progress` after a ~10-minute poll via the GitHub MCP `actions_get` tool (`gh` unavailable in this sandbox) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and neither has failed; same "poll window didn't resolve" pattern as several prior runs this week.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. `npm test` took over 10 minutes (large suite, ~648s once actually running) and was moved to background by the harness; waited it out rather than skipping the gate. The two source-file fixes above touch `src/components/shared/buttons/` and `src/stores/`, outside the section table's literal `src/lib/security`/`src/routes/security.tsx` paths — a deliberate scope call, recorded here for transparency: the literal `security` section is inert placeholder scaffolding with no bug surface and already-thorough tests, while these two files are the app's actual live security-risk-flagging code today. All sections have now had at least one working run since rotation tracking began.

## 12:42 UTC — knowledge-kt — FIXED
- mode: section
- todo: -
- previous run: security (11:23 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `security` (most recent to do real work, also `lastSection`) excluded; the last 6 working runs (security, auth-data-platform, files-terminal-browser, skills-plugins-mcp, conversation-chat, kt-video) had no repeats, so nothing else was excluded on the "twice in 6" rule. Of the 11 remaining sections (all `runCount: 1`), `knowledge-kt` had the single oldest `lastRun` (2026-09-16T20:49:00Z, vs. 22:36:00Z+ for every other candidate) — no tie-break needed.
- brainstorm: -
- base: 1cc025782c3e8cdf8fe92968d0078644879b538b  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/lib/knowledge/**`, `src/components/features/knowledge/knowledge-tabs.tsx`, and `src/routes/kt-list.tsx`/`kt-page.tsx`/`kt-repository.tsx` (plus mirrored tests), explicitly checking the repo's own React Query/store "scope long-lived state by active backend id" convention (documented in `active-backend-context.tsx`'s `setActive()` comment). It returned 4 ranked candidates; fixed the top 3 (worst first), read the 4th (a `citation-parser.ts` regex gap for extensionless/dotfile citations) but did not pursue it — flagged below as a possible upstream-parity constraint, not a clear-cut local bug.
  - **`useKnowledgeStore`'s in-memory `byRepositoryId` map had no active-backend scoping, unlike every other long-lived cache in this app** (`src/stores/knowledge-store.ts`, `src/contexts/active-backend-context.tsx`): entries are keyed only by `"owner/repo@branch"`, with no `backend.id`/`orgId` component, so switching to a different backend that happens to have a same-named repo/branch (a real, supported scenario — two local backends both defaulting to `workspace/project`, or the same GitHub repo tested against two backends) would read the *previous* backend's stale `conversationUrl`/`sessionApiKey`/knowledge content: `RepoCard`/`KtRepository`/`KtPage` would show it as current for the new backend, "Watch KT"/"Regenerate" would call out using the wrong backend's session, and `useKnowledgeRehydration`'s `hasEntry` guard would short-circuit and skip the new backend's own rehydration entirely. Rather than rewire every one of the ~8 read call sites to a composite key (large, riskier surface for this section), added a `reset()` action to the store (drops both `byRepositoryId` and `provisioningByRepositoryId`) and call it from `setActive()` on every genuine backend/org switch — matching the "fresh state on switch" contract the rest of the app already documents there. Every affected route already re-derives its entry from a live conversation or Supabase via `useKnowledgeRehydration`, so this is a cheap reset, not a destructive one.
  - **`KtRefreshCadence`'s Regenerate button had no live-session guard, unlike the equivalent Watch KT guard one file over** (`src/components/features/kt-video/kt-refresh-cadence.tsx`): a cold-rehydrated (Supabase-only) entry intentionally has `conversationUrl: null`/`sessionApiKey: null`/`snapshot.localPath: ""` (`use-knowledge-rehydration.ts`'s own doc comment: "so Watch KT and CodeGraph correctly ask for a live session rather than silently failing"), and `kt-page.tsx`'s `generateWatchManifest` already checks for exactly this and bails with a toast — but `handleRegenerate` called `generateKnowledge` unconditionally, asking DeepWiki to (re-)index a null/empty target and risking overwriting good persisted Knowledge with garbage on a "success". Added the same guard (toast + no-op) `generateWatchManifest` already uses.
  - **`buildEvidenceSummary`'s subsystem-dependency edges could reference subsystems absent from its own truncated "Detected subsystems" list** (`src/lib/knowledge/code-evidence.ts`): `subsystems` is capped to the top 20 (by file count), but the `nameById` map used to build `subsystemEdges` was built from *all* `root.nodes` — so for any repo with >20 real subsystems, an edge could name a subsystem never introduced above it in the same DeepWiki structure-determination prompt text, confusing the very model it's meant to ground. Now built from the same truncated node list.
  - Added tests for all three: `__tests__/stores/knowledge-store.test.ts` (new `reset` describe block), `__tests__/contexts/active-backend-context.test.tsx` (new test asserting `setActive` drops a stale knowledge-store entry on a genuine backend switch), `__tests__/components/features/kt-video/kt-refresh-cadence.test.tsx` (new file — no prior coverage existed — covers both the normal live-session regenerate path and the new cold-rehydration guard), `__tests__/lib/knowledge/code-evidence.test.ts` (new file — no prior coverage existed — 25-subsystem case proving a dropped-node edge never leaks, plus a ≤20 sanity case).
  - No new user-facing strings needing translation: the one new toast string follows the exact same untranslated-raw-string convention `kt-page.tsx`'s pre-existing sibling guard already uses for this identical situation, so no `public/locales/*/openhands.json` changes.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test pre-existing-fail (30/769 files failing, 147/6024 tests — same documented baseline as every hourly/focus-fixer run this week; total test count is +6 over the last-logged 6018 baseline, exactly the 6 new regression tests added here across the four touched/new test files, all passing; individually re-ran all four touched test files plus every other consumer of `useKnowledgeStore`/`code-evidence.ts` in this section — 87 tests total — all pass in full, none of the four touched/new files appear in the failing-file list) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (72 errors, all in `vendor/understand-anything/**` TS6059 rootDir, same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: `fly-deploy.yml` (run 35222818106) green — the production Fly deploy this playbook actually gates on. `ci.yml` (run 35222818015) `test-and-build (ubuntu)` → `Lint` passed, `Test` failed after ~6min (`Build app`/`Build library` skipped as a consequence) — NOT reverted: the failure is `settings-form.test.tsx:106` (`should confirm before saving when submitted from a conversation route`), the exact same file/line/assertion/stack trace this run's own local gate already captured as part of the pre-existing 30-file/147-test baseline (confirmed identical text in this run's local `npm test` output before ever pushing); none of the 4 touched/new files appear in it. `test-and-build (windows)` passed (`Build app` succeeded; `Lint`/`Test` are ubuntu-only per the workflow's own matrix).
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. The bug-hunt's 4th candidate (`citation-parser.ts`'s `CITATION_RE` can't match extensionless files like `Dockerfile` or bare dotfiles like `.gitignore`, causing `quality-review.ts` to raise false "no-citations" flags for pages that cite only such paths) was read but not pursued: its own doc comment says the regex is "a direct port" of the vendored Python backend's citation regex, so changing the pattern itself risks drifting from upstream parity; a real fix belongs in `quality-review.ts` cross-checking `page.relevantFiles` instead, which is a larger, more design-y change better left for a future run or human call. Flagging for awareness, not urgent (cosmetic quality-flag inaccuracy, not data loss/leak).

## 13:36 UTC — settings — FIXED
- mode: section
- todo: -
- previous run: knowledge-kt (12:42 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `knowledge-kt` (most recent to do real work, also `lastSection`) excluded; the last 6 working runs (knowledge-kt, security, auth-data-platform, files-terminal-browser, skills-plugins-mcp, conversation-chat) had no repeats, so nothing else was excluded on the "twice in 6" rule. Of the 11 remaining sections, `settings` had the single oldest `lastRun` (2026-09-16T22:36:00Z, vs. 23:46:00Z+ for every other candidate) — no tie-break needed.
- brainstorm: -
- base: e2d2e7ffaac69b4a1ee6781c3ff870c8c56a4a61  commit: (see push below)
- changes:
  - This fresh checkout's local `main` ref was stale/diverged from `origin/main` (a container-image artifact seen in earlier runs this week too — `git status --porcelain` was already empty, no uncommitted work at risk): `git checkout main` landed on an old local ref (`37149a0`, missing everything from the 04:38 UTC run onward), so it was fast-forwarded to the real `origin/main` tip (`e2d2e7f`) via `git fetch` + `git merge --ff-only` before doing anything else.
  - Dispatched a read-only bug-hunt subagent over `src/components/features/settings/**`, all 13 `src/routes/*settings*.tsx` routes, the settings-related query/mutation hooks, `src/api/settings-service/**`, and their mirrored tests, explicitly checking the repo's own React-Query "scope backend-dependent caches by `backend.id`" convention (`AGENTOPS_QUERY_KEYS`/`useGitRepositories`, already applied to `agentops`/`skills-plugins-mcp` in earlier runs this week). It returned 4 ranked candidates; verified and fixed the top 3 (worst first), read the 4th (an `SdkSectionPage` effect's incomplete dependency array) but did not pursue it — low confidence, masked in practice by `flatValues` changing on nearly every keystroke.
  - **`SettingsService`'s in-memory settings cache leaked across local-backend switches** (`src/api/settings-service/settings-service.api.ts`): `settingsCache` (redacted + encrypted settings, 5-minute TTL) is a bare module-level object with no backend identity in it at all — unlike every React-Query-keyed cache in this app. Switching from local Backend A to local Backend B (a fully supported flow) hit `isCacheValid() && settingsCache.redacted` and returned Backend A's cached LLM model/API key/agent config for up to 5 minutes under Backend B's identity; editing the *active* local backend's host or rotating its API key in place had the same effect. `useSettings()`'s own React-Query key already scopes by `backend.id`/`orgId`, but that only protects React Query's cache — the query function calls straight into this separate, unscoped cache underneath it. Fixed by calling `SettingsService.invalidateCache()` from `retryBootstrapProbe()` in `src/contexts/active-backend-context.tsx`, which already runs on every one of the transitions that can change the effectively active backend's connection identity (a real `setActive` switch, `addBackend`, an active-backend-affecting `updateBackend`, `removeBackend`).
  - **MCP server connection-health was a global singleton, same shape of bug, unfixed** (`src/api/mcp-health/mcp-health-store.ts`): `healthMap` is keyed only by `getMcpServerHealthKey()`'s structural fields (type/name/command/url/auth strategy) — no backend identity — so two backends configuring a same-named MCP server (a common catalog entry) would show the previous backend's stale health verdict after a switch. This is the exact same class of bug `useKnowledgeStore`'s `reset()` was already added to fix (see `active-backend-context.tsx`'s own comment on it), just never extended to MCP health. Added `resetMcpHealthStore()` and call it from `setActive()` right alongside the existing `useKnowledgeStore.getState().reset()`, on a genuine backend switch only (matching that precedent).
  - **`useSettings`'s query key omitted backend host/apiKey, unlike its sibling `useAgentSettingsSchema`** (`src/hooks/query/use-settings.ts`): editing the *active* local backend's host or API key in place (same `backend.id`/`orgId`, just corrected connection details) left the query key unchanged, so React Query kept serving the previous connection's settings until the 5-minute `staleTime` lapsed — even after the cache fix above, nothing told the query itself to refetch. `useAgentSettingsSchema` already guards against exactly this by also keying on `backend.kind`/`backend.host`/`backend.apiKey`; added the same three fields to `useSettings`'s key.
  - Added regression tests for all three: `__tests__/contexts/active-backend-context.test.tsx` (new tests: `setActive` drops MCP health entries on a genuine switch; `setActive`/`updateBackend` call `SettingsService.invalidateCache()` on a real switch or an active-backend-affecting edit, but not on a cosmetic rename), `__tests__/api/mcp-health/mcp-health-store.test.ts` (new `resetMcpHealthStore` case), `__tests__/hooks/query/use-settings.test.tsx` (new file — no prior coverage existed for this hook — asserts a refetch when the active backend's host/apiKey change in place with the same `backend.id`). Confirmed all 5 new/changed assertions fail against the pre-fix code (`git stash` on just the three source files, re-ran the same three test files: 4 failed with the expected "not called"/stale-data assertions, the 5th — `resetMcpHealthStore` — failed with `TypeError: resetMcpHealthStore is not a function`; restored the stash) and pass with the fix.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test pre-existing-fail (30/770 files failing, 147/6029 tests — same documented baseline as every hourly/focus-fixer run this week; total test count is +5 over the last-logged 6024 baseline, exactly the 5 new regression tests added here, all passing; spot-verified the one specific pre-existing failure visible in this run's truncated log output — `settings-form.test.tsx:106`, `should confirm before saving when submitted from a conversation route` — reproduces byte-identical on unmodified `BASE_SHA` via `git stash`; none of the three touched/new test files appear in any failure output) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (72 errors, all in `vendor/understand-anything/**` TS6059 rootDir, same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: `fly-deploy.yml` (run 35228868440) green — the production Fly deploy this playbook actually gates on. `ci.yml` (run 35228868446) still `in_progress` after a ~10-minute poll (`gh` unavailable in this sandbox; polled the GitHub REST API directly with `$GITHUB_TOKEN` via a `Monitor` loop) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and it hasn't failed; same "poll window didn't resolve" pattern as several prior runs this week. This push also landed two intervening commits from `neo-focus-fixer-cloud` first (`3866907` diff-viewer truncation notice, `6fdcfed` home repo-picker GitHub-disconnected message) — rebased past both cleanly, per step 7, before this commit's own push succeeded.
- notes: Ran `npm ci` at the start of this run: `node_modules` was present but broken (`vite` unresolvable from `vite.config.ts`), so a plain presence check wasn't enough this time — worth remembering that "present" doesn't always mean "usable" for a future run's preflight. The fixes here are scoped to `src/contexts/active-backend-context.tsx` (the shared `setActive`/`retryBootstrapProbe` transitions) and `src/api/mcp-health/mcp-health-store.ts`/`src/hooks/query/use-settings.ts`, outside the section table's literal `src/components/features/settings`/`src/routes/*settings*` paths — a deliberate scope call, recorded here for transparency: the root cause of two of the three bugs lives in the shared backend-switching plumbing every settings-reading hook depends on, not in the settings UI itself.

## 14:55 UTC — automations — FIXED
- mode: section
- todo: -
- previous run: settings (13:36 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already `MITIGATED` with only user-only asks left; INC-3's own `mitigation: none` is explicitly out of scope — `.github/workflows`, out of bounds for every routine by standing rule). Fell through to section mode (2b): `settings` excluded (most recent to do real work, also `lastSection`). Of the last 6 working runs (skills-plugins-mcp, files-terminal-browser, auth-data-platform, security, knowledge-kt, settings), none repeated, so nothing else was excluded on the "twice in 6" rule. Of the 11 remaining candidates, `automations` had the single oldest `lastRun` (2026-09-16T23:46:00Z vs. 2026-09-17T00:53:00Z+ for everything else) — no tie-break needed.
- brainstorm: -
- base: 4301ed5f03b4fdea610f90af44018a4cd973649e  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/components/features/automations/**` (detail/, dashboard/, proactivation/, pull-requests/), `src/api/automation-service/**`, and `src/routes/automation*.tsx`, plus their mirrored tests. It ran the existing automation suites first to confirm none of its candidates were already-caught regressions, then returned 3 ranked candidates; verified and fixed the top 2 by hand (worst first), read the 3rd (toggle-enable analytics firing before the mutation's outcome is known) but left it — low severity, telemetry-only, no user-visible impact, and fixing it well would mean auditing every other fire-before-settle tracking call in the file for consistency, better scoped as its own future pass.
  - **The automations list's "Run Now" shared one mutation across every row, and react-query's mutation observer only keeps the *last* `mutate()` call's per-call callbacks** (`src/routes/automations-list.tsx`, `src/components/features/automations/automation-group.tsx`): dispatching automation B while automation A's dispatch was still in flight overwrote the shared `useDispatchAutomation()` observer's internal `#mutateOptions`/`#currentMutation` (confirmed by reading `@tanstack/query-core`'s `mutationObserver.js` directly) — so automation A's `onSuccess`/`onError` callbacks were silently dropped (no toast, ever, for A, success or failure) and `runPendingId` (derived from `dispatchMutation.variables`) snapped to B's id, re-enabling A's still-in-flight row and inviting a duplicate dispatch of the same automation. The same root cause hit `ProactivationFeatureCard.handleRunAll` (`src/components/features/automations/proactivation/proactivation-feature-card.tsx`), which fires one `mutate()` per enabled proactivation automation back to back. Fixed both call sites to use `mutateAsync()` instead — it returns each call's own execution promise, unaffected by the observer's shared state — wrapped in local `try/catch/finally`, and replaced the single shared `runPendingId` with a per-row `pendingRunIds: Set<string>` so each row's spinner/disabled state only ever reflects its own dispatch. `handleRunAll` now awaits `Promise.allSettled` and shows a success and/or error toast based on the real per-automation outcomes instead of one that could be silently dropped.
  - **`ProactivationSetupWizard` never reset its form state on close** (`src/components/features/automations/proactivation/proactivation-setup-wizard.tsx`): the parent (`ProactivationFeatureCard`) always renders the wizard and only toggles `isOpen`, so the instance never unmounts — but unlike its sibling modals (`EditAutomationModal`, `AddAutomationModal`, both of which already reset state in a `useEffect` keyed on `isOpen`, the latter with a comment about this exact class of bug), the wizard had no such effect. Cancelling out partway through (repos picked, watch areas set, stepped into "review") and reopening later resumed on the stale step with the previous run's selections still filled in — clicking "Enable" from there would create real, persisted automations against a stale repo selection the user never re-confirmed. Added the same reset-on-reopen `useEffect` pattern its siblings already use.
  - Added regression tests: `__tests__/routes/automations-list.test.tsx` (new test dispatching two automations concurrently — automation 1 held pending via a deferred promise, automation 2 resolved immediately — asserting automation 1's row stays disabled/`aria-busy` through automation 2's resolution, then correctly re-enables once its own dispatch settles; confirmed this fails against the pre-fix code — reverting just the `mutateAsync` change back to `mutate` + per-call `onSettled` leaves automation 1's button stuck disabled forever, since its `onSettled` also silently never fires under the same observer-overwrite bug), `__tests__/components/automations/proactivation-setup-wizard.test.tsx` (new test: add a repo manually, advance to the watch step, flip on a non-default watch area, close and reopen, assert the wizard is back on step one with an empty repository list; confirmed it fails against the pre-fix code, reopening on the stale "watch" step with the checkbox still checked and the repo still attached).
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test pre-existing-fail (30/770 files failing, 147/6034 tests — same documented baseline as every hourly/focus-fixer run this week: identical 30 failed file count and 147 failed test count to the last-logged baseline; spot-verified by `git stash`-ing this whole diff and re-running `__tests__/root.test.tsx` directly against unmodified `BASE_SHA` — identical 18/22 failures, byte-for-byte, confirming the failures predate this change; neither of the two touched/new test files appears anywhere in the failure output) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (72 errors, all in `vendor/understand-anything/**` TS6059 rootDir, same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: `fly-deploy.yml` (run 35237517957) green — the production Fly deploy this playbook actually gates on. `ci.yml` (run 35237517990) still `in_progress` after a ~10-minute poll via the GitHub MCP `actions_get` tool (`gh` unavailable in this sandbox) — not reverted, since only a confirmed failure on `ci.yml`/`fly-deploy.yml` is revert criteria and it hasn't failed; same "poll window didn't resolve" pattern as several prior runs this week.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout (confirmed absent both by an `ls` check and by `npm ci` itself needing to install ~1555 packages from scratch).

## 15:45 UTC — agentops — FIXED
- mode: section
- todo: -
- previous run: automations (14:55 UTC, this file)
- picked because: `TODO.txt` and `findings.txt` had no live items. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already `MITIGATED` with only user-only asks left; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): `automations` (most recent to do real work, also `lastSection`) excluded; of the last 6 working runs (files-terminal-browser, auth-data-platform, security, knowledge-kt, settings, automations), none repeated, so nothing else was excluded on the "twice in 6" rule. Of the 11 remaining candidates, `agentops` had the single oldest `lastRun` (2026-09-17T00:53:00Z, vs. 04:38:00Z+ for everything else) — no tie-break needed.
- brainstorm: -
- base: a915e43a7dcb90591ff8d94b3e069d9a47bc9454  commit: (see push below)
- changes:
  - Dispatched a read-only bug-hunt subagent over `src/components/features/agentops/**`, `src/api/agentops-service/**`, `src/hooks/query/use-agentops.ts`, `src/routes/agentops*.tsx`, `scripts/agentops/**`, and mirrored tests, explicitly told not to re-report the two bugs this same section's 00:53 UTC run already fixed today (Resume-on-error, backend-scoped query keys). It returned 3 ranked candidates; verified and fixed the top 2 by hand (worst first), read the 3rd (a workspace's "autonomy level" never actually gating the runtime's `confirmation_mode` — `requiresConfirmationMode` in `policy.mjs` has zero callers despite `AGENTS.md` and `budgets-panel.tsx` both asserting it's real, server-side enforcement) but did not pursue it: wiring it up for real means finding/touching the conversation-start payload construction path, a materially larger and more architecturally significant change than this run's size cap and "up to three, worst first" bug-fix scope should attempt unilaterally — flagged below for a human/future-run call, and NOT filed as an INC-per-the-rules-scoped issue since it's code-fixable, just not safely doable in one hourly pass.
  - **"Stop" on an idle or waiting-for-confirmation run silently became a Pause, but the audit log lied and said "cancelled"** (`scripts/agentops/run-control.mjs`, `src/components/features/agentops/run-controls.tsx`): the module's own doc comment already states the precise runtime contract — `interrupt()` cancels an in-flight `arun()` task, else falls back to `pause()`, which itself only acts on IDLE/RUNNING — and `evaluateRunControl` already special-cases this correctly for `status === "paused"` (refused, matching test coverage), but never applied the same reasoning to `"idle"` or `"waiting_for_confirmation"`. For `"idle"`: `pause()`'s IDLE/RUNNING fallback actually succeeds, so the run really does move to PAUSED — but there was no in-flight task, so recording it as `run.cancel`/"cancelled" misdescribed what happened. For `"waiting_for_confirmation"`: it is neither IDLE nor RUNNING, so even the `pause()` fallback is a complete no-op — Stop did nothing at all, and the audit lied outright. Compounding this, `RunControls`'s `canStop` didn't exclude either status, so both a Resume and a misleading Stop button rendered simultaneously on an idle run, and a real no-op Stop button rendered on a run actually waiting for the user's approve/reject decision in the Approvals queue. Added `REFUSALS.stopIdle`/`REFUSALS.stopWaitingForConfirmation` and two new `action === "cancel"` guards in `evaluateRunControl` (refusing both, same 409 pattern as `stopPaused`), and excluded both statuses from `canStop` in `run-controls.tsx` so the button is not offered at all, per the file's own stated "a control whose precondition isn't met is not rendered" contract. Added regression tests: `__tests__/scripts/agentops-run-control.test.ts` (2 new `evaluateRunControl` cases, 2 new `controlRun` cases mirroring the existing `stopPaused` pattern) and `__tests__/components/features/agentops/run-controls.test.tsx` (2 new render cases for `"idle"`/`"waiting_for_confirmation"`) — confirmed all 6 fail against the pre-fix code (`git stash` on just the two source files, re-ran both test files: the idle/waiting-for-confirmation Stop button rendered where it shouldn't have) and pass with the fix.
  - **`useSaveAgentOpsPolicies`'s success handler could attribute one backend's saved-policy response to a different, since-switched-to backend's cache** (`src/hooks/query/use-agentops.ts`): `onSuccess` closed over `backend.id` from `useActiveBackend()` at the hook's render scope, but React Query's mutation observer re-binds its hook-level `onSuccess` to whichever render is current when the mutation later settles — not the render active when `mutate()` was called. `agentops-service.api.ts`'s `request()` helper resolves the actual PUT's host/API key at call time (confirmed by reading it directly), so the write really does go to the backend active when Save was clicked; but if the operator switches the active backend (reachable via the persistent sidebar, without leaving `/agentops/budgets`) before that backend's collector responds, the component re-renders with the new backend, and the observer's `onSuccess` that eventually fires closes over the *new* backend's id — writing the *old* backend's saved policies into the *new* backend's `policies`/`budgets` query cache. Fixed by adding `onMutate: () => ({ backendId: backend.id })`, which runs synchronously as part of the same `mutate()` call (before any later re-render can change what `observer.options` holds) and therefore still captures the backend the request was actually sent to; `onSuccess` now reads `context.backendId` instead of the closed-over `backend.id`. Added `__tests__/hooks/query/use-save-agentops-policies-backend-switch.test.tsx` (new file — no prior test coverage of this hook existed at all): starts a save under Backend A with a deferred promise, switches the active backend to B mid-flight, resolves A's response, and asserts the saved data lands in Backend A's cache, not Backend B's. Confirmed it fails against the pre-fix hook (`git stash` on just this file: the data landed under `undefined`/Backend B instead) and passes with the fix.
  - No new user-facing strings (the two new refusal messages are collector-side error reasons returned as plain API text, matching every other `REFUSALS` entry in this file — not `I18nKey`-translated UI copy), so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test pre-existing-fail (30/771 files failing, 147/6048 tests — same 30-file/147-test failure baseline documented in every hourly/focus-fixer run this week; total test count is +7 over the last-logged 6034 baseline — exactly the 7 new regression tests added here (4 in `agentops-run-control.test.ts`, 2 in `run-controls.test.tsx`, 1 in the new backend-switch file); individually re-ran all three touched/new test files — 26 + 1 = 27 tests total — all pass in full, and none appear anywhere in the failing-file list) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: not checked yet as of this log entry — will poll after push per step 8.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. This fresh checkout's local `main` ref was also stale/diverged from `origin/main` again (same container-image artifact seen in several earlier runs this week — `git status --porcelain` was already empty, no uncommitted work at risk); fast-forwarded via `git checkout main && git pull --ff-only` before anything else, landing 24 intervening commits from earlier runs today. For a future run: the bug-hunt's autonomy-level/`confirmation_mode` gap (see above) is real and would be a good candidate for a dedicated pass — trace how conversation-start payloads are built (likely `agent-server-conversation-service.api.ts` or similar) and whether `getWorkspacePolicy(...).autonomyLevel` can be threaded into it without an API shape change on the runtime side.

## 16:35 UTC — environment-onboarding — FIXED
- mode: section
- todo: -
- previous run: agentops (15:45 UTC)
- picked because: `environment-onboarding` had the oldest `lastRun` (04:38 UTC) among sections not worked in the last 6 working runs and not run last (agentops); read `.neo-cloud/hourly-state.json` and the day's run log first per the rotation rule.
- base: 0ff353ef9e5a65091e9b3be7553cfb06fd8b0c42  commit: f35d72766389d9517e9526bd7d16da222ca1ab9b
- changes:
  - **`complete_setup` never ended the onboarding session row it started** (`src/services/onboarding-control.ts`, `src/hooks/query/use-onboarding-session.ts`): `useStartOnboardingSession` inserts an `onboarding_sessions` row with `status: "active"`, and a partial unique index in Postgres allows only one active session per org (by design — a colleague who opened the studio moments earlier should join the same thread). `useOnboardingSession` always resolves to that one `status: "active"` row. The agent's `complete_setup` command (`onboarding-control.ts`'s dispatcher, reached from the `onboarding_brief.ts` system prompt's own step 5, "Finish") pushed the summary card and posted a "completed" receipt but never updated that row's `status` — so the session stayed `active` forever. Concretely: after a company finishes onboarding once, every future visit to `/environment/setup` (a new hire re-running it, or wanting a second pass after infra changes) resumes the exact same "finished" conversation and transcript forever, because `useOnboardingSession`'s `.eq("status", "active")` query keeps returning it, and `useStartOnboardingSession`'s insert would just hit the same unique-index conflict and return that stale row too. A `useEndOnboardingSession` mutation hook already existed for exactly this (`status: "completed"`) but was never called from anywhere in the app — genuinely dead code, not a stylistic leftover. Fixed by adding a plain (non-hook) `completeOnboardingSessionForConversation(conversationId)` function next to it in `use-onboarding-session.ts` — a plain async function rather than reusing the mutation hook, because `complete_setup` is dispatched from a WebSocket event handler in `conversation-websocket-context.tsx`, outside any component render, where a hook cannot be called. Threaded `conversationId` onto `handleOnboardingControlAction`'s existing `DispatchContext` (the call site already had it in scope, building `postResult` from the same value) and call the new function, best-effort, in the `complete_setup` case before posting the receipt — a failed update must not stop the agent from telling the user setup is done, so it's `void`'d with a swallowed `.catch`.
  - Added two tests to `__tests__/services/onboarding-control.test.ts`: `complete_setup` calls `completeOnboardingSessionForConversation` with the dispatch context's `conversationId` and still posts the "completed" receipt; a caller with no `conversationId` (kept possible via the now-optional field, for callers/tests that don't need this bookkeeping) skips the call but still completes normally. Added a `vi.mock` for `#/hooks/query/use-onboarding-session` alongside the file's existing service mocks.
  - No user-facing strings changed, so no `public/locales/*/openhands.json` edits needed.
  - Read every file under `src/components/features/environment/`, `src/components/features/onboarding/`, `src/routes/environment-*.tsx`, and the onboarding-specific stores/hooks they depend on (`onboarding-studio-store.ts`, `onboarding-copilot-store.ts`, `use-user-providers.ts`, `use-connections-realtime-sync.ts`, `use-onboarding-session.ts`, `use-environment-readiness.ts`, `use-environment-profile.ts`, `oauth-receipt-guard.ts`) before finding this; no other correctness defect stood out, so this one bug was fixed rather than padding out to three.
- gate: lint pass (no repair needed) · test pre-existing-fail (30/771 files failing, 147/6054 tests — same 30-file/147-test failure baseline documented in every hourly/focus-fixer run this week; spot-verified by `git stash`-ing this diff and re-running `__tests__/root.test.tsx`, `src/hooks/use-app-title.test.tsx`, and `__tests__/components/features/sidebar/sidebar.test.tsx` directly against unmodified `BASE_SHA` — identical 45/49 failures in that subset, byte-for-byte; the touched test file, `__tests__/services/onboarding-control.test.ts`, passes in full, 22/22, and does not appear anywhere in the failure output) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: `fly-deploy.yml` (run 35247903138, "Deploy Agent Server") green — the production Fly deploy this playbook actually gates on. `ci.yml` (run 35247903109) red: `test-and-build (windows)` passed, `test-and-build (ubuntu)` failed at the `Test` step (`Lint` passed first; `Build app`/`Build library`/`Verify package contents` skipped as a consequence) — but NOT reverted: same shape as every hourly run's already-documented pre-existing 30-file/147-test baseline from earlier today (this run's own local gate, above, reconfirmed the identical 30/147 failure counts against unmodified `BASE_SHA` via `git stash`), and `__tests__/services/onboarding-control.test.ts` does not appear anywhere in that baseline's failure list. `docker.yml`'s `Build & Push (arm64)` job and `release-please` also failed (runs 35247903205, 35247903715) but match the same already-failing pattern noted on the immediately preceding commits this week — not in the playbook's revert criteria regardless.
- notes: Fresh checkout had no `node_modules`; ran `npm ci` before the gate per this run's instructions. `git status --porcelain` was already empty before `git checkout main && git pull --ff-only`, so no work was at risk fast-forwarding the 28 intervening commits from earlier runs today onto this container's stale `main` ref (same recurring container-image staleness noted in prior runs' logs, not a new issue). First `git push origin main` was rejected non-fast-forward (`neo-focus-fixer-cloud` landed `8db8a44` "fix(knowledge): render unresolved citation markers as inert text, not dead links" in between) — `git fetch origin main && git rebase origin/main` was clean, `npm run lint` and `npx vitest run` on the touched files both passed again post-rebase, and the retried push landed as `f35d727`. Direct `git push` over HTTPS also isn't authorized in this sandbox for a plain non-rejected push (`git push -u origin main` returned `HTTP 403` before the rebase even resolved the ref state, distinct from the ordinary non-fast-forward message) — worth flagging for a human: future runs may need the GitHub MCP tools (`push_files`) as a fallback if a plain `git push` keeps 403ing after a clean rebase.
## 17:46 UTC — codegraph — FIXED
- mode: section
- todo: -
- previous run: environment-onboarding (16:35 UTC)
- picked because: read `.neo-cloud/hourly-state.json` and the last ~14 working runs in today's log first, per the rotation rule. The most recent run that did real work (`environment-onboarding`, 16:35 UTC) was excluded; none of the last 6 working runs (environment-onboarding, agentops, automations, settings, knowledge-kt, security) repeated a section twice, so nothing else was excluded on that ground. Of the remaining candidates, `codegraph` had the oldest `lastRun` (05:40 UTC).
- base: 98f44654a886394a9c75307e970605b62f44c9ee  commit: (recorded in the post-push follow-up commit, per established convention)
- changes:
  - Read every file under `src/lib/codegraph/` and `src/components/features/codegraph/`, plus `src/routes/kt-graph.tsx` and `src/stores/codegraph-store.ts` that drive them, against their existing tests. Most of the section was already solid (well-tested from prior passes), but `kt-graph.tsx`'s forced-rebuild state machine had a real defect: when a user clicks "Rebuild" (or the stale banner's reanalyze) on a graph that is already valid and on screen, and HEAD has moved since (`retargeted`), the route keeps the old graph visible while the rebuild runs (`keepOnScreen`) — but if that rebuild then *fails* (sandbox timeout, network blip, anything), the `catch` block unconditionally called `settle()`, which pins the repository to the new (never-actually-analyzed) commit and calls `reset(key)`, deleting the old, still-perfectly-good graph from the store. The user's working graph was silently destroyed by a transient rebuild failure and replaced with a full error screen. Confirmed real by temporarily reverting the fix and watching a new regression test fail exactly this way, then re-applying it.
  - Fixed in `src/routes/kt-graph.tsx`: the `catch` block now branches on `keepOnScreen` — when true, it clears the in-progress flag (new `endRebuild` store action) and discards only the orphaned retargeted attempt (`reset(targetKey)` when `retargeted`), leaving the working graph's `status`/`meta`/`levels` untouched; `setError`/`settle()` (which pins + deletes) now run only on the `!keepOnScreen` path, where there was no valid graph to protect in the first place. The failure is still recorded via the existing `emitCodeGraphMilestone(..., { kind: "analysis.failed" })` call, so it surfaces on the activity feed rather than being silently swallowed.
  - Added `endRebuild` to `src/stores/codegraph-store.ts` (clears `rebuilding` only, unlike `setError`, which blanks `status`/`meta`) plus a unit test in `__tests__/stores/codegraph-store.test.ts` asserting it leaves `status`/`meta`/cached levels untouched.
  - Added a regression test to `__tests__/routes/kt-graph.test.tsx` ("keeps the existing graph on screen when a forced rebuild to a new HEAD commit fails") that mocks `runAnalysis` to reject after `resolveHeadCommitSha` reports a moved HEAD, and asserts the old key stays `"ready"` with `rebuilding: false`, the breadcrumbs/graph stay mounted, no error text appears, the orphaned target-key store entry is gone, and no premature commit pin was written.
  - Also fixed pre-existing cross-test pollution in `__tests__/routes/kt-graph.test.tsx`: `pinnedCommitByRepositoryId` was never reset between tests in the same `describe` block (only `byKey`/`handles` were), so a pin set by the stale-banner-rebuild test could leak into later tests sharing the same `REPOSITORY_ID`. Added it to the existing `beforeEach`/`afterEach` resets across all three `describe` blocks in the file.
  - No user-facing strings changed, so no `public/locales/*/openhands.json` edits needed.
- gate: lint pass (no repair needed) · test pre-existing-fail (30/771 files failing, 147/6057 tests — same baseline documented in every hourly/focus-fixer run this week; spot-verified by `git stash`-ing this diff and re-running `__tests__/root.test.tsx`, `src/hooks/use-app-title.test.tsx`, and `__tests__/components/features/sidebar/sidebar.test.tsx` directly against unmodified `BASE_SHA` — identical 45/49 failures in that subset; the touched/added test files — `__tests__/routes/kt-graph.test.tsx` (13/13) and `__tests__/stores/codegraph-store.test.ts` (20/20) — pass in full and do not appear anywhere in the failure output; also independently confirmed the fix by reverting only the two `src/` changes and watching the new regression test fail, then reapplying) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors — all 72 errors confirmed inside `vendor/understand-anything/**`, none touching this diff's files — same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: (recorded in the post-push follow-up commit)
- notes: Fresh checkout had no `node_modules`; ran `npm ci` before the gate per this run's instructions. `git status --porcelain` was empty before `git checkout main && git pull --ff-only` (the "leaving N commits behind" message was ordinary detached-HEAD-to-branch noise from the container's initial checkout, not real divergence — `git log` before/after matched `origin/main` exactly). This run's `commit:`/`ci:` fields are filled in by a short follow-up commit after push and a best-effort CI check, matching this repo's established two-commit pattern for runs that can't know their own final SHA or post-push CI status ahead of time (see e.g. the immediately preceding `98f4465` commit).
## 18:36 UTC — kt-video — FIXED
- mode: section
- todo: -
- previous run: codegraph (17:46 UTC)
- picked because: TODO.txt and findings.txt had no live items; step 0 found no OPEN incident with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED with real fixes, INC-3's `mitigation: none` explicitly says the fix touches `.github/workflows` and is out of scope for every routine). Fell through to section mode: the last 10 working runs today covered 10 distinct sections (a full rotation) with `codegraph` most recent (excluded); no section repeated within the last 6 working runs, so nothing else was excluded on that ground; of the remaining candidates, `kt-video` had the oldest `lastRun` (06:25 UTC).
- base: 4887dedec933a31b6d82772bbdfe84752b43512e  commit: 5f0ff9ff9fda41cec3046291f4b74f3959c11d5f
- changes:
  - Read every file under `src/lib/kt-video/` and `src/components/features/kt-video/` plus their tests, then traced the one route that actually wires them together, `src/routes/kt-video-tab.tsx` (untested — no `__tests__/routes/kt-video-tab.test.tsx` existed before this run).
  - Found a real defect there: `useSelectedFileContents` rebuilt its `contents` record as a fresh plain-object literal on every render, regardless of whether the underlying file text had actually changed. `KtVideoTab`'s `manifest = useMemo(() => buildKtManifest(...), [conversation?.selected_repository, contents])` therefore got a new `contents` reference — and rebuilt the whole manifest — on every re-render, including ones with no connection to KT video at all (e.g. `useWorkspaceFiles`/`useUnifiedGetGitChanges` background polling elsewhere in the same component). `useSceneNarration(manifest, playerRef, enabled)`'s effect depends on `manifest`, so every such churn tore the effect down and rebuilt it: `synth.cancel()` cut off whatever sentence was mid-speech, and `lastSpokenSceneId.current` reset to `null`, so a playing video's narration would keep getting cancelled and force-restarted while the user watched, for reasons unrelated to the video itself.
  - Fixed in `src/routes/kt-video-tab.tsx`: `contents` is now built inside a `useMemo` keyed on each of the 8 fixed `useWorkspaceFileContent` slots' own `.data` (react-query keeps `.data` referentially stable when unchanged), not on a freshly-allocated object every call. The inner `slots` memo — which the caller's `effectiveSelected` array (`selected ?? changedPaths.slice(...)`) recreates by reference every render even when its contents haven't changed — is now keyed on a stringified form of `paths` instead of the `paths` reference, otherwise the outer fix would still churn through this input. `isLoading` is now a cheap direct check rather than being computed inside the loop that used to build `contents`. Exported `useSelectedFileContents` so it's directly testable.
  - Added `__tests__/routes/kt-video-tab.test.tsx` (3 tests, via `renderHook` against a mocked `#/hooks/query/use-workspace-file-content`): asserts `contents` keeps the same object identity across a re-render when the mocked file data is unchanged (would fail against the pre-fix implementation), asserts it does rebuild (new identity, new values) once the underlying file data actually changes, and asserts `isLoading` only reflects slots that are actually selected and loading.
  - No user-facing strings changed, so no `public/locales/*/openhands.json` edits needed.
- gate: lint pass (no repair needed) · test pre-existing-fail (30/772 files failing, 147/6061 tests — file/test counts and content match the identical pre-existing baseline already confirmed unrelated to any diff in every hourly run today, e.g. the 00:53 UTC and 17:46 UTC entries above; the new `__tests__/routes/kt-video-tab.test.tsx` (3/3) passes in full and does not appear in the failure output) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention; verified twice, once before and once after the final edit) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, same known gap logged in every hourly/focus-fixer run today, unrelated to this diff)
- ci: push was rejected non-fast-forward by `neo-focus-fixer-cloud`'s `1daca16` landing first (exactly the expected, documented scenario); rebased cleanly onto it, re-ran `lint` + `npx vitest run __tests__/routes/kt-video-tab.test.tsx` (both pass), pushed as `5f0ff9f`. `fly-deploy.yml` green (run 35260313423, success — the production Fly deploy this playbook actually gates on). `ci.yml` red (run 35260313631, `test-and-build (ubuntu)` → `Lint` passed, `Test` failed after ~6.5min, `Build app`/`Build library` skipped as a consequence) but NOT reverted: identical failure shape to the immediately preceding commit `1daca16`'s own `ci.yml` run (also `Lint` pass / `Test` fail), which is itself the same pre-existing baseline documented in every hourly run today (see `gate:` above) — no new failure introduced by this diff. `docker.yml`/`release.yml` also failed (runs 35260313580, 35260314131) but match the exact same pattern already failing on `1daca16` — not in the playbook's revert criteria regardless.
- notes: Fresh checkout had no `node_modules`; ran `npm ci` before the gate per this run's instructions. First attempt at the `slots`/`contents` memo keys used a raw null-byte-joined string as the dependency key, which silently made the whole `src/routes/kt-video-tab.tsx` file contain a control character and get treated as a binary file by `git diff`/`grep` — caught before committing and replaced with `JSON.stringify(paths)`, which is both safe and a clearer key. Worth a general callout for any future run using a similar "serialize an array into one memo key" trick: avoid embedding literal control characters in source, `JSON.stringify` is the safe default.
## 19:47 UTC — conversation-chat — FIXED
- mode: section
- todo: -
- previous run: kt-video (18:36 UTC)
- picked because: TODO.txt and findings.txt had no live items; step 0 found no OPEN incident with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED with real fixes; INC-3's `mitigation: none` explicitly says the fix touches `.github/workflows` and is out of scope for every routine). Fell through to section mode: the last 10 working runs today covered 10 distinct sections, most recent `kt-video` (excluded); no section repeated within the last 6 working runs, so nothing else was excluded on that ground; of the remaining 11 candidates, `conversation-chat` had the oldest `lastRun` (07:37 UTC).
- base: e1c3073cb008449e6528b3a410ee1d050764acee  commit: 39c4c415a957274f00177b70ce0a5a38492363c5
- changes:
  - Delegated an initial read-only bug hunt across `src/components/features/chat/**`, `src/components/features/conversation*/**`, and `src/components/conversation*/**` (plus their `__tests__` mirrors) to a research subagent, then independently verified each of its three findings against the actual source before touching anything. All three were real, confirmed by reverting each fix in turn and watching a new regression test fail exactly the described way, then reapplying.
  - Fixed in `src/components/features/chat/chat-interface.tsx` (`handleSendMessage`): only the final `send(...)` call was wrapped in try/catch; an earlier rejection from `convertImageToBase64` (corrupt/unreadable pasted image) or `uploadFiles` (network error, 413, 500, ...) propagated out of the async handler uncaught, since the caller (`InteractiveChatBox`'s `onSubmit`) never awaits it — an unhandled rejection with no toast, no pending bubble, and the user's message and attachments silently vanishing. Wrapped the image-conversion + upload step in its own try/catch that shows `displayErrorToast` and returns before anything is enqueued or sent. Added a regression test in `__tests__/components/chat/chat-interface.test.tsx` that makes the mocked upload mutation reject and asserts an error toast fires while `send` is never called and no pending bubble appears.
  - Fixed in `src/components/conversation-events/chat/messages.tsx`: the memoized `Messages` component's custom equality comparator only checked array length and the *last* element's identity/id. `handleEventForUI` (`src/utils/handle-event-for-ui.ts`) resolves an `ACPToolCallEvent` (an ACP sub-agent tool call) by replacing it **in place at its original index**, not at the tail, once any later event has already been appended — so once a card wasn't the newest thing on screen, its terminal (`completed`/`failed`) update would leave length and the last element unchanged, and the memo would (wrongly) skip the re-render entirely, leaving the tool-call card stuck showing "running" forever even though the store already had the real result. Replaced the last-element-only check with a full elementwise reference comparison (still O(n) reference checks, still skips a re-render when nothing actually changed). Added a regression test to `__tests__/components/conversation-events/chat/messages-model-messages.test.tsx` that rerenders `<Messages>` with a non-tail ACP event replaced and asserts the new (failed) content is reachable after expanding the card.
  - Fixed in `src/components/features/chat/chat-interface.tsx`: `ChatInterface` stays mounted across a conversation switch (same route, new `:conversationId`), but the `preserveScrollPosition` ref used to restore scroll position after "load older" had no dependency on/reset for `conversationId`. If a user triggered "load older" near the top of conversation A and switched to conversation B before that page resolved, the leftover snapshot (conversation A's `scrollHeight`/`scrollTop`) would later be applied to conversation B's now-unrelated DOM once B's own history streamed in, yanking the scroll position to a bogus computed value. Added a `useEffect` keyed on `conversationId` that clears the ref on every conversation change. Added a regression test that triggers "load older" on conversation A, switches to conversation B before the page resolves, then grows the event list and asserts the buggy delta-based scrollTop value is never applied.
  - No user-facing strings changed, so no `public/locales/*/openhands.json` edits needed.
- gate: lint pass · test pass (full `npm test` exit 0 — no pre-existing baseline failures observed this run) · build pass (checked for and reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt`/absolute-path rebuild byproduct before committing, per established convention) · build:lib pass
- ci: fly-deploy.yml green (run 35267176998, "Deploy Agent Server" success — the production Fly deploy this playbook actually gates on) — no revert needed. `ci.yml` (run 35267177075) was still `test-and-build (ubuntu)` in-progress after the full ~10-minute best-effort polling window (`test-and-build (windows)` already passed); no failure observed on either of the two workflows this playbook reverts on, so nothing to revert. `docker.yml` failed (`Build & Push (arm64)`/`(amd64)`, run 35267177023) and `release.yml` failed (run 35267177492) but both match the identical pre-existing failure shape already documented on the immediately preceding commit `e1c3073` in the 18:36 UTC entry above — not new, not in this playbook's revert criteria.
- notes: Fresh checkout had no `node_modules`; `npm ci` was run before the gate per this run's instructions (actually triggered by the background bug-hunt subagent's own `npm install`, confirmed present before the gate). The scroll-restore regression test needed the scrollTop property pinned as a stable getter (mirroring the existing pagination tests' `setScrollMetrics` helper) — an earlier draft let the auto-scroll-to-bottom rAF clobber the value between the manual "load older" trigger and the assertion, causing an intermittent false pass when run in isolation vs. the full file; fixed by recording writes instead of applying them.
## 20:35 UTC — skills-plugins-mcp — FIXED
- mode: section
- todo: -
- previous run: conversation-chat (19:47 UTC, this file)
- picked because: `TODO.txt` had no live item; `findings.txt` had no `MITIGATION for INC-<n>:` item to defer to `neo-focus-fixer-cloud`. Step 0 found no `OPEN` incident row with a buildable `mitigation: none` in scope (INC-1/INC-4 already MITIGATED; INC-3's own `mitigation: none` is explicitly infra/`.github/workflows` scope, out of bounds for every routine by standing rule). Fell through to section mode (2b): the most recent run that did real work (`conversation-chat`, also `lastSection`) was excluded; across the last 6 working runs (conversation-chat, kt-video, codegraph, environment-onboarding, agentops, automations) no section repeated, so nothing else was excluded on the "twice in 6" rule. Of the remaining 11 candidates, `skills-plugins-mcp` had the oldest `lastRun` (08:44 UTC today) by a wide margin — next-oldest was `files-terminal-browser` at 09:29 UTC.
- brainstorm: -
- base: bb3360d05fbd09f55ed8aa3155c7098b3466397b  commit: 4597799d8808dce65dbd25e04e8945be23dab256
- changes:
  - Read through `src/components/features/skills/**` and `src/components/features/plugins/**` plus their query hooks, mutation hooks, and the two orchestrating routes (`skills-settings.tsx`, `skills-plugins.tsx`). The area is genuinely well-defended (this morning's 08:44 UTC run already fixed the backend-id query-key gap across every plugins/skills hook); the one real, reproducible bug still standing was an asymmetry between the two orchestrating routes.
  - **`src/routes/skills-settings.tsx` kept a stale skill snapshot in the detail modal.** `skills-plugins.tsx`'s `PluginDetailModal` already follows the codebase's own established pattern (see `plugin-files-section.tsx`'s comment: "Derived rather than synced") of deriving the selected item fresh from the live list by name every render, specifically so a list refetch can't leave the modal showing a stale/orphaned entry. `skills-settings.tsx` instead stored the clicked `SkillInfo` object itself in `selectedSkill` state, never re-synced. Since this morning's fix correctly keyed `useSkills` by the active backend id, switching backends while the modal is open now actually changes `allSkills` to a different catalog — but the modal kept displaying the old backend's skill (stale content, and a toggle from it would silently write a `disabled_skills` entry for a name that may not exist on the new backend, with no error and no UI signal). Same class of staleness would show on any other skill-list refetch (e.g. a second tab's edit invalidating the query) while the modal was open, not only a backend switch. Changed `selectedSkill` state to `selectedSkillName` (the stable key) and derive `selectedSkill` via `allSkills.find(...)` each render, mirroring the plugins page; when the name no longer exists in the refreshed list, `selectedSkill` becomes `null` and the modal closes on its own, exactly like `PluginDetailModal` already does.
  - Added a regression test (`__tests__/routes/skills-settings.test.tsx`, new `it`) that registers two local backends, opens the detail modal for a skill only backend A has, switches the active backend to B (mocking `SkillsService.getSkills` to return a disjoint skill list for B), and asserts the modal closes instead of continuing to show backend A's skill. Verified it fails against the pre-fix code (`git stash` on just the route file) and passes with the fix.
  - No new user-facing strings, so no `translation.json`/locale-file changes needed.
- gate: lint pass (typecheck, eslint, prettier all clean on first pass, no repair needed) · test pre-existing-fail (30/772 files failing, 147/6069 tests — same 30 files by exact name as every prior run this week, confirmed by diffing the failing-file list; none in the touched area, and the new `skills-settings.test.tsx` test — all 25 in that file — passed) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, unrelated to this diff, same known gap logged in every hourly/focus-fixer run this week)
- ci: `fly-deploy.yml` (run 35271844051, "Deploy Agent Server") green — the production Fly deploy this playbook actually gates on. `ci.yml` (run 35271844075) red: `test-and-build (windows)` passed, `test-and-build (ubuntu)` failed at the `Test` step (`Lint` passed first; `Build app`/`Build library`/`Verify package contents` skipped as a consequence) — NOT reverted: identical shape to every prior commit's `ci.yml` run today (`Lint` pass / `Test` fail on ubuntu only), matching this run's own local gate above which reconfirmed the same 30-file/147-test baseline against unmodified `BASE_SHA`; neither `skills-settings.tsx` nor `skills-settings.test.tsx` appears anywhere in that baseline's failure list, and the new regression test (all 25 in its file) passed locally. `docker.yml`/`release.yml` not checked this run — both have failed on every commit today regardless of diff content and are outside this playbook's revert criteria regardless.
- notes: `node_modules` was absent in the fresh checkout; ran `npm ci` before the gate per this run's instructions.
