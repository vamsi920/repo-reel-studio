## 00:24 UTC — codegraph — FIXED
- mode: section
- todo: -
- previous run: environment-onboarding
- picked because: Step 0 incident check first — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did cheap liveness/code re-checks on all three OPEN rows anyway and bumped `last confirmed` timestamps (INC-3: `GET https://neo-agent-server.fly.dev/server_info` uptime 2758s (~46 min), same known restart-on-deploy pattern; INC-6/INC-7: re-read the same files, unchanged) — pushed as its own standalone commit (`c58da20`) before starting section work, since this repo's push velocity from other cloud routines makes it worth landing small, safe edits immediately rather than holding them uncommitted through a long bug-hunt. `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. This run's preflight discovered the checked-out `origin/main` was ~50 commits stale relative to the true current tip (the container's initial clone predates a large batch of same-day work from other cloud routines) — reset cleanly to the real `origin/main` tip before re-running the rotation pick against current `hourly-state.json`/run logs (see notes). Of the last 6 working runs against the *current* state (environment-onboarding, automations, agentops, knowledge-kt, settings, security), each section appeared once; `environment-onboarding` (most recent, per `lastSection`) was excluded, none of the remaining 11 candidates had been worked twice or more in that window. Of what remained, `codegraph` had the single oldest `lastRun` (2026-09-24T11:38:00Z) by a wide margin.
- brainstorm: -
- base: c58da20 (after the incident-liveness commit)  commit: (recorded after push, see below)
- changes:
  - Delegated a read-only bug hunt across the full `codegraph` surface (`src/lib/codegraph/**`, `src/components/features/codegraph/**`, `src/stores/codegraph-store.ts`, `src/routes/kt-graph.tsx`, and every matching test file) to an `Explore` subagent, explicitly told this section has been fixed 16 times already and to skip the cross-workspace `pinnedCommitByWorkspaceRepo` fix already landed at 11:11 UTC yesterday. It returned 4 ranked candidates; independently verified the top one by reading the exact code before fixing, per this playbook's own standard.
  - **Bug fix (stale drill-down race)**: `kt-graph.tsx`'s `drillDown` callback called `navigateTo(key, nodeId)` unconditionally once its async `handle.loadLevel(nodeId)` fetch resolved, with no check for whether the user was still where they started. Reproduction: drill into subsystem A (an uncached, slow-resolving fetch); while that fetch is still in flight, select and drill into sibling subsystem B instead (an already-cached level, or a second fetch that resolves first) — B's fast/resolved path navigates there correctly; when A's stale fetch then resolves, the old code silently snapped the screen back to A, discarding the user's deliberate move to B, with no click involved. The store's own `failLevel` (`codegraph-store.ts`) already guards against exactly this class of race on the *error* path via a `stillReachable` check before surfacing anything — the *success* path in the route had no equivalent guard. Fixed by capturing the `currentParentId` at the moment each drill-down request starts and only completing its `navigateTo` if that value is unchanged once the fetch resolves; `drillDown` now returns `Promise<boolean>` (whether it actually navigated) so callers can react to a stale/failed drill instead of assuming success.
  - **Bug fix (search selecting a failed drill)**: `selectSearchResult` awaited `drillDown(entry.parentId)` and then unconditionally called `selectNode`+cleared the search query, even when that drill failed (a real fetch/storage error) and never navigated anywhere — silently pointing `selectedNodeId` at a node the open level doesn't contain (so no details panel opens) while also wiping the search box, leaving the user with no visible feedback and no easy way to retry. Now uses `drillDown`'s new boolean return to bail out before selecting/clearing when the drill didn't actually land.
  - Added 2 regression tests to `__tests__/routes/kt-graph.test.tsx`: the stale-drill race (drills into a slow "Payments", switches to a fast "Billing" mid-flight, then releases the stale fetch and asserts the view stays on Billing, both via the rendered breadcrumbs and the store's own `currentParentId`/cached `levels` state) and the search-failure case (search result whose parent level fails to load leaves the search box populated and opens no details panel). Verified the first test fails against the pre-fix code (reproduced "SystemPayments" instead of "Billing" via `git stash` on just `kt-graph.tsx`) and passes with the fix restored.
  - The subagent's other findings (a missing `MAX_LEVEL_CHILDREN` cap on `buildModuleTree`'s per-folder module split in `hierarchy.ts`, confirmed via the module's own test data producing 27 children in one level against a documented 24-node budget) is a real, larger structural gap left for a future run — flagging as a **FINDING (needs a dedicated pass)**: fixing it correctly needs a capping/"Other" bucket strategy analogous to the existing `capSubsystemBuckets` for level 1, which is more than this run's "up to three, worst first" budget after the two fixes above.
  - No new user-facing strings, so no `public/locales/*/openhands.json` edits needed.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `npm run build` rebuild byproduct via `git checkout --` before running `build:lib`, per established convention.
- gate: lint pass (typecheck + eslint + prettier all clean) · test: touched file (`kt-graph.test.tsx`) 17/17 green (15 pre-existing + 2 new); full-suite run: 21 failed files / 69 failed tests / 6463 passed / 5 skipped / 7 todo out of 6544 — matches the current declining pre-existing baseline logged by the immediately preceding `environment-onboarding` run (20/68, within the normal one-file/one-test drift this baseline has shown run to run); zero failures in `kt-graph.test.tsx` or anywhere else under `src/lib/codegraph/`, `src/components/features/codegraph/`, `src/stores/codegraph-store.ts`, or `src/routes/kt-graph.tsx` (grep-confirmed against the failure list) · build pass (byproduct reverted) · build:lib pre-existing-fail (72 `error TS6059` lines, confirmed 100% confined to `vendor/understand-anything/**` via grep; this diff touches neither `vendor/` nor any file under it)
- ci: pushed clean as `9138c7b` (fast-forward, no rebase needed). `fly-deploy.yml` (run 36077489798) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36077489972) was still `in_progress` after ~10.5 minutes of best-effort polling via the GitHub MCP `actions_get`/`actions_list` tools (`gh` unavailable in this sandbox) and was not waited on further, matching this section's own recent history (this repo's `Test` step has taken 11-13 minutes on several other commits this week); this run's own local full-suite gate already confirmed zero failures in any file this diff touches, so a subsequent `ci.yml` failure there would be the same declining pre-existing baseline (21 failed files/69 failed tests, logged above), not a regression from this diff. `docker.yml` (run 36077489791) and `release.yml` (run 36077490377) both completed **failure**, matching the identical pre-existing pattern already showing on the immediately-preceding commit (`c58da20`) and every recent commit before it — neither is one of the two workflows (`ci.yml`/`fly-deploy.yml`) this playbook gates a revert on.
- notes: **Note added during a later rebase this same morning:** the immediately-following `codegraph` run (00:56 UTC, below) picked this same section concurrently, from a `hourly-state.json` snapshot taken before this run's commit had landed — an expected collision under this playbook's rotation model when two instances of this routine overlap, not a bug in either run. **Stale checkout discovered and resolved mid-run.** This run's fresh container checkout of `main` was ~50 commits behind the true `origin/main` tip (the checkout evidently predates a large batch of same-day work landed by other cloud routines between the container's provisioning and this run's start). The divergence was caught only after a full bug-hunt-and-fix cycle had already been completed against the stale base — that work (also a `security`-section fix, plus test additions) turned out to substantially duplicate two fixes/tests already landed upstream in the interim (`ec59616 fix(security): memoize repository-select label counts...`, `4ee5de0 test(security): cover picker exclusion...`). Discarded that stale, duplicate work entirely via `git reset --hard origin/main` (nothing had been committed yet, so this was non-destructive) and re-ran the section-selection logic fresh against the real current state, landing on `codegraph` instead. One genuinely novel piece from that discarded pass (a doc-comment accuracy fix on `SecurityWorkspaceScopeState.isLoading`/`isError` in `src/routes/security.tsx`, which still incorrectly claimed both flags are "always false once `repositories` is non-empty") was NOT carried forward, since it lived in the same file as the now-superseded diff and re-deriving it cleanly against the current `security.tsx` would have meant re-opening that whole section mid-`codegraph`-run — logging it here as a **FINDING (needs a future security-section run)**: `SecurityWorkspaceScopeState`'s `isLoading`/`isError` doc comments are still inaccurate for the `"requested-not-connected"` branch (repositories can be non-empty while either flag is still true, since those entries can be *other*, already-resolved repositories) and should be corrected the next time that section comes up. `npm ci` was run before the gate (dependencies not pre-installed in this fresh container; 1555 packages, ~37s). No `supabase/**` paths touched — this commit deploys fully via Netlify on push.
## 00:56 UTC — codegraph — IMPROVED
- mode: section
- todo: -
- previous run: environment-onboarding (22:43 UTC 2026-09-24)
- picked because: step 0 incident check first — read `incidents.md` in full. No OPEN row had a buildable `mitigation: none` in this run's scope: INC-3 (agent-server restarts on every push) is explicitly out of scope, touches `.github/workflows`/an infra decision; INC-6 (Jira webhook receiver bearer check not bound to registration) and INC-7 (OAuth `state` not bound to the initiating browser) both explicitly document a prior run's deliberate hold-off pending external verification this sandbox cannot do. Did the cheap liveness/code re-check anyway: INC-3 `GET https://neo-agent-server.fly.dev/server_info` returned `uptime: 1408`s, same known deploy-triggers-restart pattern; INC-6 (`jira-webhook-receiver/index.ts`) and INC-7 (`connections-oauth-start/index.ts`) re-read, both unchanged. Bumped all three `last confirmed` timestamps (folded into this run's commit rather than a separate bookkeeping-only commit, since one had already been pushed minutes earlier by another instance of this routine — see notes). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. Of the last 10 working runs today (kt-video, conversation-chat, skills-plugins-mcp, files-terminal-browser, auth-data-platform, security, settings, knowledge-kt, agentops, automations, environment-onboarding), `environment-onboarding` (most recent to do work) was excluded; no remaining candidate had been worked twice or more in the last 6 working runs (environment-onboarding, automations, agentops, knowledge-kt, settings, security — one each). Of the 11 remaining candidates, `codegraph` had by far the oldest `lastRun` (2026-09-24T11:38:00Z vs. `kt-video`'s next-oldest 12:44:00Z).
- brainstorm: -
- changes:
  - Read through every file under `src/lib/codegraph/` and `src/components/features/codegraph/` plus `src/stores/codegraph-store.ts` and `src/routes/kt-graph.tsx` looking for real defects (wrong conditionals, stale closures, races, bad cache keys). The module has been worked 16 times before and is unusually well-hardened already — no correctness bug found this pass; found one genuine, narrow accessibility gap and one piece of confirmed dead code instead, plus a related a11y inconsistency, and fixed all three (kept well under the "up to three" cap).
  - **Gap 1 — the CodeGraph node-details side panel had no keyboard escape hatch.** The canvas (`codegraph-canvas.tsx`) and the search box (`codegraph-toolbar.tsx`) both already dismiss on Escape, by design (their own comments say so), but `CodeGraphNodeDetails` renders as a sibling of the canvas in `kt-graph.tsx`, not nested inside it — a keyboard user who tabs onto one of the panel's own focusable children (e.g. the close button, a dependency chip) had no way to dismiss the panel without reaching for a mouse, unlike every other dismissible surface on this page. Fixed by adding an `onKeyDown` handler directly on the panel's `<aside>` that calls `onClose()` on Escape (with an `eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions` justified the same way three other call sites in this codebase already justify the identical pattern — `sidebar.tsx`, `context-menu.tsx`, `conversation-tag-chips.tsx`).
  - **Gap 2 — the full-page "analyzing" and "analysis failed" states in `kt-graph.tsx` had no live-region role**, unlike the toolbar's own level-loading indicator (`role="status"`) and the canvas's own layout-failure state (`role="alert"`) for the exact same category of state. A screen-reader user got no announcement when analysis started, progressed, or failed on this page. Added `role="status"` to the analyzing block and `role="alert"` to the error block, matching the pattern already established elsewhere in the same feature.
  - **Dead code** — `CODEGRAPH_ROOT_LEVEL` (`codegraph-store.ts`) was exported but had zero importers anywhere in `src/`/`__tests__/` (grep-confirmed); removed it.
  - Added regression tests: `codegraph-node-details.test.tsx` gains one case asserting Escape closes the panel while focus is on the close button; `kt-graph.test.tsx` gains one case asserting `role="status"`/`role="alert"` on the analyzing/failed states with the expected copy. Verified both fail against the pre-fix code and pass after.
  - No user-facing strings added (no new i18n keys — the new roles/handler are pure ARIA/keyboard wiring on existing copy), so no locale-file updates were needed.
- gate: lint pass (typecheck + eslint + prettier all clean, after fixing one `jsx-a11y/no-noninteractive-element-interactions` and one `prettier/prettier` formatting error the first pass surfaced) · test: touched files green (`codegraph-node-details.test.tsx` 15/15, `kt-graph.test.tsx` 16/16, full section suite 200/200 across `__tests__/lib/codegraph`, `__tests__/stores/codegraph-store.test.ts`, `__tests__/components/features/codegraph`, `__tests__/routes/kt-graph.test.tsx`, `__tests__/lib/data-platform/repositories/codegraph-repository.test.ts`); full-suite run: 20 failed files / 68 failed tests / 6464 passed / 5 skipped / 7 todo out of 6544 — matches the documented pre-existing declining baseline (still 20 files/68 tests, same as the immediately preceding `environment-onboarding` run), zero failures in any file this diff touches (grep-confirmed the 20 failed files list, none under `codegraph`) · build pass (`public/codegraph-analyzer/{analyze.mjs,manifest.json}` byproducts reverted via `git checkout --` before commit, per established convention) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor any file that import-chains into it beyond what already did)
- base: c58da20  commit: cf4713a (after rebase onto 32a61ba, see notes)
- ci: pushed as `9647c88`, then rebased onto `origin/main` (see notes) and re-pushed as `cf4713a` (fast-forward after rebase). `fly-deploy.yml` (run 36080306415) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36080306506) was still `in_progress` after ~10 minutes of best-effort polling via the GitHub MCP `actions_get`/`actions_list` tools (`gh` unavailable in this sandbox: `command not found`) and was not waited on further; this run's own local full-suite gate already confirmed zero failures in any file this diff touches (20 failed files/68 failed tests, the same declining pre-existing baseline every recent commit has shown), so a subsequent `ci.yml` failure there would be that same baseline, not a regression from this diff. `docker.yml` (run 36080306426) and `release.yml` (run 36080306806) both completed **failure**, matching the identical pre-existing pattern already showing on every recent commit this week (including the immediately-preceding `32a61ba`) — neither is one of the two workflows (`ci.yml`/`fly-deploy.yml`) this playbook gates a revert on.
- notes: On preflight, found the checked-out HEAD already sitting on a commit (`c58da20`, "chore(neo-cloud): bump INC-3/INC-6/INC-7 liveness-check timestamps") that only touched `incidents.md` and had no matching run-log entry in `hourly-2026-09-24.md` — apparently a prior invocation of this same scheduled routine ran the step-0 incident check and pushed it, but did not complete (or log) a section-mode run afterward. Treated `hourly-state.json`'s own `lastSection`/per-section `lastRun` values (last real completed run: `environment-onboarding` at 22:43 UTC) as authoritative for rotation purposes rather than that orphan commit, since the state file is the documented source of truth for section selection. **Push collision with a concurrent instance of this same routine.** `git push origin main` for this run's commit (`9647c88`) was rejected as non-fast-forward: another invocation of this routine had, unbeknownst to this run, independently picked the same `codegraph` section from the same pre-push `hourly-state.json` snapshot and landed two commits first (`9138c7b` "fix(codegraph): stop stale drill-down fetches from overriding later navigation" plus its own `chore(neo-cloud)` CI-status commit `32a61ba`) — see that run's own `00:24 UTC` log entry above, which now also carries a cross-reference note. Per this playbook's step 7, rebased onto `origin/main` rather than forcing: `.neo-cloud/hourly-state.json` and this same run-log file both hit real conflicts (both runs touched the `codegraph` section's `lastRun`/`runCount` and both created today's log file) and were resolved by hand — `hourly-state.json`'s `codegraph.runCount` set to `18` (16 -> 17 on each side independently, correctly reconciled to reflect that *two* runs happened, not one) with `lastRun` taking the later of the two completion timestamps; the run log resolved by keeping both entries in chronological order. `src/routes/kt-graph.tsx` and its test file auto-merged with no conflicts (the other run's fix was in `drillDown`/`selectSearchResult`; this run's changes were in the unrelated analyzing/error-state JSX and the sibling `codegraph-node-details.tsx` file) — read the merged diff by hand to confirm the two changes composed correctly, then re-ran `npm run lint` plus `npx vitest run` on every file this run touched (202/202 green) before pushing again, per the playbook's rebase-and-retry instructions; did not re-run the full suite a second time since the rebase touched no logic this diff's own files depend on. No `supabase/**` paths touched — this commit deploys fully via Netlify on push.
## 02:00 UTC — kt-video — ABANDONED (rebase conflict)
- mode: section
- todo: -
- previous run: codegraph (00:56 UTC)
- picked because: Step 0 incident-first check — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did the cheap liveness/code re-check anyway and bumped all three `last confirmed` timestamps in the commit below (INC-3 `GET https://neo-agent-server.fly.dev/server_info` uptime 2271s (~38 min), same known pattern; INC-6 `jira-webhook-receiver/index.ts` and INC-7 `connections-oauth-start/index.ts` re-read, both unchanged). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item → section mode. Of the last 6 working runs (codegraph x2, environment-onboarding, automations, agentops, knowledge-kt), codegraph was excluded (most recent + worked twice); of the rest, `kt-video` had by far the oldest `lastRun` (2026-09-24T12:44:00Z).
- brainstorm: -
- base: cf4713a  commit: -
- changes:
  - Full implementation was completed and gated cleanly: delegated a bug hunt to an Explore subagent across `src/lib/kt-video/**`, `src/components/features/kt-video/**`, `src/routes/kt-video-tab.tsx`, and their tests. Found and fixed one real, reproducible defect — the KT Video sidebar's `unavailablePaths` check claimed to mirror `build-manifest.ts`'s scene-eligibility filter but only copied the outer `c.trim().length > 0` check, not `rankFiles`'s stricter inner filter (ignored/noise paths, non-source extensions, content under 40 trimmed chars). A selected file that passed the outer check but failed the inner one (e.g. `package-lock.json`, a `.env`, a tiny stub file) looked normally selected in the sidebar while silently getting no scene in the built video. Fixed by extracting `rankFiles`'s exact filter into an exported `isFileSceneEligible(path, content)` in `build-manifest.ts` and having the sidebar call it directly instead of duplicating a subset of the logic. Added a 6-case `isFileSceneEligible` test block to `build-manifest.test.ts` and 2 regression cases to `kt-video-tab.test.tsx`, plus lengthened 2 pre-existing fixtures that happened to use under-40-char mock content and asserted "available" (they were testing unrelated behavior, not the length threshold).
  - Gate passed clean: lint pass; touched-file tests 37/37 green; full-suite 20 failed files/68 failed tests matching the documented pre-existing baseline with zero failures in any file this diff touched; build pass; build:lib pre-existing-fail (72 `TS6059` lines, 100% confined to `vendor/understand-anything/**`, confirmed unrelated).
  - Committed as `343a6ae` (base `cf4713a`) and pushed — rejected non-fast-forward (`neo-focus-fixer-cloud`/another instance of this routine landed `8d93033` first, expected per this playbook). `git fetch && git rebase origin/main` produced a genuine `CONFLICT (content)` in `.neo-cloud/runs/hourly-2026-09-25.md` (the append-only run log — both this run's own kt-video block and the concurrent commit's edit landed at/near the same anchor lines). Per this playbook's explicit rule ("conflict → abort, reset to origin/main, log ABANDONED (rebase conflict), stop") — no exception carved out for a log-only conflict — aborted the rebase and reset hard to `origin/main` (`8d93033`) rather than hand-resolving it, discarding the local commit entirely (nothing lost from `origin/main`'s perspective; the working code diff no longer exists in this run's tree).
  - The underlying `kt-video` bug (documented above) is still present in the codebase — a future `kt-video` section run will rediscover it, since section-mode fixes (unlike `TODO.txt`/`findings.txt` items) have no cross-run carry-forward mechanism in this playbook.
- gate: lint pass · test pass (touched files 37/37; full-suite matched pre-existing baseline) · build pass · build:lib pre-existing-fail (unrelated vendor/ baseline) — all confirmed BEFORE the rebase conflict; the abandonment is a push-conflict, not a gate failure
- ci: the discarded `kt-video` code diff never reached `origin/main`. This ABANDONED run's own bookkeeping commit (`7e60ed0`, `.neo-cloud/` only) did land, though, and its `fly-deploy.yml` (run 36084307105) — the workflow this playbook gates a revert on — completed **success**; no revert needed. `ci.yml` (run 36084307072) was still `in_progress` after the check and not waited on further (matches this repo's documented 11-13 minute `Test` step timing); `docker.yml`/`release.yml` follow the same pre-existing-failure pattern already logged on the immediately preceding commit, neither is a revert-gating workflow.
- notes: Re-bumped the three `incidents.md` liveness timestamps (INC-3/6/7) as part of this same log-only commit below, since those checks are still valid/current as of this run regardless of the code-diff discard. `npm ci` was run before the gate (dependencies not pre-installed in this fresh container).

## 02:46 UTC — conversation-chat — FIXED
- mode: section
- todo: -
- previous run: kt-video (02:00 UTC, ABANDONED — rebase conflict, discarded)
- picked because: TODO.txt and findings.txt had no live items. Incident check (step 0): INC-3 liveness re-confirmed (`GET /server_info` on neo-agent-server.fly.dev returned uptime 418s, consistent with the known deploy-triggers-restart pattern; bumped its `last confirmed` and pushed that alone first as `5eb832e`, since a repo hook required no uncommitted changes be left idle before the bug-hunt subagent started). INC-6/INC-7 held off unchanged (no new claim-schema/design information since their last check 10 minutes earlier in this same run window). Section rotation: most recent section that did work was `kt-video` (02:00 UTC, ABANDONED counts as "did work" per this playbook) — removed. `codegraph` also removed (worked twice in the last 6 working runs: 00:24 and 00:56 UTC today). Of the remaining 10 candidates, `conversation-chat` had the single oldest `lastRun` (2026-09-24T13:58:00Z).
- brainstorm: delegated a wide bug hunt to a research subagent, focused on the less-recently-touched subdirectories of this 15-times-hardened section (usage-panel, metrics-modal, conversation-main, conversation-tabs, start-task-card, system-message-modal, conversation-card), since the last two runs already fixed the O(n^2) grouping issue, transcript-export-modal races, a task-polling unhandled rejection, and an optimistic-message watchdog leak. The subagent found a real, reproducible cross-conversation state-leak: `useCompactContextAction` (`src/hooks/use-compact-context-action.ts`) and `useAwaitContextCompaction` (`src/hooks/use-await-context-compaction.ts`) keep in-flight compaction state (`beforeToken`, `baselineEventIdsRef`) and a live event/metrics-store watcher with no reset on conversation switch. `ConversationTabContent`'s `tabWrapperKey` only remounts the *terminal* tab on conversation change (`conversation-tab-content.tsx`); every other tab, including Usage, survives a switch. Concrete failure: user starts a compaction on conversation A, switches to conversation B (still on the Usage tab or with the composer's context-window popover mounted) before it resolves — the stale watcher, still subscribed to the now-B-scoped stores, can match one of B's own Condensation events as "the" completion and pop a spurious "Context compacted"/"failed" toast for B, and/or leaves the Compact button permanently disabled on B until the stale 90s timeout fires. Verified by reading both files directly. Two lower-confidence runner-ups from the subagent (an architectural note that only the terminal tab remounts on conversation change, and `staleTime: Infinity` on the LLM balance card, likely intentional) were not acted on — the first is a design observation not a standalone bug, the second is explicitly documented as intentional in its own hook.
- changes:
  - `src/hooks/use-compact-context-action.ts`: added a `useEffect` keyed on `conversation?.id` whose cleanup resets `beforeToken` to `null` and clears `baselineEventIdsRef.current` whenever the active conversation changes (or the hook unmounts) — before this, that state was scoped only to `handleCompactionComplete`/`handleCompact`, with nothing reacting to a conversation switch. Resetting `beforeToken` also causes `useAwaitContextCompaction`'s internal effect (keyed on `beforeToken`) to tear down its event/metrics-store subscriptions and pending timers from the old conversation on the very next render, instead of letting them keep running against the new one.
  - `src/hooks/use-compact-context-action.test.tsx`: changed the `useActiveConversation` mock from a static object literal to a mutable `activeConversation.current` the test can reassign, added an `afterEach` reset back to `c1`, and added a new regression test — starts a compaction on `c1`, switches to `c2` mid-flight via `rerender()`, asserts `isCompacting` immediately drops to `false`, then feeds a `c2` Condensation event + metrics drop + a full timeout advance and asserts neither the success nor error toast fires (i.e. `c2`'s own activity is never mistaken for `c1`'s compaction result).
  - No user-facing strings added or changed, so no `public/locales/*/openhands.json` edits needed.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `npm run build` rebuild byproducts via `git checkout --` before committing, per established convention.
- gate: lint pass (typecheck + eslint + prettier all clean) · test: touched-file tests green (`use-compact-context-action.test.tsx` 3/3 including the new test, `use-await-context-compaction.test.ts` and `compact-context-button.test.tsx` and `context-window-meter.test.tsx` unaffected, 13/13); full-suite run: 20 failed files / 68 failed tests / 6467 passed / 5 skipped / 7 todo out of 6547 — matches the documented pre-existing baseline from the 13:57 UTC run today (20 failed files / 68 failed tests) exactly; grep-confirmed none of the 20 failing files are in this diff's paths (root.test.tsx, add-backend-modal, sidebar, telemetry, package-library, use-app-title, etc. — all pre-existing/unrelated) · build pass (byproducts reverted before commit) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 36087740854) — the actual production Fly deploy this playbook gates a revert on — completed **success** (polled via Monitor + `curl`/`$GH_TOKEN`, `gh` unavailable in this sandbox). No revert needed. `ci.yml` (run 36087740855) was still `in_progress` after ~10 minutes of polling (the playbook's best-effort watch budget); the immediately preceding bookkeeping-only commit's `ci.yml` run (36085273110) completed **failure** on the same recurring pre-existing-baseline pattern documented on every commit today (its `Test` step, not `Lint`), and this run's own local full-suite gate above (20 failed files / 68 failed tests, zero in this diff's files) matches that baseline exactly — so if this commit's `ci.yml` later shows the same shape, it is not a regression from this diff and no revert is warranted; a human/future run should confirm it settled the way the baseline predicts.
- notes: `npm ci` was run before the gate (dependencies not pre-installed in this fresh container; 1555 packages, ~47s). No `supabase/**` paths touched — this commit deploys fully via Netlify on push. This run needed an initial standalone bookkeeping commit (`5eb832e`, INC-3 timestamp bump only) before the section work, same pattern as prior runs today; that push also surfaced and fixed a stale/detached local `main` (it was not tracking `origin/main` at session start and had diverged 50 commits — `git checkout -B main origin/main` before cherry-picking the bookkeeping commit resolved it; worth a future run double-checking `git branch -vv` at Preflight if this recurs).

## 03:40 UTC — kt-video — FIXED
- mode: section
- todo: -
- previous run: conversation-chat (02:46 UTC)
- picked because: Step 0 incident-first check — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did the cheap liveness/code re-check anyway and bumped all three `last confirmed` timestamps (INC-3 `GET https://neo-agent-server.fly.dev/server_info` uptime 636s (~10.6 min), same known restart-on-deploy pattern; INC-6 `jira-webhook-receiver/index.ts` and INC-7 `connections-oauth-start/index.ts` re-read, both unchanged). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item → section mode. Of the last 6 working runs (conversation-chat, kt-video (ABANDONED, still counts), codegraph x2, environment-onboarding, automations), `conversation-chat` was excluded (most recent) and `codegraph` was excluded (worked twice); of the rest, `kt-video` had the oldest `lastRun` (2026-09-24T12:44:00Z) since its 02:00 UTC attempt never updated the state (discarded via rebase-conflict abandonment). That prior 02:00 UTC run's own log left the underlying bug fully diagnosed and its fix/tests fully designed but discarded (rebase conflict on the run-log file itself, not a gate failure) — this run re-implemented that exact same fix from scratch against the current tree rather than re-deriving it.
- brainstorm: -
- base: c5edc481f9426129947d1f2d91dad7fa3c00abe7  commit: 7faa472
- changes:
  - Fixed a real bug in the KT Video tab: the sidebar's `unavailablePaths` check (`useSelectedFileContents` in `src/routes/kt-video-tab.tsx`) only mirrored `buildKtManifest`'s outer `c.trim().length > 0` filter, not `rankFiles`'s actual, stricter scene-eligibility filter (ignored/vendor paths, noise files like lockfiles/`.min.`/`.map`/`.d.ts`, non-source extensions, and a 40-character minimum). A selected file that passed the loose sidebar check but failed the real one (e.g. `package-lock.json`, a config `.yaml`, a tiny stub file under 40 chars) looked like a normal selected file in the sidebar while silently getting no scene in the built video, with nothing telling the user why.
  - Extracted `rankFiles`'s exact candidate filter into a new exported `isFileSceneEligible(path, content)` in `src/lib/kt-video/build-manifest.ts`, and had both `rankFiles` and the sidebar's `unavailablePaths` check call it directly instead of the sidebar duplicating a weaker subset of the same logic.
  - Added a 6-case `isFileSceneEligible` test block to `__tests__/lib/kt-video/build-manifest.test.ts` (accepts real source with enough content; rejects under-40-char content; rejects an ignored `node_modules` path; rejects a lockfile; rejects a non-source extension; accepts a README despite `.md` not being a source extension) and a new regression case in `__tests__/routes/kt-video-tab.test.tsx` asserting a lockfile that loads as valid non-empty text is still flagged unavailable. Lengthened two pre-existing fixtures (`a.ts`, `available.ts`) that used 20-char mock content and asserted "available" — they were testing unrelated behavior (loading/error state) and would have started failing the moment the filter became accurate, not because the behavior they tested actually changed.
  - No user-facing strings added or changed, so no `public/locales/*/openhands.json` edits needed.
  - `npm run build` regenerated `public/codegraph-analyzer/{analyze.mjs,manifest.json}` as a byproduct of gating (those files bundle current `src/lib/codegraph/*` sources, which have drifted since the last commit that touched them) — reverted both via `git checkout --` before committing, per the convention the immediately preceding run (02:46 UTC) established.
- gate: lint pass (typecheck + `eslint src` + `prettier --check src/**/*.{ts,tsx}` all clean; the two touched test files under `__tests__/` have pre-existing, unrelated lint nits — an unused `beforeEach` import and two formatting lines — confirmed present on unmodified `main` too via a stash-and-relint check; `npm run lint` doesn't cover `__tests__/` at all, so these don't affect the gate) · test: touched-file tests 36/36 green (`build-manifest.test.ts`, `kt-video-tab.test.tsx`); full-suite: 20 failed files / 68 failed tests / 6474 passed / 5 skipped / 7 todo out of 6554 — matches the documented pre-existing baseline exactly (same count as the 02:00 UTC and 02:46 UTC runs today); grep-confirmed zero kt-video/build-manifest files among the failures · build pass (byproducts reverted before commit) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 36091627092) — the actual production Fly deploy this playbook gates a revert on — completed **success** (checked via `mcp__github__actions_get`, no `gh` in this sandbox). No revert needed. `ci.yml` (run 36091627041) completed **failure**: `Lint` step green, `Test` step failed (03:46:53-03:53:07, ~6m14s), `Build app`/`Build library` skipped as a result — same shape (Lint passes, Test fails) as every other commit's `ci.yml` run today, and this run's own local full-suite gate above (20 failed files / 68 failed tests, zero in this diff's files) matches that documented baseline exactly, so this is not a regression from this diff and no revert is warranted.
- notes: `npm ci` was run before the gate (dependencies not pre-installed in this fresh container; 1555 packages, ~37s). No `supabase/**` paths touched — this commit deploys fully via Netlify on push. Preflight found local `main` detached and ~50 commits diverged from the true `origin/main` tip (same stale-clone pattern the 00:24 UTC run hit and documented) — resolved via `git reset --hard origin/main` after stashing the in-progress `incidents.md` liveness edit, then reapplying it.

## 04:43 UTC — skills-plugins-mcp — IMPROVED
- mode: section
- todo: -
- previous run: kt-video (03:40 UTC)
- picked because: Step 0 incident-first check — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did the cheap liveness/code re-check anyway and bumped all three `last confirmed` timestamps (INC-3 `GET https://neo-agent-server.fly.dev/server_info` uptime 781s (~13 min), same known restart-on-deploy pattern; INC-6 `jira-webhook-receiver/index.ts` and INC-7 `connections-oauth-start/index.ts` re-read, both unchanged). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item → section mode. Of the last 6 working runs (kt-video FIXED, conversation-chat FIXED, kt-video ABANDONED (still counts), codegraph x2, environment-onboarding), `kt-video` was excluded (most recent run + worked twice) and `codegraph` was excluded (worked twice); of the remaining candidates, `skills-plugins-mcp` had by far the oldest `lastRun` (2026-09-24T14:47:00Z, ~14h stale vs. the next-oldest candidate at 15:31Z).
- brainstorm: -
- base: f4cac1bc69f5d27cbdbe77c0c22d14ed75cf4ab7  commit: 89be10a
- changes:
  - Bug-hunted `src/components/features/skills/**`, `src/components/features/plugins/**`, their route wiring (`src/routes/skills-settings.tsx`, `src/routes/skills-plugins.tsx`), every query/mutation hook under `src/hooks/{query,mutation}` touching skills/plugins, and the supporting utils (`src/utils/plugin-slash-commands.ts`, `src/utils/skill-install-events.ts`, `src/components/features/plugins/plugin-spec-identity.ts`). This section has clearly been hardened repeatedly in past runs — every file carries detailed comments documenting prior race/stale-closure fixes (e.g. `skills-settings.tsx`'s ref-based toggle guards, `skills-plugins.tsx`'s per-plugin busy-name tracking) — and no new real defect (wrong conditional, unhandled rejection, stale closure, missing cleanup, bad cache key) turned up after a thorough read plus checking existing test coverage against each candidate. Falling through to step 4's "no bugs → three improvements" path.
  - **Improvement 1 (error handling):** `SkillsSettingsScreen`'s `skills-error` banner and `SkillsPluginsScreen`'s `plugins-error` banner reported a failed fetch but gave the user no way to recover short of a full page reload — unlike `PluginPicker`'s sibling error state, which already had a working retry button. Added a retry button to both (`skills-error-retry` refetches the `useSkills()` query; `plugins-error-retry` refetches whichever of `usePluginsMarketplace`/`usePlugins`/`useLocalPlugins` actually failed), matching the existing `PluginPicker` pattern exactly.
  - **Improvement 2 (accessibility):** none of `skills-error`, `plugins-error`, or `plugin-picker-error` announced themselves to assistive tech (no `role="alert"`) despite `AddPluginModal`'s inline install-error already using that pattern elsewhere in this same section. Added `role="alert"` to all three. `PluginPicker`'s loading state (`plugin-picker-loading`) was also silent to screen readers, unlike the page-level loading skeletons in `skills-settings.tsx`/`skills-plugins.tsx` which already use `role="status" aria-live="polite"` — added the same pair here for consistency.
  - **Improvement 3 (real test coverage):** added regression tests exercising the new behavior: `skills-settings.test.tsx` ("refetches skills when the error state's retry button is clicked"), `plugins-page.test.tsx` ("refetches the failed source when the error state's retry button is clicked"), and `plugin-picker.test.tsx` ("announces the loading state to assistive tech"); existing error-state tests in all three files were extended to assert the new `role="alert"` attribute.
  - No new user-facing strings — the retry buttons reuse the existing, already-fully-translated `I18nKey.FILES$RETRY` ("Retry") key rather than adding a new one, so no `public/locales/*/openhands.json` edits needed.
  - `npm run build` regenerated `public/codegraph-analyzer/{analyze.mjs,manifest.json}` as a gating byproduct (bundles current `src/lib/codegraph/*`, unrelated to this diff) — reverted via `git checkout --` before committing, per the convention established by prior runs.
- gate: lint pass (typecheck + `eslint src` + `prettier --check src/**/*.{ts,tsx}` all clean, no repair needed) · test: touched-file tests 65/65 green (`skills-settings.test.tsx`, `plugins-page.test.tsx`, `plugin-picker.test.tsx`, including all 3 new tests); `npm ci` run first (fresh container, node_modules present but stale/incomplete — `vite` itself was missing, 1555 packages installed in ~35s); full-suite run: 20 failed files / 68 failed tests / 6477 passed / 5 skipped / 7 todo out of 6557 — matches the documented pre-existing baseline from every recent run today exactly (root.test.tsx onboarding-guard suite, add-backend-modal.test.tsx, sidebar.test.tsx version-tile ordering, use-app-title.test.tsx stale-title assertions, package-library.test.ts, telemetry.test.ts, msw `ProgressEvent is not defined`, etc.); grep-confirmed zero failures mention "skill" or "plugin" · build pass (byproducts reverted before commit) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 36095708898) — the actual production Fly deploy this playbook gates a revert on — completed **success** (checked via `mcp__github__actions_list`/`actions_get`, no `gh` in this sandbox). No revert needed. `ci.yml` (run 36095708910) was still `in_progress` after ~10 minutes of polling (the playbook's best-effort watch budget), same as most other commits today; this run's own local full-suite gate above (20 failed files / 68 failed tests, zero in this diff's files) matches the documented baseline exactly, so if it later shows the same Lint-pass/Test-fail shape, it is not a regression from this diff.
- notes: Preflight found the fresh checkout's local `main` branch was a stale shallow-clone ref (`c29a9e3`, dated 2026-09-24) one day behind the real `origin/main` tip (`f4cac1b`) that the initial detached HEAD already pointed at — `git merge-base` found no common ancestor at all because of the shallow clone depth, not a real history divergence. Resolved via `git fetch origin main && git checkout -B main origin/main` (no local work existed yet, so this was a safe pointer reset, not a discard). Same class of stale-clone issue two prior runs today already flagged; worth a future run adding an explicit shallow-repo check to Preflight if it keeps recurring.

## 05:18 UTC — files-terminal-browser — FIXED
- mode: section
- todo: -
- previous run: skills-plugins-mcp
- picked because: Step 0 incident check first — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do, last confirmed just this run's predecessor at 04:12 UTC so not re-verified again). `TODO.txt` had no live item (comments only) → section mode. Of the last 6 working runs (skills-plugins-mcp, kt-video, conversation-chat, kt-video[ABANDONED], codegraph, codegraph) skills-plugins-mcp is most-recent (excluded) and both kt-video and codegraph appeared twice (excluded). Of the remaining 8 candidates, `files-terminal-browser` had the single oldest `lastRun` (2026-09-24T15:31:00Z).
- brainstorm: -
- base: 48d4770ba49d4b76bde14fa1af4c5f9f1e5e0330  commit: 9942045d2f23b8ebccb1ca20625c765c5b538984
- changes:
  - **Real bug fix**: `useHasGitCommits` (`src/hooks/query/use-has-git-commits.ts`) — used by `files-tab.tsx` to decide whether the Files tab defaults to Diff view or Files view — called `useRuntimeIsReady()` with no options, which uses `RUNTIME_INACTIVE_STATES` (includes `AgentState.ERROR`) and requires `execution_status` to be strictly "active". Every sibling hook this probe sits alongside in the same feature — `useWorkspaceFiles`'s local branch, `useWorkspaceFileContent`, `useWorkspaceSession` — passes `{ allowAgentError: true }` specifically because, per that option's own doc comment, "the underlying sandbox process stays up when the agent/LLM loop errors out... only the agent's turn failed, not the runtime" and callers that "just read the workspace... rather than drive the agent" should pass it so they "don't go dark on every LLM failure". `useHasGitCommits` runs exactly that kind of read-only workspace probe (`git rev-parse --verify HEAD`) but was the one outlier missing the flag. Effect: the instant a conversation's agent hits an LLM/execution error (AgentState.ERROR / execution_status "error") — which, per this repo's own `incidents.md` INC-4, is a real recurring condition on this deployment's default Gemini profile — the probe silently disables itself and `hasCommits` gets stuck at `null` forever (not "loading", genuinely disabled) even though the sandbox and the `git rev-parse` it would run are both still alive. Since the Files tab's `diffViewDefault` formula treats `hasCommits !== false` as "stay optimistic, default to diff", an attached-but-commit-less workspace (brand-new empty repo, fresh `git init`) whose agent then errors out gets permanently stuck on the unhelpful empty Diff view instead of correctly falling back to the Files view, with no way to self-correct short of the user manually toggling it. Fixed by adding `{ allowAgentError: true }` to match the other three workspace-reading hooks in this exact file group.
  - Added a regression test to `__tests__/hooks/query/use-has-git-commits.test.tsx` asserting `useRuntimeIsReady` is called with `{ allowAgentError: true }` (the existing mock swallowed call arguments entirely — `useRuntimeIsReady: () => useRuntimeIsReadyMock()` — so it could never have caught this; widened it to `(...args) => useRuntimeIsReadyMock(...args)` so the new assertion is meaningful and any future regression on this call would be caught).
  - Bug-hunted the rest of the section's surface first (`src/components/features/{files-tab,files,terminal,browser}/**`, `src/stores/{files-tab-store,browser-store,command-store,use-workspace-mutation-counter}.ts`, `src/hooks/{use-terminal,use-auto-refresh-files-on-edit,use-has-attached-source,use-runtime-is-ready}.ts`, `src/hooks/query/{use-workspace-files,use-workspace-file-content,use-workspace-session}.ts`, `src/utils/{file-tree,file-priority,browser-observation,file-language}.ts`, `src/routes/{files-tab,browser-tab}.tsx`) before finding this — no other reproducible defect found in that pass. Given how load-bearing this one is (it can permanently wedge a core tab's default view during a known-recurring account-level condition), did not pad with additional lower-value "3 improvements" busywork this run, matching the precedent set by this section's most recent prior pass.
  - No new user-facing strings, so no `public/locales/*/openhands.json` edits needed.
  - `npm run build` regenerated `public/codegraph-analyzer/{analyze.mjs,manifest.json}` as a gating byproduct (unrelated to this diff) — reverted via `git checkout --` before committing, per established convention.
  - Step 0: did the cheap liveness check on INC-3 (only OPEN row with a cheap, sandbox-reachable check) — `GET https://neo-agent-server.fly.dev/server_info` returned `uptime: 800`s (~13 min), same recurring restart pattern, no change — and bumped its `last confirmed` timestamp in `incidents.md`. Did not re-touch INC-6/INC-7 (already re-confirmed by this run's immediate predecessor at 04:12 UTC, same day).
- gate: lint pass (`npm run typecheck && eslint src && prettier --check src/**/*.{ts,tsx}`, all clean, no repair needed) · test: touched-file tests 81/81 green (`use-has-git-commits.test.tsx` plus `use-workspace-files.test.tsx`/`use-workspace-file-content.test.tsx`/`use-workspace-session.test.tsx`/`files-tab.test.tsx` re-run for safety, including the new regression test); `npm ci` run first (fresh container, no `node_modules`, 1555 packages in ~55s); full-suite run: 20 failed files / 68 failed tests / 6478 passed / 5 skipped / 7 todo out of 6558 — matches (fractionally better than) today's documented recurring pre-existing baseline (e.g. `sidebar.test.tsx` version-tile-ordering, seen directly in this run's own tail output); zero failures in any file this diff touched · build pass (byproducts reverted before commit) · build:lib pre-existing-fail (14+ `error TS6059` lines visible in the captured tail, all confined to `vendor/understand-anything/**` per grep; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 36099203315) — the actual production Fly deploy this playbook gates a revert on — completed **success** (checked via direct GitHub REST API calls using the sandbox's `$GH_TOKEN`, no `gh` binary available). No revert needed. `ci.yml` (run 36099203408) was still `in_progress` after ~10 minutes of polling (the playbook's best-effort watch budget), same as most other commits today; this run's own local full-suite gate above (20 failed files / 68 failed tests, zero in this diff's files) matches the documented baseline exactly, so if it later shows the same Lint-pass/Test-fail shape, it is not a regression from this diff.
- notes: Preflight found the fresh checkout's local `main` branch pointer stale (`c29a9e3`) relative to the detached-HEAD checkout and `origin/main` tip (`48d4770`, which the container's initial detached HEAD already matched) — `git pull --ff-only` correctly refused as non-fast-forward (50 commits each way is a stale/shallow local ref, not a real divergence: the container's `HEAD` was already sitting on `origin/main`'s tip before this run touched anything). Resolved via `git checkout main && git reset --hard origin/main` (no local work existed yet, so this was a safe pointer reset, not a discard) — same recurring stale-clone-pointer class of issue several other runs today have already flagged in their own notes.

## 06:42 UTC — auth-data-platform — FIXED
- mode: section
- todo: -
- previous run: files-terminal-browser
- picked because: Step 0 incident check first — read `incidents.md`; every `OPEN` row with `mitigation: none` (INC-3, INC-6, INC-7) is explicitly out of this run's scope per its own "needs from user"/mitigation text (INC-3 touches `.github/workflows`, standing-rule-forbidden; INC-6/INC-7 need external verification — real Atlassian claim docs / a live browser OAuth round-trip — this sandbox cannot do), so none could get a real mitigation this run. Did the cheap liveness check anyway: `GET https://neo-agent-server.fly.dev/server_info` → `uptime: 1382`s (~23 min), same known deploy-restart pattern, bumped INC-3's `last confirmed`; re-read INC-6/INC-7's cited files, unchanged, bumped their timestamps too. `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. Read the last ~10 working runs across today's and yesterday's log (files-terminal-browser, skills-plugins-mcp, kt-video ×2, conversation-chat, codegraph ×2, environment-onboarding, automations, agentops). Removed `files-terminal-browser` (most recent) and `kt-video` (worked twice in the last 6 working runs). Of the 10 remaining candidates, `auth-data-platform` had the oldest `lastRun` (2026-09-24T16:43:00Z) in `hourly-state.json`.
- brainstorm: -
- base: 41170d87899a9070003ddf66492428e1f189c2ed  commit: (pending)
- changes:
  - Found a real data-loss bug while bug-hunting `src/lib/data-platform/` and `supabase/functions/` (connections/credentials path): `credential-request-sheet.tsx`'s "secret rotation on an already-connected provider" form (its own doc comment) deliberately shows/submits only the specific secret field(s) the agent asked to rotate, plus every non-secret field — any *other* secret field the manifest declares (AWS Bedrock's optional `sessionToken` alongside required `secretAccessKey`; Datadog's optional `appKey` alongside required `apiKey`) never appears in `values` and so is absent from the `credentials` object POSTed to `connections-set-credentials`. That Edge Function encrypted exactly the submitted `credentials` object and upserted it as the connection's entire `encrypted_credentials` column — so rotating just one secret field on a multi-secret-field connector silently deleted every other secret already stored on that connection (and the same-request probe ran against the incomplete credential set too, not just storage).
  - Added `mergeConnectionCredentials()` (new file `supabase/functions/_shared/connection-credentials.ts`) — a small pure helper that starts from the existing connection's decrypted secrets (filtered to the fields the manifest still declares), then overlays whatever was actually submitted in this request (submitted always wins for a given field name).
  - `connections-set-credentials/index.ts` now looks up the existing row's `encrypted_credentials` for the same `(org_id, capability, provider_id, instance_key)` before building the credential set, decrypts it best-effort (a corrupt/undecryptable blob degrades to "nothing stored" rather than blocking the request), and merges it with the newly submitted fields via the new helper. The merged set (not the bare submission) now drives the required-field check, the redacted summary, the pre-save connectivity probe, and the final encrypted blob — so a partial secret rotation preserves every field it didn't touch, and the probe now actually exercises the connection's real, complete credential set.
  - Added `__tests__/supabase/functions/connection-credentials.test.ts` (5 cases: preserves an unrotated stored secret, lets a resubmitted value override the stored one, drops a field the manifest no longer declares as secret, handles nothing-stored-yet, accepts a `Set` the same way the edge function passes it) — same dynamic-import-from-`supabase/functions/_shared/` pattern as `github.test.ts`/`jira.test.ts`, since `supabase/functions` is excluded from this project's `tsconfig`.
- gate: lint pass (typecheck + `eslint src` + `prettier --check src/**/*.{ts,tsx}`, all clean, no repair needed) · test: touched-file test 5/5 green (`connection-credentials.test.ts`), plus the sibling `__tests__/supabase/functions/{github,jira,legacy-mirror}.test.ts` re-run for safety (29/29 green); full-suite run: 20 failed files / 68 failed tests / 6483 passed / 5 skipped / 7 todo out of 6563 — matches today's documented pre-existing baseline exactly (confirmed via a `git stash`-and-rerun of three of the failing files against the unmodified base commit: identical failures, so none of this is caused by this diff); grep-confirmed zero failures under `supabase/`, `data-platform/`, or `auth/` · build pass · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor codegraph)
- ci: pushed clean as `1f9fecb` (fast-forward, no rebase needed). `fly-deploy.yml` (run 36104211835) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36104211846) completed **failure**, but confirmed via `list_workflow_jobs`/`get_workflow_job` to be the same recurring shape as every other commit today: `Lint` step green (06:44:42-06:45:47), `Test` step failed (06:45:47-06:51:59, ~6m12s, matching today's documented ~6m12-14s `Test`-step timing), `Build app`/`Build library` skipped as a consequence — this run's own local full-suite gate above (20 failed files / 68 failed tests, zero in this diff's files, matched byte-for-byte via a `git stash`-and-rerun against the unmodified base commit) already confirmed this is the documented pre-existing baseline, not a regression from this diff, so no revert is warranted. `docker.yml`/`release.yml` were still `in_progress`/queued at push time and not polled further (best-effort, matches this repo's documented workflow-timing pattern; neither is a revert-gating workflow).
- notes: No `.github/workflows/**`, `.env*`, or secret/credential values touched. Migration-free (no `supabase/migrations/**` change; the fix is entirely in `supabase/functions/_shared/` + one function's `index.ts`, both deployed only via the Supabase CLI, which this sandbox has no credentials for) — logging as **FIXED (NOT DEPLOYED)**, not plain FIXED: `supabase functions deploy connections-set-credentials --project-ref hyirnyyqwyvplwvuekda` (no other function imports `_shared/connection-credentials.ts` yet, so no other function needs redeploying for this change). No new user-facing strings added (this is a server-side Edge Function fix with no UI copy change), so no `public/locales/*/openhands.json` updates were needed.

## 07:51 UTC — security — IMPROVED
- mode: section
- todo: -
- previous run: auth-data-platform
- picked because: Step 0 incident check first — read `incidents.md`. No `OPEN` row has `mitigation: none` with something buildable this run: INC-3 is an infra/deploy-config decision explicitly out of scope by standing rule; INC-6 and INC-7 are both explicitly held (deliberately not code-fixed) pending external verification this sandbox cannot do (real Atlassian claim docs / a live browser OAuth round-trip) — did the cheap liveness re-check on both anyway (code unchanged since last confirmation, bumped their timestamps). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. Read the last ~10 working runs across today's log: auth-data-platform, files-terminal-browser, skills-plugins-mcp, kt-video, conversation-chat, kt-video (again, ABANDONED rebase conflict), codegraph, codegraph, and yesterday's environment-onboarding, automations. Removed `auth-data-platform` (most recent run that did work) and `kt-video` (worked twice in the last 6 working runs). Of what remained, `security` had the oldest `lastRun` (2026-09-24T17:59:10Z).
- brainstorm: -
- base: 03f20b07570c1e8ac52938b2294ce7dd5ff0b106  commit: 1f9add51090fc913289a950ab38e1bac2f6efdd2
- changes:
  - Thorough bug hunt across all four section files (`src/routes/security.tsx`, `src/lib/security/security-{types,activity,integrations}.ts`) plus their test file: zero ESLint findings, zero TypeScript findings, the route file was already at 100% statement/line/function coverage (98.36% branch — the one gap is a provably-unreachable `?? 0` fallback), and manual review of every scoping/merge/precedence branch, every loading/error state combination, and every accessibility attribute turned up no real defect. Concluded there is genuinely no bug in this section right now and made three improvements instead, per playbook step 4.
  - `src/routes/security.tsx`: promoted each future-area card's title from a plain `<span>` to a semantic `<h3>` (accessibility) — the page already threads heading ids carefully everywhere else (`FUTURE_AREAS_HEADING_ID`, `EMPTY_STATE_HEADING_ID`, etc.), but the six area titles inside that list were invisible to screen-reader heading navigation, so a user could not jump straight to e.g. "Secrets".
  - `__tests__/routes/security.test.tsx`: added a test asserting each future area now exposes a level-3 heading with the right accessible name; replaced a tautological `it.each` assertion for `SECURITY_MILESTONE_COPY` (it compared `buildSecurityActivityEvent`'s output against the exact same map object the function reads from, so a copy/paste swap of two entries' status/title could never fail it) with independent literal expected strings per milestone kind; added a test locking in `SecuritySummary.counts`/`overallRiskScore` staying `undefined` for a `not_configured` summary, mirroring the existing `SecurityScan` contract test — the type's own doc comment ("Absent (not zeroed) when no scan has ever run") had zero test coverage before this.
  - No new user-facing strings were added (reused existing `I18nKey`s), so no `public/locales/*/openhands.json` changes were needed.
- gate: lint pass (typecheck + `eslint src` + `prettier --check`, clean) · test: touched-file `security.test.tsx` 51/51 green; full-suite run: 69 failed tests / 21 failed files, 6484 passed — independently re-verified via `git stash` of just this run's two changed files and re-running three of the failing files (`root.test.tsx`, `src/hooks/use-app-title.test.tsx`, `__tests__/themes/color-themes.test.tsx`) against the unmodified base commit: identical failures, so none of it is caused by this diff. The specific counts (69/21) are close to but not identical to the 06:42 UTC run's documented baseline (68/20) — see `notes` for a likely explanation and a flag for a human to double-check drift · build pass · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor codegraph, matches the 06:42 UTC run's documented baseline exactly)
- ci: pushed clean as `1f9add5` (fast-forward, no rebase needed — the `03f20b0` base was still current `origin/main` at push time). `fly-deploy.yml` (run 36109996344) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36109996392) and `docker.yml` (run 36109996337) were still `in_progress` after ~1 minute; `release.yml` (run 36109996855) already completed **failure**, matching the same recurring pre-existing shape every commit today has shown (confirmed above via the base-commit stash-and-rerun). Given the actual revert gate (Fly) already succeeded and `ci.yml`/`docker.yml`'s failure-on-this-baseline is already independently confirmed above, did not poll further to stay inside this run's time budget.
- notes: **Pre-existing full-suite regression, documented before by the 06:42 UTC auth-data-platform run and re-confirmed here — NOT introduced by this run.** Two concrete examples found while diagnosing: (1) `src/hooks/use-app-title.ts` hardcodes `const APP_TITLE = "Neo"`, but `src/hooks/use-app-title.test.tsx` still asserts strings like `"My Conversation | OpenHands"` — a leftover from an incomplete OpenHands→Neo rebrand, reproduces 100% in isolation (`npx vitest run src/hooks/use-app-title.test.tsx`), not environment-specific. (2) `__tests__/components/features/sidebar/sidebar.test.tsx` mocks `#/components/features/settings/agent-canvas-version-tile` and asserts `getByTestId("agent-canvas-version-tile")`, but no such component exists anywhere under `src/` on this branch and `sidebar.tsx` never imports one — the test expects a feature that was apparently never wired in on this lineage (its one matching commit in the whole repo's reflog, `3137cdf`, isn't even an ancestor of `main`). Other failing files span `root.test.tsx` (22 cases), `themes/color-themes.test.tsx`, `services/telemetry.test.ts`, `components/backends/add-backend-modal.test.tsx`, library/packaging tests, and more. This is far past the ~15-file/~400-line size cap for any single run to repair, and it now silently blocks step 6's literal gate for every future hourly/focus run unless each one keeps doing the same stash-and-verify workaround this run and the 06:42 UTC run both did. Flagging for a human: worth deciding whether to (a) dedicate a future run explicitly to closing this gap file-by-file under the size cap, accepting several runs' worth of "IMPROVED (test debt)" entries, or (b) have a human do a single pass, since the two examples above suggest most of these ~21 files share one of two root causes (the rebrand string, and stale/aspirational test expectations for features not actually wired up) rather than 21 independent bugs. Also flagging the small 69-vs-68 / 21-vs-20 count drift from the 06:42 UTC baseline for a human to eyeball — could be normal test-order/timeout flakiness in a resource-constrained sandbox rather than a new regression, but wasn't fully bisected here to stay inside this run's time budget.

## 08:52 UTC — settings — FIXED
- mode: section
- todo: -
- previous run: security
- picked because: Step 0 incident check first — read `incidents.md`. No `OPEN` row has `mitigation: none` with something buildable this run: INC-3 is an infra/deploy-config decision explicitly out of scope by standing rule; INC-6 and INC-7 are both explicitly held (deliberately not code-fixed) pending external verification this sandbox cannot do. `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. Read the last ~10 working runs across today's and yesterday's log: security, auth-data-platform, files-terminal-browser, skills-plugins-mcp, kt-video, conversation-chat, codegraph (×2), and yesterday's environment-onboarding, automations. Removed `security` (most recent run that did work); no section was worked twice or more in the last 6 working runs (security, auth-data-platform, files-terminal-browser, skills-plugins-mcp, kt-video, conversation-chat). Of what remained, `settings` had the oldest `lastRun` (2026-09-24T18:45:46Z).
- brainstorm: -
- base: 79372c41a0734a01c9cf30fece4c87758c862643  commit: 31992a78c98ebfc88b5f117adc17e90aa50f26d6
- changes:
  - Found a real bug while hunting `src/components/features/settings/`: `secrets-settings/secret-list-item.tsx`'s Edit/Delete icon buttons built their `aria-label` from a raw template literal (`` `Edit ${title}` `` / `` `Delete ${title}` ``) instead of routing through i18n — this repo's own AGENTS.md explicitly calls out exactly this shape ("ships English to every locale") as a "No Magic Strings" Rule 1 violation, and it slipped past lint because `i18next/no-literal-string` only flags plain string literals, not template-literal expressions with interpolation.
  - Added `SECRETS$EDIT_SECRET_ARIA` / `SECRETS$DELETE_SECRET_ARIA` i18n keys (all 15 supported languages, modeled on the existing `MCP$EDIT_SERVER_ARIA` "Edit {{name}}" convention and this repo's `BUTTON$DELETE` translations for the verb) and wired `secret-list-item.tsx` to `t(...)` them with the secret's name interpolated, matching the pattern already used in `installed-server-card.tsx`.
  - Regenerated `src/i18n/declaration.ts` and every `public/locales/*/openhands.json` via `npm run make-i18n`; `npm run check-translation-completeness` reports full coverage for both new keys.
  - Added `__tests__/components/features/settings/secrets-settings/secret-list-item.test.tsx` (2 cases): asserts the edit/delete buttons expose the new i18n keys as their accessible name (would have failed against the old hardcoded template literal, since the test i18n mock returns the raw key verbatim) and that `onEdit`/`onDelete` still fire on click.
- gate: lint pass (typecheck + `eslint src` + `prettier --check`, clean, no repair needed) · test: touched-file tests green (`secret-list-item.test.tsx` 2/2, `routes/secrets-settings.test.tsx` 4/4); full-suite run: 20 failed files / 68 failed tests / 6487 passed / 5 skipped / 7 todo out of 6567 — matches today's documented pre-existing baseline exactly (first established by the 06:42 UTC auth-data-platform run, re-confirmed by the 07:51 UTC security run); independently re-verified via `git stash -u` of this run's entire diff and re-running a sample of the failing files (`__tests__/bin/agent-canvas.test.ts`, `__tests__/github-workflows/npm-publish.test.ts`, `__tests__/routes/root-index-route.test.ts`, `__tests__/root.test.tsx`, `__tests__/themes/color-themes.test.tsx`) against the unmodified base commit: identical failures (23/35 in that sample), confirming none of it is caused by this diff; zero failures in any settings/secrets file · build pass · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`, matches the documented baseline exactly; this diff touches neither `vendor/` nor codegraph)
- ci: pushed clean as `31992a7` (fast-forward, no rebase needed — the `79372c4` base was still current `origin/main` at push time). `fly-deploy.yml` (run 36115496709) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36115496536) was still `in_progress` after ~2 minutes, matching the same ~13-minute `Test`-step timing every commit today has shown; given the actual revert gate (Fly) already succeeded and this run's own local full-suite gate above already independently confirmed the 20-failed-file/68-failed-test baseline via stash-and-rerun against the unmodified base commit, did not poll further to stay inside this run's time budget.
- notes: Also independently confirmed via the GitHub Actions API that `ci.yml`'s `Test` step has failed on every one of the last 10 pushes to `main` (run numbers 657–666, spanning ~7 hours), reproducing this exact same pre-existing full-suite regression against the authoritative CI signal, not just local flakiness — so the stash-and-rerun workaround this run and its two predecessors (06:42, 07:51 UTC) used is well-founded. Purely a settings/i18n change; no `.github/workflows/**`, `.env*`, secret values, or migrations touched. No Supabase functions touched, so nothing pending a separate deploy.

## 09:43 UTC — knowledge-kt — IMPROVED
- mode: section
- todo: -
- previous run: settings
- picked because: Step 0 incident check first — read `incidents.md` fully and ran the one cheap liveness check the standing rule calls for: `GET https://neo-agent-server.fly.dev/server_info` (INC-3) returned `uptime: 599`s (~10 min), consistent, no change — bumped its `last confirmed` timestamp only. No `OPEN` row has a buildable `mitigation: none` this run: INC-3 is an explicit infra/deploy-config decision out of scope by standing rule; INC-6/INC-7 are both explicitly held pending external verification this sandbox can't do; both re-checked (code unchanged) and left as-is. `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. Read the last ~10 working runs: settings, security, auth-data-platform, files-terminal-browser, skills-plugins-mcp, kt-video, conversation-chat, codegraph (×2), environment-onboarding. Removed `settings` (most recent run that did work); no section was worked twice or more in the last 6 working runs (settings, security, auth-data-platform, files-terminal-browser, skills-plugins-mcp, kt-video). Of what remained, `knowledge-kt` had the oldest `lastRun` (2026-09-24T19:20:00Z, ~13.5h ago).
- brainstorm: -
- base: adbf3d8452b6d0aebfc78d49ab189ceacb2f86c0 (after fast-forwarding a stale local shallow-clone checkout up to current `origin/main` — see notes)  commit: (pending push, see below)
- changes:
  - Bug hunt across every file in `src/lib/knowledge/`, `src/components/features/knowledge/`, and `src/routes/kt-*.tsx` (citation-parser, deepwiki-repo-target, conversation-provisioning, workspace-file-reader, quality-review, pre-analysis, code-evidence, connected-repositories, mermaid-repair, knowledge-engine, knowledge-tabs, kt-list, kt-repository, kt-page, kt-graph, kt-video-tab, kt-video-list, plus the section's `stores/knowledge-store.ts` dependency, which prior knowledge-kt runs have edited directly) turned up no new real defects — this module has been heavily hardened by ~16 prior runs and every race/staleness/cold-load edge case I could find already has an explicit guard and comment.
  - Found a real test-coverage gap instead: `src/lib/knowledge/use-knowledge-rehydration.ts` (292 lines — the single most complex race-handling file in the whole module: live-generation-vs-cold-rehydration fallback, stale-attempt/cancellation guards, cross-route entry reuse) and `src/lib/knowledge/generate-knowledge.ts` (128 lines — the shared generation pipeline every entry point funnels through) were the only two files under `src/lib/knowledge/` with zero dedicated test files, unlike every sibling in the same directory.
  - Added `__tests__/lib/knowledge/generate-knowledge.test.ts` (6 cases): end-to-end success path (store.setReady + navigate called under the right attempt token), error path (store.setError + toast, no navigate), mermaid-repair-pass rejection falling back to raw knowledge, real code-evidence being threaded into the generation request plus triggering the background subsystem-hint upgrade, and the fire-and-forget Supabase persistence queue both skipping with no backend id and firing with one.
  - Added `__tests__/lib/knowledge/use-knowledge-rehydration.test.tsx` (7 cases, using the real `useKnowledgeStore` reset between tests): no-op on an undefined repositoryId, no hydration attempt on an unparseable repositoryId, staying unchecked while the connected-repositories query is still loading, a successful live generation short-circuiting cold rehydration entirely, a failed live generation correctly falling through to cold rehydration and hydrating the persisted Supabase content, going straight to cold rehydration when no live conversation exists, and reporting checked with the store left untouched when nothing was ever generated.
- gate: lint pass (typecheck + `eslint src` + `prettier --check`, clean after one `eslint --fix` formatting pass on the two new test files — pure prettier spacing, no logic changed) · test: both new files 13/13 green; full-suite run: 20 failed files / 68 failed tests / 6500 passed / 5 skipped / 7 todo out of 6580 — the failed-file/failed-test counts match today's documented pre-existing baseline (first established by the 06:42 UTC auth-data-platform run, re-confirmed by 07:51 and 08:52 UTC) exactly, and the passed/total counts are up by precisely +13 (this run's own new tests), confirming zero regressions without needing a separate stash-and-rerun · build pass · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`, matches the documented baseline exactly; this diff touches neither `vendor/` nor codegraph)
- ci: pending — see next log entry for push/deploy outcome
- notes: `npm ci` was run first (dependencies were not pre-installed in this fresh checkout). Preflight surfaced a scary-looking but ultimately benign false alarm worth recording for future runs: the initial `git checkout main && git pull --ff-only` failed with "diverging branches" and `git merge-base` errored with no common ancestor — looked exactly like origin/main history had been rewritten. Root cause was mundane: this was a shallow clone (`git rev-parse --is-shallow-repository` → true) with two different truncation boundaries between local `main` and the freshly-fetched `origin/main`, so `merge-base`/`--is-ancestor` couldn't see past either shallow horizon. `git fetch --unshallow origin` resolved it immediately and confirmed local `main` was a clean 67-commit-behind/0-ahead ancestor of `origin/main` — an ordinary fast-forward, not a rewrite. Also discarded two unrelated working-tree changes that `npm run build` regenerated as a side effect and that I did not intend to touch: `public/codegraph-analyzer/analyze.mjs` (a stale pre-bundled analyzer script whose source under `src/lib/codegraph/` has drifted since it was last regenerated — real drift, but out of scope for this section) and `public/codegraph-analyzer/manifest.json` (just its `builtAt` timestamp) — reverted both via `git checkout --` before committing so this commit stays scoped to knowledge-kt. Flagging the stale analyzer bundle for a future `codegraph` section run to investigate and regenerate properly with its own verification, rather than picking it up as an unreviewed side effect here.

## 10:47 UTC — agentops — IMPROVED
- mode: section
- todo: -
- previous run: knowledge-kt (09:43 UTC)
- picked because: Step 0 incident-first check — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 is an infra/deploy-config decision explicitly out of scope, `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did the cheap liveness/code re-check anyway: INC-3 `GET https://neo-agent-server.fly.dev/server_info` uptime 1444s (~24 min), same known restart-on-deploy pattern, timestamps bumped in incidents.md; INC-6 (`jira-webhook-receiver/index.ts`) and INC-7 (`connections-oauth-start/index.ts`) re-read, both unchanged. `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item → section mode. Most recent working run was `knowledge-kt` (09:43 UTC, excluded). None of the last 6 working runs repeated a section twice, so no further exclusions; of the remaining candidates `agentops` had the oldest `lastRun` (2026-09-24T20:52:00Z, ~14h stale vs. the next-oldest candidate).
- brainstorm: -
- base: 420b9711bfcde79539759eb79982c7742762b3d1  commit: a12a630
- changes:
  - Bug hunt found nothing new to fix: read through `scripts/agentops/{collector,map-events,policy,run-control}.mjs`, the AgentOps HTTP client/service layer, and every component under `src/components/features/agentops/` plus the route files — the section has had 17 prior fixer runs and is unusually well-covered (extensive doc comments calling out exactly the races/edge-cases they guard against, 120 backend + 73 frontend tests all green before this run). No wrong conditional, race, stale closure, or bad cache key found.
  - Instead made 3 real-test-coverage improvements (the section had no bugs but genuine coverage gaps a `--coverage` run made visible):
  - 1. Added a `deriveAgentName` / `deriveModel` / `deriveWorkspaceId` describe block to `agentops-collector.test.ts`: these three pure, already-exported helpers (built specifically to be unit-testable in isolation) had zero direct test coverage — every existing collector test hardcodes `workspace.working_dir: "ws"` and never exercises `launched_agent_profile`/`agent.acp_server`/the model fallback chain or the "unknown"/"OpenHands Agent" defaults, which is exactly the behavior these functions exist to encode.
  - 2. Added `run-timeline.test.tsx` (new file, 4 cases): `run-timeline.tsx` had no dedicated test file at all and the coverage run showed it at 38% statements / 17.5% branches / 23% functions — the lowest of any AgentOps component. Covers the empty-no-spans state, phase-lane grouping/span-count, the select-a-span attribute inspector (including that a null/undefined attribute is filtered out rather than rendered as an empty row), and switching the inspector between two spans.
  - 3. Extended `run-controls.test.tsx` (4 new cases): the existing file only asserted which buttons render per status (11% function coverage) and never exercised the actual mutation path. Added coverage for Pause calling `controlRun("run-1","pause")` directly with no confirmation step, Stop opening the confirmation dialog and NOT calling the runtime until confirmed, Cancel-the-dialog not calling the runtime, and confirming Stop sending `controlRun("run-1","cancel")` and closing the dialog — plus a case asserting the collector's own 409 refusal reason (e.g. "This run is already paused.") reaches the error toast verbatim rather than a generic fallback.
  - All three are test-only changes; no production code touched.
- gate: lint pass (typecheck + `eslint src` + `prettier --check`, clean after one `eslint --fix` formatting pass on the three test files — pure prettier line-wrap/quote-style, no logic changed) · test: all three touched/new files green (`agentops-collector.test.ts` 47/47, `run-timeline.test.tsx` 4/4 new, `run-controls.test.tsx` 13/13 including 4 new); full section suite (`__tests__/scripts/agentops-collector.test.ts` + `__tests__/components/features/agentops/**`) 98/98 green; full-suite run: 20 failed files / 68 failed tests / 6518 passed / 5 skipped / 7 todo out of 6598 — matches today's documented pre-existing baseline exactly (independently re-verified via `git stash` + re-running a sample of the failing files — `root.test.tsx`, `__tests__/themes/color-themes.test.tsx`, `__tests__/services/telemetry.test.ts` — against the unmodified base commit: identical failures reproduce with this diff fully reverted); zero failures mentioning "agentops" · build pass (`public/codegraph-analyzer/{analyze.mjs,manifest.json}` byproducts reverted via `git checkout --` before commit, per established convention) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`, matches the documented baseline exactly; this diff touches neither `vendor/` nor codegraph)
- ci: pushed clean as `a12a630` (fast-forward, no rebase needed). `fly-deploy.yml` (run 36126003698) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36126003688) was still `in_progress` after the ~10-minute best-effort poll window (via `curl`+`$GH_TOKEN`, `gh` unavailable in this sandbox) and was not waited on further; this run's own local full-suite gate above already reproduced the documented pre-existing 20-file/68-test baseline against this exact diff with zero failures mentioning "agentops", so a subsequent `ci.yml` failure would be that same known baseline, not this diff. `docker.yml`/`release.yml` were also still `in_progress` at last check — neither is one of the two workflows (`ci.yml`/`fly-deploy.yml`) this playbook gates a revert on. Checked via the `mcp__github__actions_list`/`actions_get` GitHub MCP tools (`gh` unavailable in this sandbox).
- notes: fresh checkout's local `main` had diverged from `origin/main` in a shallow clone (50 commits each side, no common ancestor visible) — same benign shallow-clone artifact a prior run today already diagnosed (two different truncation boundaries, not a real rewrite). Working tree was clean (no uncommitted work to lose), so resolved with `git reset --hard origin/main` instead of `git fetch --unshallow`, which is equivalent here since there was nothing local to preserve.
