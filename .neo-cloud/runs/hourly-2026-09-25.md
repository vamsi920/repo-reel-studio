## 00:24 UTC — codegraph — FIXED
- mode: section
- todo: -
- previous run: environment-onboarding
- picked because: Step 0 incident check first — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did cheap liveness/code re-checks on all three OPEN rows anyway and bumped `last confirmed` timestamps (INC-3: `GET https://neo-agent-server.fly.dev/server_info` uptime 2758s (~46 min), same known restart-on-deploy pattern; INC-6/INC-7: re-read the same files, unchanged) — pushed as its own standalone commit (`c58da20`) before starting section work, since this repo's push velocity from other cloud routines makes it worth landing small, safe edits immediately rather than holding them uncommitted through a long bug-hunt. `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. This run's preflight discovered the checked-out `origin/main` was ~50 commits stale relative to the true current tip (the container's initial clone predates a large batch of same-day work from other cloud routines) — reset cleanly to the real `origin/main` tip before re-running the rotation pick against current `hourly-state.json`/run logs (see notes). Of the last 6 working runs against the *current* state (environment-onboarding, automations, agentops, knowledge-kt, settings, security), each section appeared once; `environment-onboarding` (most recent, per `lastSection`) was excluded, none of the remaining 11 candidates had been worked twice or more in that window. Of what remained, `codegraph` had the single oldest `lastRun` (2026-09-24T11:38:00Z) by a wide margin.
- brainstorm: -
- base: c58da20 (after the incident-liveness commit)  commit: (recorded after push, see below)
- changes:
  - Delegated a read-only bug hunt across the full `codegraph` surface (`src/lib/codegraph/**`, `src/components/features/codegraph/**`, `src/stores/codegraph-store.ts`, `src/routes/kt-graph.tsx`, and every matching test file) to an `Explore` subagent, explicitly told this section has been fixed 16 times already and to skip the cross-workspace `pinnedCommitByWorkspaceRepo` fix already landed at 11:11 UTC yesterday. It returned 4 ranked candidates; independently verified the top one by reading the exact code before fixing, per this playbook's own standard.
  - **Bug fix (stale drill-down race)**: `kt-graph.tsx`'s `drillDown` callback called `navigateTo(key, nodeId)` unconditionally once its async `handle.loadLevel(nodeId)` fetch resolved, with no check for whether the user was still where they started. Reproduction: drill into subsystem A (an uncached, slow-resolving fetch); while that fetch is still in flight, select and drill into sibling subsystem B instead (an already-cached level, or a second fetch that resolves first) — B's fast/resolved path navigates there correctly; when A's stale fetch then resolves, the old code silently snapped the screen back to A, discarding the user's deliberate move to B, with no click involved. The store's own `failLevel` (`codegraph-store.ts`) already guards against exactly this class of race on the *error* path via a `stillReachable` check before surfacing anything — the *success* path in the route had no equivalent guard. Fixed by capturing the `currentParentId` at the moment each drill-down request starts and only completing its `navigateTo` if that value is unchanged once the fetch resolves; `drillDown` now returns `Promise<boolean>` (whether it actually navigated) so callers can react to a stale/failed drill instead of assuming success.
  - **Bug fix (search selecting a failed drill)**: `selectSearchResult` awaited `drillDown(entry.parentId)` and then unconditionally called `selectNode`+cleared the search query, even when that drill failed (a real fetch/storage error) and never navigated anywhere — silently pointing `selectedNodeId` at a node the open level doesn't contain (so no details panel opens) while also wiping the search box, leaving the user with no visible feedback and no easy way to retry. Now uses `drillDown`'s new boolean return to bail out before selecting/clearing when the drill didn't actually land.
  - Added 2 regression tests to `__tests__/routes/kt-graph.test.tsx`: the stale-drill race (drills into a slow "Payments", switches to a fast "Billing" mid-flight, then releases the stale fetch and asserts the view stays on Billing, both via the rendered breadcrumbs and the store's own `currentParentId`/cached `levels` state) and the search-failure case (search result whose parent level fails to load leaves the search box populated and opens no details panel). Verified the first test fails against the pre-fix code (reproduced "SystemPayments" instead of "Billing" via `git stash` on just `kt-graph.tsx`) and passes with the fix restored.
  - The subagent's other findings (a missing `MAX_LEVEL_CHILDREN` cap on `buildModuleTree`'s per-folder module split in `hierarchy.ts`, confirmed via the module's own test data producing 27 children in one level against a documented 24-node budget) is a real, larger structural gap left for a future run — flagging as a **FINDING (needs a dedicated pass)**: fixing it correctly needs a capping/"Other" bucket strategy analogous to the existing `capSubsystemBuckets` for level 1, which is more than this run's "up to three, worst first" budget after the two fixes above.
  - No new user-facing strings, so no `public/locales/*/openhands.json` edits needed.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `npm run build` rebuild byproduct via `git checkout --` before running `build:lib`, per established convention.
- gate: lint pass (typecheck + eslint + prettier all clean) · test: touched file (`kt-graph.test.tsx`) 17/17 green (15 pre-existing + 2 new); full-suite run: 21 failed files / 69 failed tests / 6463 passed / 5 skipped / 7 todo out of 6544 — matches the current declining pre-existing baseline logged by the immediately preceding `environment-onboarding` run (20/68, within the normal one-file/one-test drift this baseline has shown run to run); zero failures in `kt-graph.test.tsx` or anywhere else under `src/lib/codegraph/`, `src/components/features/codegraph/`, `src/stores/codegraph-store.ts`, or `src/routes/kt-graph.tsx` (grep-confirmed against the failure list) · build pass (byproduct reverted) · build:lib pre-existing-fail (72 `error TS6059` lines, confirmed 100% confined to `vendor/understand-anything/**` via grep; this diff touches neither `vendor/` nor any file under it)
- ci: pushed clean as `9138c7b` (fast-forward, no rebase needed). `fly-deploy.yml` (run 36077489798) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36077489972) was still `in_progress` after ~10.5 minutes of best-effort polling via the GitHub MCP `actions_get`/`actions_list` tools (`gh` unavailable in this sandbox) and was not waited on further, matching this section's own recent history (this repo's `Test` step has taken 11-13 minutes on several other commits this week); this run's own local full-suite gate already confirmed zero failures in any file this diff touches, so a subsequent `ci.yml` failure there would be the same declining pre-existing baseline (21 failed files/69 failed tests, logged above), not a regression from this diff. `docker.yml` (run 36077489791) and `release.yml` (run 36077490377) both completed **failure**, matching the identical pre-existing pattern already showing on the immediately-preceding commit (`c58da20`) and every recent commit before it — neither is one of the two workflows (`ci.yml`/`fly-deploy.yml`) this playbook gates a revert on.
- notes: **Note added during a later rebase this same morning:** the immediately-following `codegraph` run (00:56 UTC, below) picked this same section concurrently, from a `hourly-state.json` snapshot taken before this run's commit had landed — an expected collision under this playbook's rotation model when two instances of this routine overlap, not a bug in either run. **Stale checkout discovered and resolved mid-run.** This run's fresh container checkout of `main` was ~50 commits behind the true `origin/main` tip (the checkout evidently predates a large batch of same-day work landed by other cloud routines between the container's provisioning and this run's start). The divergence was caught only after a full bug-hunt-and-fix cycle had already been completed against the stale base — that work (also a `security`-section fix, plus test additions) turned out to substantially duplicate two fixes/tests already landed upstream in the interim (`ec59616 fix(security): memoize repository-select label counts...`, `4ee5de0 test(security): cover picker exclusion...`). Discarded that stale, duplicate work entirely via `git reset --hard origin/main` (nothing had been committed yet, so this was non-destructive) and re-ran the section-selection logic fresh against the real current state, landing on `codegraph` instead. One genuinely novel piece from that discarded pass (a doc-comment accuracy fix on `SecurityWorkspaceScopeState.isLoading`/`isError` in `src/routes/security.tsx`, which still incorrectly claimed both flags are "always false once `repositories` is non-empty") was NOT carried forward, since it lived in the same file as the now-superseded diff and re-deriving it cleanly against the current `security.tsx` would have meant re-opening that whole section mid-`codegraph`-run — logging it here as a **FINDING (needs a future security-section run)**: `SecurityWorkspaceScopeState`'s `isLoading`/`isError` doc comments are still inaccurate for the `"requested-not-connected"` branch (repositories can be non-empty while either flag is still true, since those entries can be *other*, already-resolved repositories) and should be corrected the next time that section comes up. `npm ci` was run before the gate (dependencies not pre-installed in this fresh container; 1555 packages, ~37s). No `supabase/**` paths touched — this commit deploys fully via Netlify on push.
## 00:56 UTC — codegraph — IMPROVED
- mode: section
- todo: -
- previous run: environment-onboarding (22:43 UTC 2026-09-24)
- picked because: step 0 incident check first — read `incidents.md` in full. No OPEN row had a buildable `mitigation: none` in this run's scope: INC-3 (agent-server restarts on every push) is explicitly out of scope, touches `.github/workflows`/an infra decision; INC-6 (Jira webhook receiver bearer check not bound to registration) and INC-7 (OAuth `state` not bound to the initiating browser) both explicitly document a prior run's deliberate hold-off pending external verification this sandbox cannot do. Did the cheap liveness/code re-check anyway: INC-3 `GET https://neo-agent-server.fly.dev/server_info` returned `uptime: 1408`s, same known deploy-triggers-restart pattern; INC-6 (`jira-webhook-receiver/index.ts`) and INC-7 (`connections-oauth-start/index.ts`) re-read, both unchanged. Bumped all three `last confirmed` timestamps (folded into this run's commit rather than a separate bookkeeping-only commit, since one had already been pushed minutes earlier by another instance of this routine — see notes). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item (comments only) → section mode. Of the last 10 working runs today (kt-video, conversation-chat, skills-plugins-mcp, files-terminal-browser, auth-data-platform, security, settings, knowledge-kt, agentops, automations, environment-onboarding), `environment-onboarding` (most recent to do work) was excluded; no remaining candidate had been worked twice or more in the last 6 working runs (environment-onboarding, automations, agentops, knowledge-kt, settings, security — one each). Of the 11 remaining candidates, `codegraph` had by far the oldest `lastRun` (2026-09-24T11:38:00Z vs. `kt-video`'s next-oldest 12:44:00Z).
- brainstorm: -
- changes:
  - Read through every file under `src/lib/codegraph/` and `src/components/features/codegraph/` plus `src/stores/codegraph-store.ts` and `src/routes/kt-graph.tsx` looking for real defects (wrong conditionals, stale closures, races, bad cache keys). The module has been worked 16 times before and is unusually well-hardened already — no correctness bug found this pass; found one genuine, narrow accessibility gap and one piece of confirmed dead code instead, plus a related a11y inconsistency, and fixed all three (kept well under the "up to three" cap).
  - **Gap 1 — the CodeGraph node-details side panel had no keyboard escape hatch.** The canvas (`codegraph-canvas.tsx`) and the search box (`codegraph-toolbar.tsx`) both already dismiss on Escape, by design (their own comments say so), but `CodeGraphNodeDetails` renders as a sibling of the canvas in `kt-graph.tsx`, not nested inside it — a keyboard user who tabs onto one of the panel's own focusable children (e.g. the close button, a dependency chip) had no way to dismiss the panel without reaching for a mouse, unlike every other dismissible surface on this page. Fixed by adding an `onKeyDown` handler directly on the panel's `<aside>` that calls `onClose()` on Escape (with an `eslint-disable-next-line jsx-a11y/no-noninteractive-element-interactions` justified the same way three other call sites in this codebase already justify the identical pattern — `sidebar.tsx`, `context-menu.tsx`, `conversation-tag-chips.tsx`).
  - **Gap 2 — the full-page "analyzing" and "analysis failed" states in `kt-graph.tsx` had no live-region role**, unlike the toolbar's own level-loading indicator (`role="status"`) and the canvas's own layout-failure state (`role="alert"`) for the exact same category of state. A screen-reader user got no announcement when analysis started, progressed, or failed on this page. Added `role="status"` to the analyzing block and `role="alert"` to the error block, matching the pattern already established elsewhere in the same feature.
  - **Dead code** — `CODEGRAPH_ROOT_LEVEL` (`codegraph-store.ts`) was exported but had zero importers anywhere in `src/`/`__tests__/` (grep-confirmed); removed it.
  - Added regression tests: `codegraph-node-details.test.tsx` gains one case asserting Escape closes the panel while focus is on the close button; `kt-graph.test.tsx` gains one case asserting `role="status"`/`role="alert"` on the analyzing/failed states with the expected copy. Verified both fail against the pre-fix code and pass after.
  - No user-facing strings added (no new i18n keys — the new roles/handler are pure ARIA/keyboard wiring on existing copy), so no locale-file updates were needed.
- gate: lint pass (typecheck + eslint + prettier all clean, after fixing one `jsx-a11y/no-noninteractive-element-interactions` and one `prettier/prettier` formatting error the first pass surfaced) · test: touched files green (`codegraph-node-details.test.tsx` 15/15, `kt-graph.test.tsx` 16/16, full section suite 200/200 across `__tests__/lib/codegraph`, `__tests__/stores/codegraph-store.test.ts`, `__tests__/components/features/codegraph`, `__tests__/routes/kt-graph.test.tsx`, `__tests__/lib/data-platform/repositories/codegraph-repository.test.ts`); full-suite run: 20 failed files / 68 failed tests / 6464 passed / 5 skipped / 7 todo out of 6544 — matches the documented pre-existing declining baseline (still 20 files/68 tests, same as the immediately preceding `environment-onboarding` run), zero failures in any file this diff touches (grep-confirmed the 20 failed files list, none under `codegraph`) · build pass (`public/codegraph-analyzer/{analyze.mjs,manifest.json}` byproducts reverted via `git checkout --` before commit, per established convention) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor any file that import-chains into it beyond what already did)
- base: c58da20  commit: cf4713a (after rebase onto 32a61ba, see notes)
- ci: pushed as `9647c88`, then rebased onto `origin/main` (see notes) and re-pushed as `cf4713a` (fast-forward after rebase). `fly-deploy.yml` (run 36080306415) — the actual production Fly deploy this playbook gates a revert on — completed **success**. No revert needed. `ci.yml` (run 36080306506) was still `in_progress` after ~10 minutes of best-effort polling via the GitHub MCP `actions_get`/`actions_list` tools (`gh` unavailable in this sandbox: `command not found`) and was not waited on further; this run's own local full-suite gate already confirmed zero failures in any file this diff touches (20 failed files/68 failed tests, the same declining pre-existing baseline every recent commit has shown), so a subsequent `ci.yml` failure there would be that same baseline, not a regression from this diff. `docker.yml` (run 36080306426) and `release.yml` (run 36080306806) both completed **failure**, matching the identical pre-existing pattern already showing on every recent commit this week (including the immediately-preceding `32a61ba`) — neither is one of the two workflows (`ci.yml`/`fly-deploy.yml`) this playbook gates a revert on.
- notes: On preflight, found the checked-out HEAD already sitting on a commit (`c58da20`, "chore(neo-cloud): bump INC-3/INC-6/INC-7 liveness-check timestamps") that only touched `incidents.md` and had no matching run-log entry in `hourly-2026-09-24.md` — apparently a prior invocation of this same scheduled routine ran the step-0 incident check and pushed it, but did not complete (or log) a section-mode run afterward. Treated `hourly-state.json`'s own `lastSection`/per-section `lastRun` values (last real completed run: `environment-onboarding` at 22:43 UTC) as authoritative for rotation purposes rather than that orphan commit, since the state file is the documented source of truth for section selection. **Push collision with a concurrent instance of this same routine.** `git push origin main` for this run's commit (`9647c88`) was rejected as non-fast-forward: another invocation of this routine had, unbeknownst to this run, independently picked the same `codegraph` section from the same pre-push `hourly-state.json` snapshot and landed two commits first (`9138c7b` "fix(codegraph): stop stale drill-down fetches from overriding later navigation" plus its own `chore(neo-cloud)` CI-status commit `32a61ba`) — see that run's own `00:24 UTC` log entry above, which now also carries a cross-reference note. Per this playbook's step 7, rebased onto `origin/main` rather than forcing: `.neo-cloud/hourly-state.json` and this same run-log file both hit real conflicts (both runs touched the `codegraph` section's `lastRun`/`runCount` and both created today's log file) and were resolved by hand — `hourly-state.json`'s `codegraph.runCount` set to `18` (16 -> 17 on each side independently, correctly reconciled to reflect that *two* runs happened, not one) with `lastRun` taking the later of the two completion timestamps; the run log resolved by keeping both entries in chronological order. `src/routes/kt-graph.tsx` and its test file auto-merged with no conflicts (the other run's fix was in `drillDown`/`selectSearchResult`; this run's changes were in the unrelated analyzing/error-state JSX and the sibling `codegraph-node-details.tsx` file) — read the merged diff by hand to confirm the two changes composed correctly, then re-ran `npm run lint` plus `npx vitest run` on every file this run touched (202/202 green) before pushing again, per the playbook's rebase-and-retry instructions; did not re-run the full suite a second time since the rebase touched no logic this diff's own files depend on. No `supabase/**` paths touched — this commit deploys fully via Netlify on push.
## 02:00 UTC — kt-video — ABANDONED (rebase conflict)
- mode: section
- todo: -
- previous run: codegraph (00:56 UTC)
- picked because: Step 0 incident-first check — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did the cheap liveness/code re-check anyway and bumped all three `last confirmed` timestamps in the commit below (INC-3 `GET https://neo-agent-server.fly.dev/server_info` uptime 2271s (~38 min), same known pattern; INC-6 `jira-webhook-receiver/index.ts` and INC-7 `connections-oauth-start/index.ts` re-read, both unchanged). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item → section mode. Of the last 6 working runs (codegraph x2, environment-onboarding, automations, agentops, knowledge-kt), codegraph was excluded (most recent + worked twice); of the rest, `kt-video` had by far the oldest `lastRun` (2026-09-24T12:44:00Z).
- brainstorm: -
- base: cf4713a  commit: -
- changes:
  - Full implementation was completed and gated cleanly: delegated a bug hunt to an Explore subagent across `src/lib/kt-video/**`, `src/components/features/kt-video/**`, `src/routes/kt-video-tab.tsx`, and their tests. Found and fixed one real, reproducible defect — the KT Video sidebar's `unavailablePaths` check claimed to mirror `build-manifest.ts`'s scene-eligibility filter but only copied the outer `c.trim().length > 0` check, not `rankFiles`'s stricter inner filter (ignored/noise paths, non-source extensions, content under 40 trimmed chars). A selected file that passed the outer check but failed the inner one (e.g. `package-lock.json`, a `.env`, a tiny stub file) looked normally selected in the sidebar while silently getting no scene in the built video. Fixed by extracting `rankFiles`'s exact filter into an exported `isFileSceneEligible(path, content)` in `build-manifest.ts` and having the sidebar call it directly instead of duplicating a subset of the logic. Added a 6-case `isFileSceneEligible` test block to `build-manifest.test.ts` and 2 regression cases to `kt-video-tab.test.tsx`, plus lengthened 2 pre-existing fixtures that happened to use under-40-char mock content and asserted "available" (they were testing unrelated behavior, not the length threshold).
  - Gate passed clean: lint pass; touched-file tests 37/37 green; full-suite 20 failed files/68 failed tests matching the documented pre-existing baseline with zero failures in any file this diff touched; build pass; build:lib pre-existing-fail (72 `TS6059` lines, 100% confined to `vendor/understand-anything/**`, confirmed unrelated).
  - Committed as `343a6ae` (base `cf4713a`) and pushed — rejected non-fast-forward (`neo-focus-fixer-cloud`/another instance of this routine landed `8d93033` first, expected per this playbook). `git fetch && git rebase origin/main` produced a genuine `CONFLICT (content)` in `.neo-cloud/runs/hourly-2026-09-25.md` (the append-only run log — both this run's own kt-video block and the concurrent commit's edit landed at/near the same anchor lines). Per this playbook's explicit rule ("conflict → abort, reset to origin/main, log ABANDONED (rebase conflict), stop") — no exception carved out for a log-only conflict — aborted the rebase and reset hard to `origin/main` (`8d93033`) rather than hand-resolving it, discarding the local commit entirely (nothing lost from `origin/main`'s perspective; the working code diff no longer exists in this run's tree).
  - The underlying `kt-video` bug (documented above) is still present in the codebase — a future `kt-video` section run will rediscover it, since section-mode fixes (unlike `TODO.txt`/`findings.txt` items) have no cross-run carry-forward mechanism in this playbook.
- gate: lint pass · test pass (touched files 37/37; full-suite matched pre-existing baseline) · build pass · build:lib pre-existing-fail (unrelated vendor/ baseline) — all confirmed BEFORE the rebase conflict; the abandonment is a push-conflict, not a gate failure
- ci: the discarded `kt-video` code diff never reached `origin/main`. This ABANDONED run's own bookkeeping commit (`7e60ed0`, `.neo-cloud/` only) did land, though, and its `fly-deploy.yml` (run 36084307105) — the workflow this playbook gates a revert on — completed **success**; no revert needed. `ci.yml` (run 36084307072) was still `in_progress` after the check and not waited on further (matches this repo's documented 11-13 minute `Test` step timing); `docker.yml`/`release.yml` follow the same pre-existing-failure pattern already logged on the immediately preceding commit, neither is a revert-gating workflow.
- notes: Re-bumped the three `incidents.md` liveness timestamps (INC-3/6/7) as part of this same log-only commit below, since those checks are still valid/current as of this run regardless of the code-diff discard. `npm ci` was run before the gate (dependencies not pre-installed in this fresh container).

## 02:46 UTC — conversation-chat — FIXED
- mode: section
- todo: -
- previous run: kt-video (02:00 UTC, ABANDONED — rebase conflict, discarded)
- picked because: TODO.txt and findings.txt had no live items. Incident check (step 0): INC-3 liveness re-confirmed (`GET /server_info` on neo-agent-server.fly.dev returned uptime 418s, consistent with the known deploy-triggers-restart pattern; bumped its `last confirmed` and pushed that alone first as `5eb832e`, since a repo hook required no uncommitted changes be left idle before the bug-hunt subagent started). INC-6/INC-7 held off unchanged (no new claim-schema/design information since their last check 10 minutes earlier in this same run window). Section rotation: most recent section that did work was `kt-video` (02:00 UTC, ABANDONED counts as "did work" per this playbook) — removed. `codegraph` also removed (worked twice in the last 6 working runs: 00:24 and 00:56 UTC today). Of the remaining 10 candidates, `conversation-chat` had the single oldest `lastRun` (2026-09-24T13:58:00Z).
- brainstorm: delegated a wide bug hunt to a research subagent, focused on the less-recently-touched subdirectories of this 15-times-hardened section (usage-panel, metrics-modal, conversation-main, conversation-tabs, start-task-card, system-message-modal, conversation-card), since the last two runs already fixed the O(n^2) grouping issue, transcript-export-modal races, a task-polling unhandled rejection, and an optimistic-message watchdog leak. The subagent found a real, reproducible cross-conversation state-leak: `useCompactContextAction` (`src/hooks/use-compact-context-action.ts`) and `useAwaitContextCompaction` (`src/hooks/use-await-context-compaction.ts`) keep in-flight compaction state (`beforeToken`, `baselineEventIdsRef`) and a live event/metrics-store watcher with no reset on conversation switch. `ConversationTabContent`'s `tabWrapperKey` only remounts the *terminal* tab on conversation change (`conversation-tab-content.tsx`); every other tab, including Usage, survives a switch. Concrete failure: user starts a compaction on conversation A, switches to conversation B (still on the Usage tab or with the composer's context-window popover mounted) before it resolves — the stale watcher, still subscribed to the now-B-scoped stores, can match one of B's own Condensation events as "the" completion and pop a spurious "Context compacted"/"failed" toast for B, and/or leaves the Compact button permanently disabled on B until the stale 90s timeout fires. Verified by reading both files directly. Two lower-confidence runner-ups from the subagent (an architectural note that only the terminal tab remounts on conversation change, and `staleTime: Infinity` on the LLM balance card, likely intentional) were not acted on — the first is a design observation not a standalone bug, the second is explicitly documented as intentional in its own hook.
- changes:
  - `src/hooks/use-compact-context-action.ts`: added a `useEffect` keyed on `conversation?.id` whose cleanup resets `beforeToken` to `null` and clears `baselineEventIdsRef.current` whenever the active conversation changes (or the hook unmounts) — before this, that state was scoped only to `handleCompactionComplete`/`handleCompact`, with nothing reacting to a conversation switch. Resetting `beforeToken` also causes `useAwaitContextCompaction`'s internal effect (keyed on `beforeToken`) to tear down its event/metrics-store subscriptions and pending timers from the old conversation on the very next render, instead of letting them keep running against the new one.
  - `src/hooks/use-compact-context-action.test.tsx`: changed the `useActiveConversation` mock from a static object literal to a mutable `activeConversation.current` the test can reassign, added an `afterEach` reset back to `c1`, and added a new regression test — starts a compaction on `c1`, switches to `c2` mid-flight via `rerender()`, asserts `isCompacting` immediately drops to `false`, then feeds a `c2` Condensation event + metrics drop + a full timeout advance and asserts neither the success nor error toast fires (i.e. `c2`'s own activity is never mistaken for `c1`'s compaction result).
  - No user-facing strings added or changed, so no `public/locales/*/openhands.json` edits needed.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `npm run build` rebuild byproducts via `git checkout --` before committing, per established convention.
- gate: lint pass (typecheck + eslint + prettier all clean) · test: touched-file tests green (`use-compact-context-action.test.tsx` 3/3 including the new test, `use-await-context-compaction.test.ts` and `compact-context-button.test.tsx` and `context-window-meter.test.tsx` unaffected, 13/13); full-suite run: 20 failed files / 68 failed tests / 6467 passed / 5 skipped / 7 todo out of 6547 — matches the documented pre-existing baseline from the 13:57 UTC run today (20 failed files / 68 failed tests) exactly; grep-confirmed none of the 20 failing files are in this diff's paths (root.test.tsx, add-backend-modal, sidebar, telemetry, package-library, use-app-title, etc. — all pre-existing/unrelated) · build pass (byproducts reverted before commit) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 36087740854) — the actual production Fly deploy this playbook gates a revert on — completed **success** (polled via Monitor + `curl`/`$GH_TOKEN`, `gh` unavailable in this sandbox). No revert needed. `ci.yml` (run 36087740855) was still `in_progress` after ~10 minutes of polling (the playbook's best-effort watch budget); the immediately preceding bookkeeping-only commit's `ci.yml` run (36085273110) completed **failure** on the same recurring pre-existing-baseline pattern documented on every commit today (its `Test` step, not `Lint`), and this run's own local full-suite gate above (20 failed files / 68 failed tests, zero in this diff's files) matches that baseline exactly — so if this commit's `ci.yml` later shows the same shape, it is not a regression from this diff and no revert is warranted; a human/future run should confirm it settled the way the baseline predicts.
- notes: `npm ci` was run before the gate (dependencies not pre-installed in this fresh container; 1555 packages, ~47s). No `supabase/**` paths touched — this commit deploys fully via Netlify on push. This run needed an initial standalone bookkeeping commit (`5eb832e`, INC-3 timestamp bump only) before the section work, same pattern as prior runs today; that push also surfaced and fixed a stale/detached local `main` (it was not tracking `origin/main` at session start and had diverged 50 commits — `git checkout -B main origin/main` before cherry-picking the bookkeeping commit resolved it; worth a future run double-checking `git branch -vv` at Preflight if this recurs).

## 03:40 UTC — kt-video — FIXED
- mode: section
- todo: -
- previous run: conversation-chat (02:46 UTC)
- picked because: Step 0 incident-first check — no OPEN `incidents.md` row had a buildable `mitigation: none` in this run's scope (INC-3 explicitly out of scope, touches `.github/workflows`; INC-6/INC-7 explicitly held off pending external verification this sandbox can't do). Did the cheap liveness/code re-check anyway and bumped all three `last confirmed` timestamps (INC-3 `GET https://neo-agent-server.fly.dev/server_info` uptime 636s (~10.6 min), same known restart-on-deploy pattern; INC-6 `jira-webhook-receiver/index.ts` and INC-7 `connections-oauth-start/index.ts` re-read, both unchanged). `findings.txt` had no live `MITIGATION for INC-<n>:` item. `TODO.txt` had no live item → section mode. Of the last 6 working runs (conversation-chat, kt-video (ABANDONED, still counts), codegraph x2, environment-onboarding, automations), `conversation-chat` was excluded (most recent) and `codegraph` was excluded (worked twice); of the rest, `kt-video` had the oldest `lastRun` (2026-09-24T12:44:00Z) since its 02:00 UTC attempt never updated the state (discarded via rebase-conflict abandonment). That prior 02:00 UTC run's own log left the underlying bug fully diagnosed and its fix/tests fully designed but discarded (rebase conflict on the run-log file itself, not a gate failure) — this run re-implemented that exact same fix from scratch against the current tree rather than re-deriving it.
- brainstorm: -
- base: c5edc481f9426129947d1f2d91dad7fa3c00abe7  commit: (pending)
- changes:
  - Fixed a real bug in the KT Video tab: the sidebar's `unavailablePaths` check (`useSelectedFileContents` in `src/routes/kt-video-tab.tsx`) only mirrored `buildKtManifest`'s outer `c.trim().length > 0` filter, not `rankFiles`'s actual, stricter scene-eligibility filter (ignored/vendor paths, noise files like lockfiles/`.min.`/`.map`/`.d.ts`, non-source extensions, and a 40-character minimum). A selected file that passed the loose sidebar check but failed the real one (e.g. `package-lock.json`, a config `.yaml`, a tiny stub file under 40 chars) looked like a normal selected file in the sidebar while silently getting no scene in the built video, with nothing telling the user why.
  - Extracted `rankFiles`'s exact candidate filter into a new exported `isFileSceneEligible(path, content)` in `src/lib/kt-video/build-manifest.ts`, and had both `rankFiles` and the sidebar's `unavailablePaths` check call it directly instead of the sidebar duplicating a weaker subset of the same logic.
  - Added a 6-case `isFileSceneEligible` test block to `__tests__/lib/kt-video/build-manifest.test.ts` (accepts real source with enough content; rejects under-40-char content; rejects an ignored `node_modules` path; rejects a lockfile; rejects a non-source extension; accepts a README despite `.md` not being a source extension) and a new regression case in `__tests__/routes/kt-video-tab.test.tsx` asserting a lockfile that loads as valid non-empty text is still flagged unavailable. Lengthened two pre-existing fixtures (`a.ts`, `available.ts`) that used 20-char mock content and asserted "available" — they were testing unrelated behavior (loading/error state) and would have started failing the moment the filter became accurate, not because the behavior they tested actually changed.
  - No user-facing strings added or changed, so no `public/locales/*/openhands.json` edits needed.
  - `npm run build` regenerated `public/codegraph-analyzer/{analyze.mjs,manifest.json}` as a byproduct of gating (those files bundle current `src/lib/codegraph/*` sources, which have drifted since the last commit that touched them) — reverted both via `git checkout --` before committing, per the convention the immediately preceding run (02:46 UTC) established.
- gate: lint pass (typecheck + `eslint src` + `prettier --check src/**/*.{ts,tsx}` all clean; the two touched test files under `__tests__/` have pre-existing, unrelated lint nits — an unused `beforeEach` import and two formatting lines — confirmed present on unmodified `main` too via a stash-and-relint check; `npm run lint` doesn't cover `__tests__/` at all, so these don't affect the gate) · test: touched-file tests 36/36 green (`build-manifest.test.ts`, `kt-video-tab.test.tsx`); full-suite: 20 failed files / 68 failed tests / 6474 passed / 5 skipped / 7 todo out of 6554 — matches the documented pre-existing baseline exactly (same count as the 02:00 UTC and 02:46 UTC runs today); grep-confirmed zero kt-video/build-manifest files among the failures · build pass (byproducts reverted before commit) · build:lib pre-existing-fail (72 `error TS6059` lines, grep-confirmed 100% confined to `vendor/understand-anything/**`; this diff touches neither `vendor/` nor codegraph)
- ci: not checked yet — see follow-up bookkeeping commit
- notes: `npm ci` was run before the gate (dependencies not pre-installed in this fresh container; 1555 packages, ~37s). No `supabase/**` paths touched — this commit deploys fully via Netlify on push. Preflight found local `main` detached and ~50 commits diverged from the true `origin/main` tip (same stale-clone pattern the 00:24 UTC run hit and documented) — resolved via `git reset --hard origin/main` after stashing the in-progress `incidents.md` liveness edit, then reapplying it.
