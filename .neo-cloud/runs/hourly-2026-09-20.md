## 00:49 UTC — skills-plugins-mcp — IMPROVED
- mode: section
- todo: -
- previous run: conversation-chat (23:30 UTC, 2026-09-19)
- picked because: `TODO.txt` had only header comments, no live item. Step 0 incident check: `incidents.md` has no `OPEN` row with a buildable `mitigation: none` in scope this run — INC-1 and INC-4 are already `MITIGATED` with substantive mitigation text; INC-3's `mitigation: none` explicitly names it an infra/deploy-config decision touching `.github/workflows`, out of scope for every routine by standing rule (did a cheap liveness re-check on it anyway — see notes). `findings.txt` had no `MITIGATION for INC-<n>:` item to defer to `neo-focus-fixer-cloud`. Fell through to section mode (2b): of the last 10 working runs (skills-plugins-mcp, files-terminal-browser, auth-data-platform, security, settings, knowledge-kt, agentops, automations, automations, environment-onboarding, codegraph, kt-video, conversation-chat — trimmed to the most recent 10), `conversation-chat` (most recent to do real work, also `lastSection`) was excluded, and `automations` was excluded for appearing twice in the last 6 working runs (automations, automations, environment-onboarding, codegraph, kt-video, conversation-chat). Of the 10 remaining candidates, `skills-plugins-mcp` had the single oldest `lastRun` (2026-09-19T11:29:00Z) — no tie-break needed.
- brainstorm: -
- base: 11383c24b0c99ec19f573848ce529981b764ab53  commit: (see push below)
- changes:
  - Read the section's real scope end to end (`src/components/features/skills/**`, `src/components/features/plugins/**`, their host routes `src/routes/skills-plugins.tsx` / `src/routes/skills-settings.tsx`, and every query/mutation hook they call) hunting for real defects per step 3 — confirmed via prior run logs that this section's scope is skills+plugins only, not `mcp-page`/`mcp-settings` (the `-mcp` in the section key is historical; every past `skills-plugins-mcp` run touched only skills/plugins files). Found the area unusually well-hardened already: the React-Query backend-id keying convention is applied everywhere it needs to be, the install/uninstall/refresh/toggle busy-tracking race from an earlier run this week (`pendingBusyNames`) is still correct, `skill-filter.ts`'s facet/search/URL-sync logic is pure and already exhaustively guarded, and `PluginFilesSection`'s loading state (which could have repeated the `isLoading`-vs-`isPending` disabled-query bug fixed elsewhere in `files-terminal-browser` this week) is safe here because its query is never rendered while disabled. No real bug found, so made 3 improvements instead (real test coverage, worst gap first by blast radius):
  - **Zero test coverage on `SkillCard`** (`src/components/features/skills/skill-card.tsx`): the card every skill in the catalog renders through — open-on-click, open-on-Enter/Space (with the nested toggle/copy-button click-bubbling guard), the enable toggle, the copy-source button and its 2s "Copied" reset, and the non-copyable-source case — had no direct test at all; only its pure sub-helpers (`getSkillCardDescription`, `buildSkillPills`, `isCopyableSkillSource`) were tested standalone. Added `__tests__/components/features/skills/skill-card.test.tsx` (6 tests): card-click and keyboard-open, toggle-does-not-open, copy-source-does-not-open (with `navigator.clipboard` stubbed per the existing `copy-text-to-clipboard.test.ts` pattern), the non-copyable-source case, and description/pills rendering.
  - **Zero test coverage on `PluginCard`** (`src/components/features/plugins/plugin-card.tsx`): the plugins-list counterpart, with the same open/keyboard/bubbling contract plus install vs. enable-toggle vs. local-badge branching and the busy/installing label swap, also had no direct test. Added `__tests__/components/features/plugins/plugin-card.test.tsx` (6 tests) mirroring the `SkillCard` cases plus the local-badge and busy-disables-install cases.
  - **Zero test coverage on `PluginDetailModal`** (`src/components/features/plugins/plugin-detail-modal.tsx`): the modal every plugin card opens into — install vs. installed (toggle/refresh/uninstall) vs. local branching, the busy-disables-every-action state, the bundled-skills list, and the conditional Start-conversation button — had no direct test; only its `PluginFilesSection` sub-piece was tested in isolation. Added `__tests__/components/features/plugins/plugin-detail-modal.test.tsx` (6 tests) covering all of the above.
  - All 18 new tests reproduce real DOM interactions (`@testing-library/user-event`) against the actual components, not shallow snapshots; none needed new mocks beyond the existing clipboard-stub pattern used elsewhere in the repo.
  - No new user-facing strings, no bug fixes, so no `public/locales/*/openhands.json` changes and no other source files touched.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct from `npm run build` before committing, per established convention.
- gate: lint pass (typecheck + eslint + prettier, no repair needed) · test pre-existing-fail: 27 failed files / 139 failed tests (6183 passed, 5 skipped, 7 todo, 6334 total) — matches the exact 27-file/139-test baseline documented in every hourly/focus-fixer run this week (`agent-server-adapter.test.ts`, `sidebar.test.tsx`/`sidebar-email-verification.test.tsx`, `root.test.tsx`, `use-app-title.test.tsx`, `add-backend-modal.test.tsx`, `legacy/**`, etc.); all 18 new tests across the 3 new files pass and are not in the failing list · build pass (byproduct reverted, see above) · build:lib pre-existing-fail (same documented `vendor/understand-anything/**` TS6059 rootDir gap logged in every hourly/focus-fixer run this week — nothing in `vendor/` touched by this diff)
- ci: `fly-deploy.yml` (run 35479846460) — the production Fly deploy this playbook actually gates a revert on — completed **green** (`conclusion: success`) for `9785369` (commit `978536955fd5ca70ff0c967a6c51918df697e545` — GitHub's API padded/mangled the short SHA in its own response; confirmed the correct full SHA via `git rev-parse HEAD` and cross-checked `head_sha` on every run below). No revert needed. `ci.yml` (run 35479846476)'s `test-and-build (ubuntu)` job failed (28 failed files / 140 failed tests), but cross-checked against the immediately preceding commit's own CI run (`11383c2`, run 35476842543) and found the **identical** 28-failed-file/140-failed-test shape there too — including the one test (`new-conversation.test.tsx`'s "should create an empty conversation and navigate when pressing the launch from scratch button") that doesn't reproduce in this sandbox's local `npm test` (27/139) but does fail identically in CI on both commits, so it's a CI-environment-specific pre-existing flake, not caused by this diff. `docker.yml` (run 35479846461) also failed identically (`Build & Push (amd64)`/`(arm64)`) on both this commit and the immediately preceding one — same established pre-existing pattern documented in every hourly/focus-fixer run this week. `release.yml` failed too, matching the same already-failing-independent-of-any-diff pattern noted in prior runs. None of these are the playbook's revert criteria (a confirmed *new* failure on `ci.yml`/`fly-deploy.yml` caused by this diff) — `fly-deploy.yml` is green and `ci.yml`'s failure pre-dates this commit unchanged.
- notes: Fresh checkout's local `main` was left at a stale commit (`37149a0`) from container image/shallow-clone setup while `origin/main` had since advanced 50 commits — `git checkout main && git pull --ff-only` failed with "diverged, 50 and 50 different commits each" because the shallow clone's depth cut off the real common ancestor (`git merge-base` came back empty at the original depth). Working tree was clean, so resolved non-destructively this time by deepening instead of resetting: `git fetch --unshallow origin main`, which recovered the true shared history (`git merge-base --is-ancestor main origin/main` then confirmed local `main` was a strict ancestor with zero unique commits of its own), then `git merge --ff-only origin/main` — a genuine fast-forward, no `reset --hard` needed and no local work at risk. Ran `npm ci` first since the fresh sandbox had no `node_modules`. Step 0 liveness check on INC-3: `GET https://neo-agent-server.fly.dev/server_info` returned `uptime: 1729`s (~28.8 min, implying a ~23:44 UTC start) vs. the prior 23:09 UTC check's `uptime: 1169`s (implying a ~22:49 UTC start) — a restart did occur in between, consistent with the documented deploy-triggers-restart behavior; bumped `last confirmed` in `incidents.md`, no change to report.

## 01:42 UTC — files-terminal-browser — FIXED
- mode: section
- todo: -
- previous run: skills-plugins-mcp (00:49 UTC)
- picked because: `TODO.txt` had no live item (only header comments). Step 0 incident check: `incidents.md` has no `OPEN` row with a buildable `mitigation: none` in scope this run — INC-1 and INC-4 are already `MITIGATED` with substantive mitigation text; INC-3's `mitigation: none` explicitly names it an infra/deploy-config decision touching `.github/workflows`, out of scope for every routine by standing rule (did a cheap liveness re-check on it anyway — see notes). `findings.txt` had no `MITIGATION for INC-<n>:` item to defer to `neo-focus-fixer-cloud`. Fell through to section mode (2b): of the last 10 working runs (settings, knowledge-kt, agentops, automations, automations, environment-onboarding, codegraph, kt-video, conversation-chat, skills-plugins-mcp), `skills-plugins-mcp` (most recent to do real work, also `lastSection`) was excluded, and in the last 6 working runs (automations, environment-onboarding, codegraph, kt-video, conversation-chat, skills-plugins-mcp) no section repeated, so nothing else was excluded on the "twice in 6" rule. Of the 11 remaining candidates, `files-terminal-browser` had the single oldest `lastRun` (2026-09-19T12:29:00Z) — no tie-break needed.
- brainstorm: -
- base: fbd7d4a65d213e03cc091aaffeeb0945ec1584e0  commit: 187167e9b951e57a1c9178e83d3e05902659baa6
- changes:
  - Re-read the section end to end (`src/components/files{,-tab}` re-exports, `src/components/features/files-tab/**`, `src/components/features/terminal/**`, `src/components/features/browser/**`, and the backing hooks/stores: `use-terminal.ts`, `use-workspace-files.ts`, `use-workspace-file-content.ts`, `use-unified-get-git-changes.ts`, `files-tab-store.ts`, `browser-store.ts`, `use-auto-refresh-files-on-edit.ts`, `use-workspace-mutation-counter.ts`, `routes/files-tab.tsx`) hunting for real defects per step 3. Five prior runs this week already hardened almost every corner of this area (loading-state `isPending` vs `isLoading` fixes, the quick-row DOM/tab-order cap, etc.), so most files read clean.
  - **Bug — `TreeNode` never reveals an already-selected file when the tree is opened** (`src/components/features/files-tab/tree-node.tsx`): every directory's expand/collapse state started at a hardcoded `useState(false)` with no reaction to `selectedPath`. `selectedPath` can be set before the user ever touches the tree — the files-tab's own auto-select-on-load effect picks the highest-priority file the moment the listing arrives, and `useFilesTabStore`'s `setSelectedPath` is explicitly "hoisted out of files-tab.tsx local state so non-React callers (e.g. the canvas_ui tool dispatcher in the WebSocket context) can drive selection" per that store's own doc comment. In both cases, if the selected file lives inside a nested folder and the user then opens the collapsed-by-default tree (`isTreeVisible`), every ancestor directory renders closed, so the file (and its `aria-current` highlight) is invisible until the user manually expands every folder down to it — with zero visual clue where to even look. Fixed by deriving each directory's initial `isOpen` from whether it contains the current `selectedPath`, plus a `useEffect` that re-opens (never force-closes) a directory the moment the selection lands inside it — so a later programmatic selection (canvas_ui tool, or the tab's own auto-select effect resolving after first paint) still reveals itself, while a user's manual collapse of that same directory is still respected afterward.
  - Updated the existing `file-tree-view.test.tsx` "marks the selected file row with aria-current" test: it used to manually click the directory open before asserting the highlight, which the fix now makes redundant (and, since the directory auto-opens, that click would actually toggle it back *closed*, breaking the test) — removed the click, keeping the same assertions. Added two new regression tests: one asserting a file nested two directories deep already has every ancestor `aria-expanded="true"` and its own row `aria-current="true"` with no interaction at all, and one asserting a user's manual collapse-after-open isn't immediately undone.
  - Read the rest of the section carefully (`browser.tsx`, `browser-chrome-bar.tsx`, `browser-snapshot.tsx`, `empty-browser-message.tsx`, `terminal.tsx`, `empty-terminal-message.tsx`, `file-content-viewer.tsx`, `highlighted-source-view.tsx`, `file-list-error.tsx`, `file-list-truncated-notice.tsx`, `segmented-toggle.tsx`, `no-file-selected-message.tsx`, `use-unified-get-git-changes.ts`) and found nothing else worth fixing — all already carry hardening from prior runs this week. `FileItem`/`FileList` (`src/components/files/`) remain intentionally untouched dead-in-app code that's part of the published npm library's public API, per a prior run's note.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention.
- gate: lint pass (typecheck + eslint + prettier, no repair needed) · test: full suite 28 failed files / 140 failed tests / 6184 passed (documented baseline this week has been 27-29 failed files / 139-142 failed tests — this run is within that range, not a regression); `file-tree-view.test.tsx` (now 8 tests, up from 6) does not appear in the failing-file list, independently re-run standalone: 8/8 passing · build pass (byproduct reverted, see above) · build:lib pre-existing-fail (`vendor/understand-anything/**` TS6059 rootDir errors, same documented gap logged in every hourly/focus-fixer run this week, unrelated to this diff — nothing in `vendor/` touched)
- ci: `fly-deploy.yml` (run 35482164910) — the production Fly deploy this playbook actually gates a revert on — completed **green** (`conclusion: success`) for `187167e`. No revert needed. `docker.yml` (run 35482164943) and `release.yml` (run 35482165282) both `completed`/`failure`, but cross-checked against the immediately preceding two commits (`fbd7d4a`, `9785369`) and found the identical failure shape on both — pre-existing, unrelated to this diff, matching this week's documented pattern. `ci.yml` (run 35482164944) was still `in_progress` after ~3 minutes of polling; given the actual deploy gate is already green and this diff carries no `server/**`/`supabase/**` risk, moved on per step 8's best-effort allowance rather than continuing to poll `ci.yml` to completion — the same call several prior runs this week made.
- notes: Fresh checkout: local `main` and `origin/main` had **completely unrelated histories** (both exactly 50 commits, `git merge-base` returned nothing) — but tracing it further this time: the container's actual starting `HEAD` was already a detached checkout at `origin/main`'s true current tip (confirmed by reading `incidents.md`/`hourly-state.json`/`TODO.txt` *before* touching git at all — their content already matched today's real state), and a separate, stale local branch happened to also be named `main`, pointing at an unrelated, much older commit. `git checkout main` switched onto that stale leftover branch instead of tracking origin. This is local branch-naming leftover, not a real GitHub force-push/history-rewrite — resolved non-destructively without touching the existing `main` ref (a full branch reset was blocked by this sandbox's own safety classifier as "irreversible local destruction," reasonably so) by creating a fresh branch directly from `origin/main` (`git checkout -b hourly-work origin/main`) and working from there; pushed to the `main` ref on `origin` at push time instead of via a local branch named `main`. Worth flagging for a future run: if this exact "unrelated 50/50 histories" shape recurs, checking whether the *actual* starting `HEAD` (before any `git checkout main`) already matches real `origin/main` content is a fast way to tell "stale local branch artifact" apart from a genuine remote rewrite. Ran `npm ci` before the gate: the fresh sandbox's `node_modules` existed but was broken (missing `vite` itself, likely a stale/partial cache), causing every Vitest invocation to fail at config-load until reinstalled. Step 0 liveness check on INC-3: `GET https://neo-agent-server.fly.dev/server_info` returned `uptime: 2240`s (~37.3 min, implying a ~01:05 UTC start) vs. the prior 00:12 UTC check's `uptime: 1729`s (~23:44 UTC start) — a restart did occur in between, consistent with the documented deploy-triggers-restart behavior; bumped `last confirmed` in `incidents.md`, no change to report.

## 02:31 UTC — auth-data-platform — FIXED
- mode: section
- todo: -
- previous run: files-terminal-browser (01:42 UTC)
- picked because: `TODO.txt` had no live item (only header comments). Step 0 incident check: `incidents.md` has no `OPEN` row with a buildable `mitigation: none` in scope this run — INC-1 and INC-4 already carry substantive `mitigation:` text (not `none`); INC-3's `mitigation: none` explicitly names it an infra/deploy-config decision touching `.github/workflows`, out of scope for every routine by standing rule (did a cheap liveness re-check on it anyway — see notes). `findings.txt` doesn't exist in this checkout, so no `MITIGATION for INC-<n>:` item to defer. Fell through to section mode (2b): of the last 10 working runs (files-terminal-browser, skills-plugins-mcp, conversation-chat, kt-video, codegraph, environment-onboarding, automations, automations, agentops, knowledge-kt), `files-terminal-browser` (most recent, also `lastSection`) was excluded; in the last 6 working runs (skills-plugins-mcp, conversation-chat, kt-video, codegraph, environment-onboarding, automations) no section repeated, so nothing else was excluded on the "twice in 6" rule. Of the 11 remaining candidates, `auth-data-platform` had the single oldest `lastRun` (2026-09-19T13:29:00Z) — no tie-break needed.
- brainstorm: -
- base: bc1fa0ee94391b39411e43895d2621222b194416  commit: 47f11e98a32d5c2f59c44556c8fc2c06724446b5
- changes:
  - Read the section's scope (`src/components/features/auth/**`, `src/lib/data-platform/**`, `supabase/functions/**`) hunting for real defects per step 3, focusing first on `src/**` (Netlify-deployed) since a `supabase/functions/**` fix would need `FIXED (NOT DEPLOYED)` handling.
  - **Bug — sign-in was wrongly gated by the *signup* domain allowlist** (`src/lib/data-platform/auth-flow.ts`'s `signInWithPassword`, and the pre-submit check in `src/components/features/auth/password-auth-form.tsx`): both called `isAllowedSignupEmail`/`isEmailInDomainAllowlist` unconditionally, on every mode including plain sign-in. The allowlist mirrors the `enforce_signup_domain_allowlist` Postgres trigger, which the auth-flow module's own doc comments say fires only on `insert`/`update of email` (account creation) — it has no server-side bearing on login at all. So an already-existing account (created before the allowlist existed, or before an admin narrowed it, or simply outside today's list for any other reason) would be locked out of its own sign-in by this purely client-side pre-check, even though nothing server-side would ever reject that login. Fixed by dropping the allowlist gate from `signInWithPassword` entirely, and by scoping the form's pre-submit domain check to `isSignUp || isForgot` (sign-up and the domain-restricted direct-password-reset POC both legitimately keep it; sign-in no longer does).
  - Updated `__tests__/components/features/auth/password-auth-form.test.tsx`: the old "rejects a non-allowlisted email locally" test exercised the default (sign-in) mode, which was actually asserting the buggy behavior — moved it to sign-up mode (where the gate is intentional) and added a new test asserting sign-in with a non-allowlisted email still calls `signInWithPassword` and shows no domain error. Added a matching unit test to `__tests__/lib/data-platform/auth-flow.test.ts` asserting `signInWithPassword` doesn't gate on the allowlist.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct from `npm run build` before committing, per established convention.
- gate: lint pass (typecheck + eslint + prettier, no repair needed) · test: full suite 28 failed files / 140 failed tests / 6186 passed (documented baseline this week has been 27-29 failed files / 139-142 failed tests — this run is within that range, not a regression); independently confirmed by running the base commit's unmodified tree through the same failing files (sidebar.test.tsx, new-conversation.test.tsx, legacy/src/lib/smeAgent.test.ts) — identical failures reproduce with none of this diff applied. All 3 new/modified tests across the 2 touched test files pass and are not in the failing list · build pass (byproduct reverted, see above) · build:lib pre-existing-fail (same documented `vendor/understand-anything/**` TS6059 rootDir gap logged in every hourly/focus-fixer run this week — nothing in `vendor/` touched by this diff)
- ci: not checked — `gh` unavailable in this sandbox (command not found)
- notes: `git checkout main` initially failed ("would be overwritten by checkout") because the incidents.md edit from step 0 was already in the working tree when a stale local `main` branch (pointing at an old commit, same "leftover local branch" shape prior runs this week have hit) tried to check out. Fetched `origin/main` directly and confirmed it had force-moved past the stale local `main` (`37149a0` -> `bc1fa0e`, a genuine `+` forced-update line from `git fetch`, i.e. `neo-focus-fixer-cloud` or an earlier version of this routine had already reset the remote history since the local branch ref was cached) — but the new `origin/main` tip (`bc1fa0e`) matched the container's actual starting `HEAD` exactly, so this was the same "stale local branch artifact, not a real destructive rewrite" shape documented in the last two runs' notes, not new data loss. Resolved with `git checkout -B main origin/main` (moves the local `main` ref to match, discarding only the stale pointer, not any commits) and proceeded. Ran `npm ci` before the gate since `node_modules` didn't exist yet in this fresh checkout. Step 0 liveness check on INC-3: `GET https://neo-agent-server.fly.dev/server_info` returned `uptime: 1230`s (~20.5 min, implying a ~01:49 UTC start) vs. the prior 01:42 UTC check's `uptime: 2240`s (~01:05 UTC start) — another restart occurred in between, consistent with the documented deploy-triggers-restart behavior; bumped `last confirmed` in `incidents.md`, no change to report.

## 03:41 UTC — security — IMPROVED
- mode: section
- todo: -
- previous run: auth-data-platform (02:31 UTC, this file)
- picked because: `TODO.txt` had no live item (only header comments). Step 0 incident check: `incidents.md` has no `OPEN` row with a buildable `mitigation: none` in scope — INC-1/INC-4 are already `MITIGATED` with substantive mitigation text; INC-3's `mitigation: none` explicitly names it an infra/`.github/workflows` decision, out of scope for every routine by standing rule. Did the cheap liveness check anyway: `GET https://neo-agent-server.fly.dev/server_info` → `uptime: 2386`s (~02:34 UTC start), consistent with the known deploy-restart pattern — bumped `last confirmed`, no status change. `findings.txt` had no `MITIGATION for INC-<n>:` item (file is empty besides its header). Fell through to section mode (2b): `auth-data-platform` (most recent to do real work) excluded; of the last 6 working runs (auth-data-platform, files-terminal-browser, skills-plugins-mcp, conversation-chat, kt-video, codegraph) none repeated, so nothing else was excluded on the "twice in 6" rule. Of the remaining 11 candidates, `security` had the single oldest `lastRun` (2026-09-19T14:40:00Z vs. 15:44:39Z+ for every other candidate) — no tie-break needed.
- brainstorm: -
- base: edfea2433946bd1e5b0693926ee45b817903c3a6  commit: 058317a
- changes:
  - Re-read the literal `security` section (`src/lib/security/{security-activity,security-integrations,security-types}.ts`, `src/routes/security.tsx`) plus the "live surface" five prior runs this week established (`src/stores/security-analyzer-store.ts`, `src/contexts/conversation-websocket-context.tsx`'s clear-on-switch effect, `conversation-confirmation-buttons.tsx`, `risk-alert.tsx`, `get-action-content.ts`'s `getRiskText`/`getExecuteBashActionContent`, `bash.tsx`'s risk badge). Independently re-verified `getActionContent` is only ever a fallback (`resolveVisualizerBody(event) ?? getActionContent(event)`) so `bashVisualizer` and `get-action-content.ts` can never both render risk text for the same bash action — ruling out a duplicate-warning theory. Also verified all 37 `SECURITY$*` i18n keys have real, non-empty values in all 15 supported languages (`en, ja, zh-CN, zh-TW, ko-KR, no, ar, de, fr, it, pt, es, ca, tr, uk`) — no missing-translation bug. Found no new bugs — confirms the 09-19 14:40 run's own conclusion that this surface is now bug-free. Made 3 improvements instead, worst gap first:
  - **Accessibility: the "which repository is this page scoped to" paragraph (`security-workspace-scope`, `src/routes/security.tsx`) had no `role="status"`**, unlike its two sibling states in the same conditional block (`security-no-workspace`, `security-repository-not-connected`, both already `role="status"`). Switching repositories via the picker re-renders this paragraph's text (e.g. "acme/api@abcdef1" → "acme/web@abcdef1") with no announcement to screen-reader users — the one place the page tells you what you're actually looking at changes silently. Same category as the 09-19 14:40 run's `RiskAlert` `role="alert"` fix. Added `role="status"` (one line). Extended two existing tests (`__tests__/routes/security.test.tsx`) to assert the role, including after a picker-driven re-scope.
  - **Real test coverage: `SECURITY_MILESTONE_COPY` (`security-activity.ts`) only had built-event assertions for 3 of its 5 milestone kinds** (`scan.started`, `scan.failed`, `findings.ready` — `dependencies.analyzed` and `remediation.verified` were untested against `buildSecurityActivityEvent`'s actual output). A typo in either entry's `status`/`title` would ship silently. Added an `it.each` over all 5 `SecurityMilestoneKind` values asserting the built event's `kind`/`status`/`title` match the table exactly.
  - **Real test coverage: the severity legend (`SeverityLegend` in `security.tsx`) had no assertion of its actual content or order** — existing tests only checked the region's accessible name equalled the list, not that `SEVERITY_KEY` maps each of the 5 severities to the right label in the documented worst-first order. Added a test asserting the legend's five `<li>` items render `critical, high, medium, low, info` in exactly that order, so a copy/paste swap in the mapping (e.g. HIGH ↔ MEDIUM) would be caught.
  - No new user-facing strings (all three changes reuse existing i18n keys / add no new copy), so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean) · test: full suite 27 failed files / 139 failed tests / 6193 passed / 5 skipped / 7 todo (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline; independently spot-checked the one failure that looked adjacent, `agent-server-adapter.test.ts`'s "derives confirmation and security settings" test — confirmed its failure is an unrelated pre-existing mock-setup gap, `getActiveBackend` missing from a `vi.mock`, nothing to do with this diff; `security.test.tsx` itself (31/31, up from 25) does not appear in the failure list) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (vite build itself succeeds; only the `tsc -p tsconfig.lib.json` declaration-emit step fails, on the same `vendor/understand-anything/**` TS6059 rootDir errors every hourly/focus-fixer run this week has documented — this diff touches neither `vendor/` nor codegraph)
- ci: fly-deploy.yml (the production Fly deploy this playbook gates a revert on) **green** for both `058317a` and the bookkeeping follow-up `2fc9c06` — no revert needed. `docker.yml`/`release.yml` failed on `2fc9c06`, matching the identical pre-existing failure shape every commit this week has shown (including the immediately preceding `edfea24`), not caused by this diff. `ci.yml` was still `in_progress` after ~10 minutes of best-effort polling (step 8's polling window); given its `Test` step has failed with the same documented pre-existing shape on every commit this week without exception, and the local full-suite gate above already reconfirmed this diff's own test files are unaffected, treating it as the same known gap rather than continuing to poll past the window.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. Preflight hit the same shallow-clone-boundary artifact prior runs this week have documented: `git pull --ff-only` failed non-fast-forward with local `main` pointing at a stale pre-force-update ref (`git rev-parse --is-shallow-repository` → `true`); working tree was clean (nothing at risk). Realigned via `git checkout -B main origin/main` before starting, landing cleanly on `edfea24`.

## 04:40 UTC — settings — FIXED
- mode: section
- todo: -
- previous run: security (03:41 UTC, this file)
- picked because: `TODO.txt`/`findings.txt` had no live items. Step 0 checked `incidents.md` first: INC-1 and INC-4 are already MITIGATED with real fixes recorded; INC-3's `mitigation: none` is explicitly out-of-scope (infra/deploy-config decision touching `.github/workflows`, not buildable by this routine) — did a cheap liveness check anyway (`GET https://neo-agent-server.fly.dev/server_info` → `uptime: 1124`s), still OPEN, no change, recorded in its own small commit before the section work. In section mode, `security` (the most recent run that did real work) was excluded from the candidate list; no section had been worked twice-plus in the last 6 working runs (security, auth-data-platform, files-terminal-browser, skills-plugins-mcp, conversation-chat, kt-video — no repeats), so the remaining candidate with the oldest `lastRun` (`settings`, 2026-09-19T15:44:39Z) was picked.
- brainstorm: -
- base: 21108548dc16b9117a256c14611cc67a7f359a37 (commit `3f6d82e9` is the standalone incidents.md liveness-check bookkeeping commit made before this fix)  commit: fcbb71e15c2b613ecafeaa64deba8fba7d5542e0
- changes:
  - Delegated the bug hunt for `src/components/features/settings/**` + `src/routes/*settings*.tsx` (and their tests) to a research subagent, explicitly told to skip everything already fixed in prior runs this week (`openai-subscription-auth-card.tsx` poll loop, `sdk-section-page.tsx` hydration/deps effects, `agent-profiles-local-view.tsx` unrecognized-`acp_server` demotion) and to skip `mcp-server-form.tsx`/`secret-form.tsx`, which I had already read end-to-end myself and found correct (including confirming the global `MutationCache` `onError` handler in `src/query-client-config.ts` already covers mutation-failure toasts, so a missing-error-toast candidate I'd initially flagged in `secret-form.tsx` was a false positive, not a real gap). It found 1 high-confidence bug (fixed) and a related medium-confidence one (not fixed this run, see notes).
  - **Bug (data loss, silent): renaming an LLM profile via the embedded Settings → LLM Profiles edit form silently desynced `settings.title_llm_profile`.** `src/components/features/settings/llm-profiles/llm-settings-local-view.tsx`'s `handleSave` called `ProfilesService.renameProfile(originalName, trimmedName)` directly on the raw API client instead of going through the `useRenameLlmProfile()` mutation hook (`src/hooks/mutation/use-rename-llm-profile.ts`). That hook's `onSuccess` is the *only* place that checks whether `settings.title_llm_profile` was pointing at the renamed profile and re-points it at the new name (the same pattern `useDeleteLlmProfile` uses on delete, and the pattern the sibling Agent-profile embedded editor, `agent-profiles-local-view.tsx`, already uses correctly via `useRenameAgentProfile().mutateAsync`). Concretely: a user sets a profile as their "title LLM profile" in App settings, then renames that same profile from the LLM Profiles edit screen — the rename succeeds server-side, but `title_llm_profile` keeps the stale name, so `resolveTitleLlmProfile()` silently falls back to the active profile (or nothing) with no error or toast telling the user their explicit choice was dropped; it stays broken until they manually re-pick it. Fixed by adding `const renameProfile = useRenameLlmProfile();` and calling `renameProfile.mutateAsync({ name: originalName, newName: trimmedName })` in place of the raw service call (plus adding `renameProfile` to `handleSave`'s `useCallback` deps); `ProfilesService` stays imported for its still-used `getProfile` call. Added a regression assertion to the existing "renames profile before saving when name changes" test (mocked `useRenameLlmProfile` the same way the file already mocks `useSaveLlmProfile`/`useActivateLlmProfile`, wired to call through to the (already-mocked) `ProfilesService.renameProfile` so the existing assertions on that call keep working) asserting `renameProfile.mutateAsync` itself was called with `{ name, newName }` — verified it fails against the pre-fix source (via a temporary `git stash` of just the source file) and passes with the fix; the rest of that test file's 24 tests were unaffected.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
  - Not fixed this run (documented for a future run instead of rushed): the same subagent flagged a second, lower-confidence issue in the same `handleSave` (and the analogous `agent-profiles-local-view.tsx`) — the rename-then-save two-step isn't atomic, so if the *save* call fails right after a successful *rename*, `editingProfile`'s stale `originalName` makes an immediate retry from the same screen try to rename an already-renamed (now nonexistent) profile and fail again, recoverable only by backing out to the list and re-entering. Requires a mid-flow API failure to trigger (not deterministic like the fixed bug), and a real fix (e.g. updating `editingProfile`'s tracked name after a successful rename, before the save call) touches the same `handleSave` flow in both files — worth its own careful pass rather than bundling in under this run's budget.
- gate: lint pass (typecheck + eslint + prettier all clean) · test: full suite 27 failed files / 139 failed tests / 6193 passed / 5 skipped / 7 todo (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline exactly; independently confirmed neither touched file, nor any file under `llm-profiles/`, appears anywhere in the failure list) · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (72 errors, all confirmed confined to `vendor/understand-anything/**` TS6059 rootDir, same known gap logged in every hourly/focus-fixer run this week, unrelated to this diff)
- ci: `fly-deploy.yml` (the production Fly deploy this playbook gates a revert on) — **success** for `fcbb71e1`. No revert needed. `docker.yml`/`release.yml` failed on `fcbb71e1`, matching the identical pre-existing failure shape seen on the immediately preceding commit `3f6d82e9` and every commit this week, not caused by this diff. `ci.yml` was still `in_progress` after ~10 minutes of best-effort polling (step 8's polling window); given its `Test` step has failed with the same documented pre-existing shape on every commit this week without exception, and the local full-suite gate above already reconfirmed this diff's own test files are unaffected, treating it as the same known gap rather than continuing to poll past the window. `gh` unavailable in this sandbox; polled via the GitHub MCP Actions tools instead.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. Preflight hit the same shallow-clone-boundary artifact prior runs this week have documented: local `main` and `origin/main` showed as fully diverged (50/50 commits, no merge-base found within the shallow window). Verified the "stale snapshot, not a rewrite" theory conclusively before touching anything, per prior runs' precedent: `git rev-parse --is-shallow-repository` → `true`; `git fetch origin main --unshallow` then `git merge-base --is-ancestor <old local main> origin/main` → true, confirming local `main` genuinely was an ancestor of `origin/main`'s real history, just outside the shallow fetch window — no work lost. Realigned via `git reset --hard origin/main` (working tree was already clean) before starting.

## 05:37 UTC — knowledge-kt — IMPROVED
- mode: section
- todo: -
- previous run: settings (04:40 UTC, this file)
- picked because: `TODO.txt`/`findings.txt` had no live items. Step 0 checked `incidents.md` first: INC-1 and INC-4 are already `MITIGATED` with substantive fixes recorded (nothing new buildable this run); INC-3's `mitigation: none` is explicitly out-of-scope (infra/deploy-config decision touching `.github/workflows`) — no cheap liveness re-check performed this run since a working `curl`/network probe wasn't attempted and the row was last confirmed today already. `findings.txt` had no `MITIGATION for INC-<n>:` item to defer. In section mode, built the last-10-working-runs window from today + yesterday's logs: `settings` (the most recent to do real work) was excluded, and no section appeared twice-plus in the last 6 working runs (conversation-chat, skills-plugins-mcp, files-terminal-browser, auth-data-platform, security, settings — no repeats), so of the remaining 11 candidates the one with the oldest `lastRun` was picked: `knowledge-kt` at `2026-09-19T16:43:00Z`, no tie.
- brainstorm: -
- base: 5e4c7eaa7a99dd7b675ac913906ef9b7c17ca99f
- changes:
  - Read every file under `src/lib/knowledge/`, `src/components/features/knowledge/`, and the `src/routes/kt-{list,page,repository,graph}.tsx` routes end-to-end (this section has runCount 6 already and every file is densely commented with prior fixer runs' own regression notes — no new real defect found in this pass: race handling, cold-rehydration fallbacks, stale-closure guards, and error-state preservation all checked out correct on inspection and against the existing 91-test suite, which stayed green). Stepped to step 4's no-bugs-found path: three real improvements.
  - **Accessibility**: `src/routes/kt-list.tsx`'s repository search `<input>` relied on `placeholder` alone for its accessible name, unlike every other search input in the app (`automations/search-input.tsx`, `skills-toolbar.tsx`, `codegraph-toolbar.tsx`, `environment-connections.tsx`, etc.), which all pair `placeholder` with `aria-label` using the same translation key. Added `aria-label={t(I18nKey.KT$SEARCH_PLACEHOLDER)}` to match that established convention — no new i18n key needed, reuses the existing one.
  - **Test coverage**: `src/lib/knowledge/connected-repositories.ts` (`useConnectedRepositories`, `resolveCommitSha`) and `src/lib/knowledge/pre-analysis.ts` (`ensureCodeEvidence`, `upgradeCodeEvidenceInBackground`) were the only two files in `src/lib/knowledge/` with zero test coverage — every sibling file already has a `__tests__/lib/knowledge/*.test.ts`. Added `__tests__/lib/knowledge/connected-repositories.test.ts` (10 tests: dedup-by-repositoryId, blank-`selected_branch`→`main` default, blank-`working_dir`→`null` normalization, multi-page flattening, the `isLoading` gate, and `resolveCommitSha`'s poll-until-commit / timeout behavior via fake timers) and `__tests__/lib/knowledge/pre-analysis.test.ts` (6 tests: existing-analysis reuse, fallback to a fresh bounded `runAnalysis` pass, both failure paths resolving to `null` instead of throwing, and the fire-and-forget background upgrade not throwing on a rejected re-analysis).
  - No new user-facing strings (the accessibility fix reuses `KT$SEARCH_PLACEHOLDER`), so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, one `tsc` spread-argument type error self-fixed in the new `pre-analysis.test.ts` mock before it ever reached full `npm test`) · test: full suite 28 failed files / 140 failed tests / 6207 passed / 5 skipped / 7 todo (1 additional flaky-under-parallel failure, `debug-automation-button.test.tsx`, reproduced 3/3 green standalone — matches this week's documented ~27-30-file/~139-147-test pre-existing baseline; independently reconfirmed via `git stash` + isolated re-run of `sidebar.test.tsx` and `legacy/src/lib/smeAgent.test.ts` against unmodified `BASE_SHA`, identical failures); none of the 3 touched/new files appear in the failure list, and the full knowledge-kt-relevant suite (15 files, 106 tests, including the 16 new ones) passes cleanly standalone · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (same `vendor/understand-anything/**` TS6059 rootDir errors documented in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 35492190222, the production Fly deploy this playbook actually gates a revert on) — completed **success** for `e9baea3`. No revert needed. `ci.yml` (run 35492190174) was still `in_progress` after ~10 minutes of best-effort polling (step 8's window); `gh` unavailable in this sandbox, polled directly via the GitHub REST API instead. Given `ci.yml`'s `Test` step has failed with the same documented pre-existing shape (27-30 failed files / 139-147 failed tests, unrelated to any given diff) on every commit this week without exception, and the local full-suite gate above already reconfirmed this diff's own files are unaffected, treating a same-shape failure here as the same known gap rather than continuing to poll past the window.
- notes: Ran `npm ci` at the start of this run since `node_modules` was absent in the fresh checkout. Preflight hit the same shallow-clone-boundary artifact prior runs this week have documented: `git checkout main && git pull --ff-only` failed non-fast-forward, with local `main` and `origin/main` showing as fully diverged (50/50 commits, no merge-base found — `git merge-base main origin/main` returned nothing). Working tree was clean (`git status --porcelain` empty), so no work was at risk; realigned via `git reset --hard origin/main` before starting, matching the "stale pre-fetch snapshot, not an actual history rewrite" explanation prior runs this week have already verified and documented for this exact symptom.
## 06:34 UTC — agentops — FIXED
- mode: section
- todo: -
- previous run: knowledge-kt (05:37 UTC, this file)
- picked because: `TODO.txt` had no live items; `findings.txt` had no `MITIGATION for INC-<n>:` item to defer. Step 0 incident check: INC-1 and INC-4 are `MITIGATED` already (nothing new buildable this run); INC-3 is `OPEN` with `mitigation: none` explicitly out-of-scope (touches `.github/workflows`, standing rule) — did the cheap liveness check anyway (`GET https://neo-agent-server.fly.dev/server_info`, `uptime: 1453`s, unchanged pattern), updated `last confirmed` and pushed that alone first (see notes). In section mode, built the last-6-working-runs window from today's log (knowledge-kt, settings, security, auth-data-platform, files-terminal-browser, skills-plugins-mcp — no section repeated), so the only exclusion was the just-worked `knowledge-kt`; of the remaining 11 candidates the oldest `lastRun` was `agentops` (`2026-09-19T17:53:00Z`), no tie.
- brainstorm: -
- base: 65ca79f (after the separate INC-3 liveness-check commit; section BASE_SHA before that was 296948d)
- changes:
  - Delegated a read-only bug hunt across the full `agentops` surface (scripts/agentops/*.mjs, the React Query hooks, all route/component files, and their tests) to a subagent, since this section has been through 6+ prior hourly-fixer passes and is unusually well-hardened/densely commented with prior-bug context. It found one genuine, currently-reachable bug (below the worst-first cap of three — no others cleared medium confidence) and I independently read the flagged file to confirm before fixing.
  - **Bug fix**: `src/lib/activity/agentops-activity.ts`'s `FEED_ACTIONS` lookup mapped `"run.cancel"` (the user-initiated Stop audit action from `run-control.mjs`) to a feed entry, but not `"run.cancelled"` (the past-tense action `collector.mjs`'s `#closeOrphanedRuns` writes when a run's conversation was deleted out from under the collector) — despite the file's own comment already describing the intended user/system split and claiming both existed. `toWorkspaceActivityEvent` returns `null` for any unmapped action, so an auto-closed/orphaned run's halt silently never reached the Workspace Activity feed (it still recorded correctly in the raw audit trail/Audit tab, which reads a different, unfiltered list). Added `"run.cancelled": "info"` alongside the existing `"run.cancel"`, matching the icon map in `audit-list.tsx` which already had both. Added a regression test mirroring the existing `run.pause` vs `run.paused` test.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test: full suite 27 failed files / 139 failed tests / 6209 passed / 5 skipped / 7 todo out of 6360 (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline exactly — `root.test.tsx`, `agent-server-adapter.test.ts`, `sidebar.test.tsx`, `legacy/src/lib/smeAgent.test.ts`, `add-backend-modal.test.tsx`, etc., none of them agentops- or activity-scoped); neither touched file appears anywhere in the failure list, and the touched test file (3/3, including the new regression test) passes cleanly standalone · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (exactly 72 `error TS6059` lines, all inside `vendor/understand-anything/**` rootDir errors, zero outside vendor — same documented gap logged in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 35494636265, the production Fly deploy this playbook actually gates a revert on) — completed **success** for `2103f14`. No revert needed. `ci.yml` (run 35494636166) was still `in_progress` after ~10 minutes of best-effort polling via GitHub MCP tools (`gh` unavailable in this sandbox); given `ci.yml`'s `Test` step has failed with the same documented pre-existing shape (27-30 failed files / 139-147 failed tests, unrelated to any given diff) on every commit this week without exception, and the local full-suite gate above already reconfirmed this diff's own files are unaffected, treating a same-shape failure here as the same known gap rather than continuing to poll past the window. `docker.yml` and `release.yml` both failed on this commit and on the immediately preceding `65ca79f` (INC-3-only) commit alike — neither is a revert trigger under this playbook and the failure predates this diff.
- notes: Preflight hit the same shallow-clone-boundary artifact documented in prior runs this week: `git checkout main && git pull --ff-only` failed non-fast-forward, with local `main` and `origin/main` fully diverged (50/50 commits, no merge-base). Working tree was clean, so realigned via `git reset --hard origin/main` before starting, matching prior runs' verified explanation for this exact symptom. The INC-3 liveness-check edit was pushed separately as `65ca79f` before section work began, once a stop-hook flagged it as uncommitted work mid-investigation (same handling as the 2026-09-19 05:37 UTC run's note on this).

## 07:40 UTC — automations — FIXED
- mode: section
- todo: -
- previous run: agentops (06:34 UTC, this file)
- picked because: `TODO.txt` had no live item (only header comments). Step 0 incident check: `incidents.md` — INC-1 and INC-4 already `MITIGATED` with substantive mitigation text (nothing new buildable this run); INC-3 is `OPEN` with `mitigation: none` explicitly out-of-scope (infra/deploy-config decision touching `.github/workflows`, standing rule) — did the cheap liveness check anyway (`GET https://neo-agent-server.fly.dev/server_info` → `uptime: 1508`s, ~25.1 min, consistent with the known deploy-restart pattern), bumped `last confirmed`, no status change. `findings.txt` had no `MITIGATION for INC-<n>:` item to defer. Fell through to section mode (2b): built the last-6-working-runs window from today's log (agentops, knowledge-kt, settings, security, auth-data-platform, files-terminal-browser — no repeats), so the only exclusion was the just-worked `agentops` (also `lastSection`); of the remaining 11 candidates the oldest `lastRun` was `automations` (`2026-09-19T19:34:57Z`), no tie.
- brainstorm: -
- base: 943c3801da4f57e4046f06f0603f41ba378e1a04  commit: (see push below)
- changes:
  - Delegated a read-only bug hunt across the full automations surface (`src/components/features/automations/**`, `src/components/features/home/featured-automations/**`, `src/api/automation-service/**`, the automation routes, and every automation React Query hook, plus their tests) to a subagent, explicitly told to skip the six bug classes already fixed by prior runs this week (epoch `last_triggered_at` display, overview-tile loading flash, run-all partial-failure toasts, Proactivation card epoch `lastRunAt`, rate-limit Activity Log badge, `run.cancelled` feed mapping). It found one high-confidence, currently-reachable bug and one low-confidence latent (not-currently-reachable) gap; I independently read and confirmed the high-confidence one before fixing.
  - **Bug: a terminal automation run that never started execution silently corrupted the "average duration" stat.** `summarizeAutomationRuns` (`src/manifests/automation-insights.ts`) computed each terminal run's duration as `completed_at − started_at` guarded only by `Number.isFinite(ms) && ms >= 0`, with no epoch-placeholder check on `started_at`. The automation service leaves `started_at` at the epoch placeholder (`"1970-01-01T00:00:00Z"`, `getTime() === 0`) for a run that reached a terminal state (e.g. `FAILED`) without ever starting execution — the same convention three other files in this codebase already guard against (`activity-log-item.tsx`'s `isInvalidTimestamp`, `automation-run-health.ts`'s `getLastRunTimestamp`, `automation-run-insights.tsx`'s `isValidTimestamp`), but this one call site didn't. Since `new Date(epoch).getTime()` is `0` (not `NaN`), `ms = completed_at_ms − 0` is a real, positive, finite ~56-year millisecond value that passed the existing guard and was averaged in — corrupting `averageDurationMs` for that automation (and, via `computeOverviewTile("average-duration", ...)`, the dashboard's fleet-wide "Average duration" tile too) to read as hundreds of thousands of hours off a single early-provisioning-failure run. Fixed by adding a local `isValidRunTimestamp` type-guard (mirroring, not sharing, the same pattern already duplicated per-call-site elsewhere in this codebase) and requiring both `started_at` and `completed_at` to pass it before computing `ms`.
  - Added a regression test to `__tests__/manifests/automation-insights.test.ts`'s `summarizeAutomationRuns` block: a `FAILED` run with epoch `started_at` and a real `completed_at` alongside a normal completed run, asserting `averageDurationMs` reflects only the real run's 90s duration. Verified it fails against the pre-fix source (via `git stash` of just the source file — failed with `883785660000` instead of `90000`, i.e. exactly the ~56-year corruption described above) and passes with the fix; all other 28 tests in the file are unaffected.
  - The subagent's second (low-confidence) finding — `AutomationDetail`'s delete/edit modal `useState` not being reset across an `:automationId` route-param change, unlike the sibling `ActivityLogSection` which is explicitly `key`-remounted for exactly this reason — was investigated but not fixed: no current in-app navigation path goes from one `/automations/:id` page directly to a different one without an intervening unmount (every link to automation detail comes from a different route), so it's a defensive/latent gap rather than a live bug; noted here rather than bundled into a speculative fix.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test: full suite 27 failed files / 139 failed tests / 6210 passed / 5 skipped / 7 todo out of 6361 (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline exactly — `legacy/src/lib/smeAgent.test.ts`, `sidebar.test.tsx`/`sidebar-email-verification.test.tsx`, etc., none of them automations-scoped); the touched file does not appear in the failure list, and the touched test file (29/29, including the new regression test) passes cleanly standalone · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (16 `error TS6059` lines, all confined to `vendor/understand-anything/**` rootDir errors, zero outside vendor — same documented gap logged in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 35497598627, the production Fly deploy this playbook actually gates a revert on) — completed **success** for `489e1f5`. No revert needed. `docker.yml` (run 35497598646) and `release.yml` (run 35497598962) both failed on this commit, matching the identical pre-existing failure shape on the immediately preceding commit `943c380` (and every commit this week) — not a revert signal. `ci.yml` (run 35497598625) was still `in_progress` after ~7 minutes of best-effort polling via GitHub MCP Actions tools (`gh` unavailable in this sandbox); given its `Test` step has failed with the same documented pre-existing shape (27-30 failed files / 139-147 failed tests, unrelated to any given diff) on every commit this week without exception, and the local full-suite gate above already reconfirmed this diff's own file is unaffected, treating a same-shape failure here as the same known gap rather than continuing to poll past the window.
- notes: Fresh checkout's local `main` was left at a stale commit (`37149a0`, dated 2026-09-16) from the container image/shallow-clone snapshot, while `origin/main` had since advanced to `943c380` (dated 2026-09-20, from other cloud-routine runs today) — `git checkout main && git pull --ff-only` failed non-fast-forward ("diverged, 50 and 50 different commits each") because `git rev-parse --is-shallow-repository` confirmed this is a shallow clone and `git merge-base` came back empty at the original depth. Working tree was clean (`git status --porcelain` empty) and `origin/main`'s tip was strictly newer by commit date, so resolved non-destructively via `git reset --hard origin/main` — no local work was at risk. Ran `npm ci` first since the fresh sandbox had no `node_modules`. Step 0 liveness check on INC-3 detailed above.

## 08:35 UTC — environment-onboarding — FIXED
- mode: section
- todo: -
- previous run: automations (07:40 UTC)
- picked because: `TODO.txt` had no live items (only header comments). Step 0: `incidents.md` — INC-1 and INC-4 already `MITIGATED`; INC-3 is the only `OPEN` row and its `mitigation:` field explicitly names the fix out-of-scope (`.github/workflows`/infra decision, standing rule) — did the cheap liveness check instead (`GET https://neo-agent-server.fly.dev/server_info` → `uptime: 1105`s, same recurring-restart pattern, no change) and folded the `last confirmed` bump into this commit. No `findings.txt` `MITIGATION for INC-<n>:` item to defer. Fell through to section mode (2b): `automations` (most recent to do real work) excluded; of the last 6 working runs (automations, agentops, knowledge-kt, settings, security, auth-data-platform) nothing repeated. Of the 11 remaining candidates, `environment-onboarding` had the single oldest `lastRun` (2026-09-19T20:33:00Z).
- brainstorm: -
- base: 564ed898c28bf6b0fab12b4fed64e5e57a19990c  commit: -
- changes:
  - Delegated a read-only bug hunt across `src/components/features/environment/**`, `src/components/features/onboarding/**`, `src/routes/environment-*.tsx`, the environment/onboarding query hooks and stores, and `src/services/onboarding-control.ts` (cross-checked against the `__tests__` mirror) to a subagent, seeded with the list of bugs prior runs this week already fixed in this same section so it wouldn't re-report them. Verified both of its candidates myself by reading the cited code before fixing. Fixed both (worst first):
  - **1 (worst): a "Fix with agent" request silently vanished whenever an onboarding session was already active.** `environment-overview.tsx`/`environment-requirements.tsx`'s "Fix with agent" buttons seed `useOnboardingCopilotStore`; the dock's Launch button (`onboarding-dock.tsx`) consumes that seed and navigates to `/environment/setup?seed=<text>`. Inside `EnvironmentSetupScreen` (`src/routes/environment-setup.tsx`), the `seed` query param was read only by `handleStart` -- the callback bound to the "start a new session" screen's button, rendered exclusively when `conversationId` is still null. Once a session is already active (the exact state a user browsing Overview/Requirements to find something to fix is realistically in), the component renders `SetupStudioShell` directly and `seed` was never read anywhere else -- no toast, no fallback, the user's "Fix with agent" click just had no effect and the agent never learned what they wanted. Fixed by adding an effect that fires when `conversationId` is already non-null and an unconsumed `?seed=` is present: it posts the seed text as a real follow-up user message into the running conversation via the same `createConversationResultPoster` already used a few lines up for OAuth receipts, then strips `?seed=` from the URL so a refresh/remount can't resend it. Guarded with a ref (also set inside `handleStart` itself) so the fresh-conversation path, where the seed is already consumed as the new conversation's opening message, is never double-posted. Added two regression tests to `__tests__/routes/environment-setup.test.tsx`: an active session with `?seed=` posts exactly that text into the conversation; no active session yet leaves the seed alone (so `handleStart` can still consume it as the opening message).
  - **2: an Environment Overview capability tile could show a completely different connector's logo/name than the status pip right next to it.** `environment-overview.tsx`'s `providerByCapability` memo picked the tile's displayed provider via `connections?.find((c) => c.capability === capability)`, with no `instanceKey` filter -- but the product explicitly supports more than one connection per capability (e.g. a second GitHub Enterprise instance alongside github.com; `onboarding-control.ts`'s `request_credentials`/`open_connection_form` already accept an `instance_key`), and the connections query (`connections-repository.ts`) has no secondary ordering, so which same-capability row comes back first is arbitrary. Meanwhile the tile's status pip is driven separately by `useEnvironmentReadiness`, which (via last week's already-fixed bug in this same file) correctly filters to `instanceKey === "default"`. Net effect: with two same-capability connections, the tile could show the "default" instance's readiness status next to a different, non-default instance's logo and name -- a visible identity/status mismatch. Fixed by requiring `instanceKey === "default"` in the same `find`, matching the pattern `environment-connections.tsx`'s `connectionFor` and `use-environment-readiness.ts`'s `connectionStatus` already use. Added a regression test to `__tests__/components/environment/environment-overview.test.tsx`: two `source-control` connections (a non-default `github-enterprise` and a default `github`) render the default instance's name on the tile, not the other one's.
  - No new user-facing strings, so no `public/locales/*/openhands.json` changes needed.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test: full suite 28 failed files / 140 failed tests / 6212 passed / 5 skipped / 7 todo out of 6364 (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline exactly -- `sidebar.test.tsx`/`sidebar-email-verification.test.tsx`, `new-conversation.test.tsx`, `legacy/src/lib/smeAgent.test.ts`, etc., none of them environment/onboarding-scoped); neither touched source file appears in the failure list, and both touched test files (14/14, including the 3 new regression tests) pass cleanly standalone · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (every `error TS6059` line reconfirmed inside `vendor/understand-anything/**` rootDir errors, same documented gap logged in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: <filled in after push, see amended notes below>
- notes: `npm ci` was run at the start of this run since `node_modules` was absent. Preflight hit the same shallow-clone-boundary artifact prior runs this week documented: the container's local `main` was frozen 50 commits deep at a stale, unrelated-history commit (`37149a0`) while `origin/main` had since advanced to `564ed89`, no common ancestor visible at this shallow depth (`git rev-parse --is-shallow-repository` = true) -- `git checkout main && git pull --ff-only` refused as non-fast-forward. Working tree was clean, so resolved non-destructively via `git reset --hard origin/main`. Step 0 liveness check on INC-3 detailed above (folded into this commit).

## 09:38 UTC — codegraph — IMPROVED
- mode: section
- todo: -
- previous run: environment-onboarding (08:35 UTC)
- picked because: `TODO.txt` had no live item (only header comments); `findings.txt` had no `MITIGATION for INC-<n>:` item. Step 0 incident check: `incidents.md` has no `OPEN` row with a buildable `mitigation: none` in scope — INC-1/INC-4 already `MITIGATED` with substantive text and open `needs from user`; INC-3's `mitigation: none` is explicitly an out-of-scope `.github/workflows`/infra decision (unchanged, not re-verified this run since it wasn't the trigger for any action). Fell through to section mode: the last 9 runs today were skills-plugins-mcp, files-terminal-browser, auth-data-platform, security, settings, knowledge-kt, agentops, automations, environment-onboarding — `environment-onboarding` (most recent to do work) excluded; of the last 6 working runs (security, settings, knowledge-kt, agentops, automations, environment-onboarding) none repeated, so no "twice in 6" exclusion applied beyond the immediate-previous one. Of the 11 remaining candidates, `codegraph` had the single oldest `lastRun` (2026-09-19T21:39:00Z).
- brainstorm: -
- base: 355f46d1930b56f3afe50a88c09343e1edfa8a98  commit: (recorded below)
- changes:
  - Read every file under `src/lib/codegraph/` and `src/components/features/codegraph/` (hierarchy, staleness, shard-name, workspace-identity, deepwiki-bridge, activity, analyzer-entry, analyzer-runner, canvas/toolbar/node-details components) plus `src/stores/codegraph-store.ts`, cross-checked against their `__tests__` mirrors and this week's prior run logs for this section (6 previous passes already fixed 2 real bugs and hardened most of the module — level-1 subsystem cap, `kt-graph.tsx`'s duplicate-lookup race, plus several coverage/accessibility passes). Found no new reproducible defect within this run's scope — all 145 pre-existing section tests passed cleanly before any change, and nothing in the hunt (conditionals, closures, cleanup, races, cache keys) reproduced.
  - No bugs found, so per step 4 used the "real test coverage" category. Ran section-scoped coverage first to find where the real gap actually was rather than padding an already-thin module: `analyzer-runner.ts` sat at 48.75%/47.26% stmt/line — by far the largest gap in the section, and it covers the module's single most consequential function, `runAnalysis` (the whole upload-sandbox/run-analyzer/read-shards/mirror-to-storage pipeline), which had **zero** test coverage despite being the exact code path that decides whether a repository's graph gets built at all. Added to `__tests__/lib/codegraph/analyzer-runner.test.ts`:
  - `runAnalysis` end-to-end: a full successful run (asserts the returned handle's `meta`/`root`, that all 3 manifest files actually get uploaded via `fileUpload`, that hints get uploaded via `uploadText`, and that `onProgress` fires with the exact parsed milestone sequence) plus every one of its distinct failure branches — sandbox has no usable node (rejects before any upload happens), the analyzer payload was never built (`manifest.json` 404), one of the manifest's own assets 404s mid-upload, the analyzer exits non-zero (surfaces its own `failed` milestone reason), and the analyzer exits clean but leaves no readable `meta.json`/`root.json` — plus a Storage-mirror-success-optional case confirming a failed mirror (`find` command failing) does not fail the run, since `uploadArtifactsToStorage` is explicitly documented as best-effort.
  - The `AnalysisHandle` closures returned by both `makeHandle` (sandbox-backed) and `makeStorageHandle` (Storage-backed) were previously asserted only on their `.meta`/`.root` fields — `loadLevel`, `loadSearchIndex`, and `readSource` (the three functions the UI actually calls on every drill-down, search, and "Open source" click) were never invoked in a test at all. Added coverage for both handle variants: `loadLevel` reads the correct `shardName`-encoded absolute path, `loadSearchIndex` round-trips real tuples through `mapSearchEntries`, and `readSource` returns real content on success and `null` (not a throw) on failure — including confirming the Storage-backed handle still reads raw source from a live sandbox rather than Storage, matching `readSource`'s own doc comment ("Raw source is never mirrored to Storage").
  - Extended the shared `RemoteWorkspace` mock in the test file with `fileUpload`/`uploadText` (previously only `executeCommand`/`downloadAsText` were mocked, which is why the upload path had no coverage at all) and added a `CodeGraphAnalyzerError` unit test locking in its `stage`/`message`/`name`/`Error`-subclass contract, since every new failure-path assertion depends on that shape.
  - `analyzer-runner.ts` moved from 48.75%/61.61%/48.27%/47.26% to 91.87%/84.84%/89.65%/92.46% (stmt/branch/func/line); the section overall moved from 75.21%/70.84% to 82.48%/74.95% (stmt/branch). Remaining gaps are `analyzer-entry.ts` (0% — the sandbox-only CLI script every prior run this week has documented as not meaningfully unit-testable without a heavy `node:fs` harness) and a handful of trivial one-line JSX branches in the three components.
  - No new user-facing strings (test-only change), so no `public/locales/*/openhands.json` edits needed.
- gate: lint pass (typecheck + eslint + prettier, all clean — the new test file needed one `prettier --write` pass for line-wrapping before `eslint`/`prettier --check` were clean, no logic changes) · test: full suite 28 failed files / 140 failed tests / 6222 passed / 5 skipped / 7 todo out of 6374 (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline exactly — `sidebar.test.tsx`/`sidebar-email-verification.test.tsx`, `new-conversation.test.tsx`, `legacy/src/lib/smeAgent.test.ts`, `use-search-providers.test.tsx`, etc., grepped explicitly for "codegraph" in the failure list and found none); the full 155-test codegraph suite (13 files, up from 145) passes cleanly, including the 36 touched/new analyzer-runner tests · build pass (reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct before committing, per established convention) · build:lib pre-existing-fail (exactly 72 `error TS6059` lines, all confined to `vendor/understand-anything/**` rootDir errors, zero outside vendor — same documented gap logged in every hourly/focus-fixer run this week; this diff touches only a test file)
- ci: `fly-deploy.yml` (run 35502903086) — the production Fly deploy this playbook actually gates a revert on — completed **success** for `0e8118f`. No revert needed. `release.yml` (run 35502903399) failed, matching the identical pre-existing failure shape on the immediately preceding commit `355f46d` (and every commit this week) — not a revert signal. `docker.yml` (run 35502903026) and `ci.yml` (run 35502903055) were still `in_progress` after ~6 minutes of polling via the GitHub MCP Actions tools (`gh` unavailable in this sandbox); given the one check that actually gates a revert is already confirmed green, and this diff's own local gate above already reconfirmed it lands on the same documented pre-existing baseline every commit this week has shown regardless of diff content, moved on per step 8's best-effort allowance rather than continuing to poll to completion.
- notes: Fresh checkout's local `main` was frozen at a stale, unrelated-history commit (`37149a0`) while `origin/main` had since advanced to `355f46d` (50 commits, no common ancestor visible — `git rev-parse --is-shallow-repository` confirmed a shallow clone) — `git checkout main && git pull --ff-only` refused as non-fast-forward. Working tree was clean, so resolved non-destructively via `git checkout -B main origin/main` (no local work at risk), same documented shallow-clone artifact prior runs this week have hit. `npm ci` was run first since the sandbox had no `node_modules`. The full local test run took ~11.5 minutes (thousands of tests); waited on it synchronously rather than moving on, per step 6's "never push ungated work".

## 10:45 UTC — kt-video — IMPROVED
- mode: section
- todo: -
- previous run: codegraph (09:38 UTC, this file)
- picked because: `TODO.txt` had no live item (only header comments); `findings.txt` had no `MITIGATION for INC-<n>:` item to defer. Step 0 incident check: `incidents.md` has no `OPEN` row with a buildable `mitigation: none` in this run's scope — INC-1 and INC-4 already carry substantive `mitigation:` text; INC-3's `mitigation: none` explicitly names it an infra/deploy-config decision touching `.github/workflows`, out of scope for every routine by standing rule. Did the cheap liveness re-check anyway: `GET https://neo-agent-server.fly.dev/server_info` → `uptime: 1662`s (~27.7 min), consistent with the known deploy-restart pattern — bumped `last confirmed`, no status change. Fell through to section mode (2b): of today's 10 working runs, `codegraph` (most recent to do real work, also `lastSection`) was excluded; in the last 6 working runs (settings, knowledge-kt, agentops, automations, environment-onboarding, codegraph) no section repeated, so nothing else was excluded on the "twice in 6" rule. Of the remaining 11 candidates, `kt-video` had the single oldest `lastRun` (2026-09-19T22:38:59Z) — no tie-break needed.
- brainstorm: -
- base: cd64312b5fd007b0a108efee54ff632416a895c4
- changes:
  - Read every file in scope end to end (`src/lib/kt-video/{build-manifest,narrate-manifest,concept-flow,use-scene-narration}.ts` and `src/components/features/kt-video/{kt-video-composition,concept-panel,diagram-panel,repo-tree-panel,kt-breadcrumb,kt-refresh-cadence,provisioning-card}.tsx`) plus every existing test file, hunting for real defects per step 3. This section is already densely commented with prior-fixed-bug context (windowing/highlight-span math, narration play/pause/scrub edge cases, mermaid-render caching, provisioning step-index resolution on failure) and all 61 existing tests passed cleanly — no new reproducible bug found. Stepped to step 4's no-bugs-found path: three improvements, worst gap first.
  - **Real test coverage**: `kt-video-composition.tsx` — the actual Remotion composition every KT video renders through (scene-type routing to the right panel, the empty-manifest placeholder, and `getActiveLines`'s "3 lines above the highlight, clamped to the file's real bounds, capped at the visible window" logic the file's own comments call out as bug-prone) — was the only file in this section with zero test coverage; every sibling component already has one. Exported `getActiveLines` (previously module-private) and added `__tests__/components/features/kt-video/kt-video-composition.test.tsx` (10 tests): 4 pure-function cases for `getActiveLines` (normal window, clamped-to-line-1 start, clamped-to-real-line-count end, capped-at-maxLines), plus rendering tests for the empty-scenes placeholder, default CodePanel rendering for code/intro/recap scenes, correct routing to DiagramPanel/RepoTreePanel/ConceptPanel for their scene types, and the scene index/title badge.
  - **Accessibility**: `DiagramPanel`'s successfully-rendered Mermaid SVG (`dangerouslySetInnerHTML`) had zero accessible semantics — a screen-reader user got nothing where a sighted user sees the actual architecture/flow diagram (the failure state already had `role="alert"`, but success had nothing). Added `role="img"` + `aria-label={`${scene.title} diagram`}` to the SVG wrapper. Extended the existing "shows the rendered SVG" test in `diagram-panel.test.tsx` to assert the accessible name.
  - **Accessibility**: `RepoTreePanel`'s real file list rendered as plain unlabelled `<div>`s with no list semantics, so assistive tech had no way to know it was a list or how many items it contained. Added `role="list"` + `aria-label={`Repository files for ${scene.title}`}` to the container and `role="listitem"` to each row. Added a new test in `repo-tree-panel.test.tsx` asserting the labelled list role and item count.
  - No new user-facing strings (all changes are code/tests/ARIA attributes, no new copy), so no `public/locales/*/openhands.json` changes needed.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` `builtAt` rebuild byproduct from `npm run build` before committing, per established convention.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test: full suite 27 failed files / 139 failed tests / 6234 passed / 5 skipped / 7 todo out of 6385 (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline exactly — `agent-server-adapter.test.ts`, `sidebar.test.tsx`/`sidebar-email-verification.test.tsx`, `root.test.tsx`, `use-app-title.test.tsx`, `add-backend-modal.test.tsx`, `legacy/**`, etc., grepped explicitly for "kt-video" in the failure list and found none); the full kt-video suite (13 files, 72 tests, including the 11 new/modified ones) passes cleanly standalone · build pass (byproduct reverted, see above) · build:lib pre-existing-fail (exactly 72 `error TS6059` lines, all confined to `vendor/understand-anything/**` rootDir errors, zero outside vendor, vite build step itself succeeds — same documented gap logged in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 35506051928) — the production Fly deploy this playbook actually gates a revert on — completed **success** for `525ffd7`. No revert needed. `docker.yml` (run 35506051916) and `release.yml` (run 35506052540) both failed on this commit, matching the identical pre-existing failure shape on the immediately preceding commit `cd64312` (and every commit this week) — not a revert signal. `ci.yml` (run 35506051880) was still `in_progress` after ~10 minutes of best-effort polling via a background `Monitor` against the GitHub REST API (`gh` unavailable in this sandbox, `GITHUB_TOKEN` used directly); given its `Test` step has failed with the same documented pre-existing shape (27-30 failed files / 139-147 failed tests, unrelated to any given diff) on every commit this week without exception, and the local full-suite gate above already reconfirmed this diff's own files are unaffected, treating a same-shape failure here as the same known gap rather than continuing to poll past the window.
- notes: Fresh checkout's local `main` was left at a stale, unrelated-history commit (`37149a0`) while `origin/main` had since advanced to `cd64312` (50 commits, no common ancestor visible under `git merge-base` — `git rev-parse --is-shallow-repository` confirmed a shallow clone). `git checkout main && git pull --ff-only` refused as non-fast-forward. Working tree was clean, so resolved non-destructively via `git checkout -B main origin/main` (no local work at risk), the same documented shallow-clone artifact prior runs this week have hit. Ran `npm ci` first since the fresh sandbox had no `node_modules`.

## 11:41 UTC — conversation-chat — FIXED
- mode: section
- todo: -
- previous run: kt-video (10:45 UTC)
- picked because: `TODO.txt` had no live item (only header comments). Step 0 incident check: `incidents.md` has no `OPEN` row with a buildable `mitigation: none` in scope — INC-1 and INC-4 are `MITIGATED` with substantive mitigation text already in place; INC-3's `mitigation: none` explicitly names it an infra/deploy-config decision touching `.github/workflows`, out of scope for every routine by standing rule. Did a cheap liveness check on INC-3 anyway (`GET https://neo-agent-server.fly.dev/server_info`): uptime 2425s (~40 min), consistent with the known deploy-triggers-restart config — bumped `last confirmed` only, no status change. `findings.txt` had no `MITIGATION for INC-<n>:` item to defer. Fell through to section mode (2b): `kt-video` (most recent to do real work, also `lastSection`) excluded; of the last 6 working runs (knowledge-kt, agentops, automations, environment-onboarding, codegraph, kt-video) none repeated, so nothing else was excluded on the "twice in 6" rule. Of the 11 remaining candidates, `conversation-chat` had the single oldest `lastRun` (2026-09-19T23:29:59Z, the previous day — well before every other candidate's same-day timestamp) — no tie-break needed.
- base: 1df6c36b9cb1c7de35773f024fbe739cc4188984  commit: 24c1a07e6de696ff1b281bc7b350504efd8d80eb
- changes:
  - Delegated a read-only bug hunt across `src/components/features/chat/**`, `src/components/features/conversation/**`, `src/components/features/conversation-panel/**`, `src/components/conversation/**`, `src/components/conversation-events/**`, `src/hooks/chat/**`, and the conversation-scoped stores/query hooks to a research subagent, explicitly told to skip everything already fixed this week (error-message-banner.tsx isExpanded reset, cloud-new-conversation-menu.tsx pagination bail-out, conversation-name.tsx stale titleMode, the two skill-name-keyed-map bugs, use-chat-input-events.ts Enter-with-attachments). Independently re-read every finding's implementation, store, and test coverage myself before touching anything, then confirmed both by reverting just the source fix via `git stash` and watching the new regression tests fail with the exact described symptom, then restoring.
  - **Fixed** `src/components/features/conversation-panel/conversation-panel.tsx` (the pin-pruning effect, ~line 366): it pruned pins against `data?.pages` from `usePaginatedConversations()` (limit 20/page) as soon as `isFetched` went true — i.e. after only the *first* page resolved, not every page. A user with more than ~20 conversations who pinned an older one (not among the most-recently-updated 20) got that pin silently and permanently deleted from the persisted `pinned-conversations` localStorage store on the very next app load, even though the conversation still exists — they just hadn't loaded far enough into pagination yet. The very next line's own comment already carved out this exact failure mode for archived IDs ("pagination would otherwise drop archives that are not on the currently loaded pages") but the same protection was never applied to pins. Fixed by gating the prune on `!hasNextPage` (already destructured in this component) so it only prunes once every page has actually loaded.
  - Added two tests to `__tests__/components/features/conversation-panel/conversation-panel.test.tsx`: "does not unpin a conversation that only lives on an unloaded page" (pin on a partial first page with `hasNextPage` true → pin must survive) and "unpins a conversation once every page has loaded and it's still missing" (single fully-loaded page, pin genuinely absent → still gets pruned, so the fix doesn't just disable pruning outright). Confirmed the first fails against pre-fix code (empty pin array) and both pass with the fix.
  - **Fixed** `src/hooks/query/use-active-conversation.ts`: the effect syncing `ConversationService.setCurrentConversation()` had a dependency array of just `[conversationId, userConversation.isFetched, userConversation?.data?.execution_status]` while its body reads the *entire* `userConversation.data` object (conversation_url, session_api_key, workspace.working_dir, title, sandbox_status, …). This hook explicitly fast-polls every 3s specifically because those fields can populate on a later tick while `execution_status` stays constant (the sandbox-starting/PAUSED-resume window documented in the hook's own comment) — so a later poll with the real `session_api_key`/`conversation_url` never re-ran the effect, leaving the `ConversationService` singleton holding a stale null snapshot. Anything reading `ConversationService.currentConversation` in that window (`uploadFiles`, `getVSCodeUrl` in `conversation-service.api.ts`) would silently use the wrong/missing session key or working dir until `execution_status` happened to change for an unrelated reason. Fixed by depending on `userConversation.data` itself instead of the narrower field subset.
  - Added a test to `__tests__/hooks/query/use-active-conversation.test.ts` ("re-syncs ConversationService.currentConversation when data changes without execution_status changing") that rerenders the hook with a second `data` object (real `conversation_url`/`session_api_key`, same `execution_status`) and asserts `setCurrentConversation` is called with the updated values. Confirmed it fails against pre-fix code (stale first-call data) and passes with the fix.
  - No new user-facing strings (both are behavioral fixes only, no new copy) — no `public/locales/*/openhands.json` changes needed.
  - Bundled the pending INC-3 liveness-check timestamp bump in `incidents.md` (see step 0 above) into this same commit rather than a separate one.
  - Reverted the incidental `public/codegraph-analyzer/{analyze.mjs,manifest.json}` rebuild byproduct from `npm run build` before committing, per established convention.
- gate: lint pass (typecheck + eslint + prettier all clean, no repair needed) · test: full suite 28 failed files / 140 failed tests / 6236 passed / 5 skipped / 7 todo out of 6388 (matches this week's documented ~27-30-file/~139-147-test pre-existing baseline exactly — `legacy/src/lib/smeAgent.test.ts`, `sidebar.test.tsx`/`sidebar-email-verification.test.tsx`, `new-conversation.test.tsx`, etc.); both touched test files pass cleanly standalone (70/70, run both before and after the full-suite run) · build pass (byproduct reverted, see above) · build:lib pre-existing-fail (exactly 72 `error TS6059` lines, all confined to `vendor/understand-anything/**` rootDir errors, zero outside vendor — same documented gap logged in every hourly/focus-fixer run this week; this diff touches neither `vendor/` nor codegraph)
- ci: `fly-deploy.yml` (run 35508636590) — the production Fly deploy this playbook actually gates a revert on — completed **success** for `6cb5a44`. No revert needed. `release.yml` (run 35508636776) completed **failure** on `6cb5a44`, matching the identical pre-existing failure already recorded on the immediately preceding commit `1df6c36` (and every commit this week) — not a revert signal. `docker.yml` (run 35508636589) and `ci.yml` (run 35508636581) were both still `in_progress` a few minutes after push (checked via the GitHub MCP tools' `actions_get`/`actions_list`, `gh` unavailable in this sandbox, a `GITHUB_TOKEN`-authenticated poll loop via `Monitor` used for the `fly-deploy.yml` wait) — same "these run long and fail on this exact same pre-existing shape regardless of diff" pattern documented in every hourly/focus-fixer run log this week; the local full-suite gate above already reconfirmed this diff's own files are unaffected.
- notes: Fresh checkout's local `main` (`37149a0`) and `origin/main` (`1df6c36`) showed no common `git merge-base` — the same documented shallow-clone-boundary artifact prior runs this week have hit repeatedly. Working tree was clean, so resolved non-destructively via `git checkout -B main origin/main` (no local work at risk). `node_modules` was absent in this fresh checkout; ran `npm ci` first per this run's explicit instructions, completed cleanly. Size well within cap: 4 files changed (2 source, 2 test), ~123 lines.
