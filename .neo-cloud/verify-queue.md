# Fixes waiting for the focus-explorer to re-check on the live app

One line per pushed fix: `<short sha> — <flow to re-test>`.

---

8afc895 — with GitHub disconnected (INC-1 still open on the OAuth side), open the repo picker in Proactivation/Automations/home/chat's "open repository" modal and confirm it shows "GitHub isn't connected — reconnect in Environment > Connections" instead of a blank "No Repository" empty state.
