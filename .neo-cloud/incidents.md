# Standing production incidents — single source of truth

One row per distinct, externally-caused problem (dead credential, infra
misconfig, third-party outage) that no routine can root-cause-fix by writing
code. This is NOT for one-off code-fixable bugs (those go in TODO.txt/findings.txt)
or one-off design questions (those go in needs-human.md).

Every routine's FIRST step each run: read this file. For each OPEN row, do a
CHEAP liveness check (one curl/API call, a few seconds) — not a full re-
investigation. Still broken → just bump `last confirmed`. Looks fixed → don't
assume; verify properly once, then set status RESOLVED and say so in your run
log. Never re-write an existing row's full text — edit fields in place.

If a routine finds a NEW standing incident, add a new `### INC-<next number>`
row here (see `~/.claude/neo-shared/blocked-item-format.md` for the exact
format and severity rubric) instead of writing prose into needs-human.md.

The daily digest reads this file as its primary source for "is the app healthy".

---

### INC-1 — CRITICAL — GitHub (and Jira) OAuth connection is dead for the account this app uses
- status: MITIGATED (GitHub repo-picker half only; Jira side and the underlying OAuth reconnect are still OPEN, see "needs from user")
- affects: Automations (GitHub-triggered), Knowledge/KT generation (repo picker), Proactivation, CodeGraph, kt-video — anything that needs to attach a GitHub repo
- first seen: 2026-09-10   last confirmed: 2026-09-16 (18:52 UTC, neo-focus-explorer run 12: /environment/connections GitHub still 'Failing / vamsi920 / Missing: read:user, repo', Jira still 'Failing / https://neodevex.atlassian.net / Missing: offline_access, read:jira-user, read:jira-work', unchanged)   confirmed by: neo-ui-tester / neo-focus-explorer
- needs from user: reconnect the account in the app — Environment > Connections > GitHub (and Jira, same issue). This is an OAuth consent step; no routine can do it for you.
- mitigation: 2026-09-16 (neo-focus-fixer-cloud): the shared repo picker (`GitRepoDropdown`, used by Proactivation, Automations manifest fields, the home page and the chat "open repository" modal) used to fall through to the generic "No Repository" empty state whenever the GitHub connection was dead, because a missing/disabled connection never set `isError` -- it just left the repositories query permanently disabled with no other signal. `useUserProviders` now exposes `isGithubDisconnected` (true once we've conclusively determined -- not just "still loading" -- that there's no working GitHub connection), threaded through `useRepositoryData`/`GitRepoDropdown` to render "GitHub isn't connected — reconnect in Environment > Connections" (`HOME$GITHUB_NOT_CONNECTED`) instead. `kt-list.tsx`'s separate "No connected repositories yet" empty state is NOT fed by live GitHub API data (it reflects locally-persisted conversation/knowledge history) and was left alone -- it's a different bug, not this incident. Jira has no equivalent picker in this app and was not touched.
- evidence: github-api-proxy edge function logs: 401 "Bad credentials"; repo picker shows no repos

### INC-3 — HIGH — neo-agent-server has no persistent storage and restarts on every push to main
- status: OPEN
- affects: any in-progress conversation, KT/video generation, or agent run — killed mid-work by an unrelated deploy
- first seen: 2026-09-16   last confirmed: 2026-09-25 (22:11 UTC, neo-hourly-fixer-cloud liveness check: `GET https://neo-agent-server.fly.dev/server_info` returned `uptime: 982` seconds (~16 min) — consistent with the known deploy-triggers-restart config; no change to report)   confirmed by: neo-hourly-fixer-cloud
- needs from user: decide whether to add a persistent volume to the Fly deployment, and/or change fly-deploy.yml so it doesn't redeploy the agent-server on every frontend-only commit
- mitigation: none — this is an infra/deploy-config decision, not something a routine should change unasked (touches .github/workflows, out of scope for all routines by standing rule)
- evidence: 5+ agent-server restarts observed in one day so far, each following an unrelated push; today's push cadence (~every 20-30 min) means most live conversations/automations now have only a partial window before being wiped

### INC-4 — CRITICAL — the account's default LLM profile (neodevex-gemini-default, gemini-3.1-pro) has exhausted its daily Gemini API quota, failing every agent run that uses it
- status: MITIGATED (Automations Activity Log now names the quota-exhaustion failure mode instead of a bare "Failed" badge — see mitigation below; the underlying Gemini daily-quota exhaustion itself is still recurring, see "needs from user") — history: OPEN (REOPENED 2026-09-16 18:56 UTC, neo-focus-explorer run 12 — recurred less than 1.5h after the 17:35 UTC RESOLVED mark) — history: RESOLVED 2026-09-16 (17:35 UTC, neo-ui-tester-hourly: verified properly — earlier than the 16:52 UTC "retry in 7h8m" prediction (~00:00 UTC 09-17) would suggest. Two clean, fully-successful manual-chat turns against the unmodified default `neodevex-gemini-default` profile in a fresh no-repo conversation: "Say the word pong" -> real Thinking step -> "pong" in ~14s, then "What is 2+2?" -> "4" in ~10s, zero errors either time. Cross-checked in AgentOps Overview immediately after: Runs Today 1, Failures 0, Tokens Today 37.8k, Cost $0.05, Recent activity shows "Run completed after 2 tool calls" for that exact conversation — so the token-usage-incrementing cross-check this playbook asks for also passed cleanly. Quota evidently reset/recovered sooner than the error message's own retry estimate implied.)
- affects: any conversation or automation run using the default "neodevex-gemini-default" profile — which is the profile new conversations use unless the user picks another — for the rest of the Google quota window (~7h from first confirmation)
- first seen: 2026-09-16 (16:31 UTC)   last confirmed: 2026-09-16 (18:56 UTC, neo-focus-explorer run 12 — a fresh no-repo conversation (db8cc, files-terminal-browser testing) on the unmodified default `neodevex-gemini-default` profile completed a large multi-file-creation turn successfully, then its next turn failed with ConversationErrorEvent again: error-message-banner text 'litellm.MidStreamFallbackError: litellm.RateLimitError: litellm.RateLimitError: vertex_ai_betaException - {"error":{"code":429,"message":"...Quota exceeded for metric: generativelanguage.googleapis.com/generate_requests_per_model_per_day, limit: 250, model: gemini-3.1-pro\nPlease retry in 5h3m25s.","status":"RESOURCE_EXHAUSTED"}}', conversation title showed a red-dot status icon. Same limit/model as the original incident, so the daily 250-request cap is being burned through again well before the day is over — confirms the earlier note that many QA/testing routines sharing this account+model can exhaust it repeatedly in one day even after a reset.)   confirmed by: neo-ui-tester / neo-focus-explorer   [16:52 UTC neo-focus-explorer run 10 finding retained below for history]
- needs from user: check the Google AI Studio / Gemini API billing plan behind this deployment's `neodevex-gemini-default` profile — the error names `generativelanguage.googleapis.com/generate_requests_per_model_per_day` with a hard limit of 250 requests/day for `gemini-3.1-pro`, exhausted well before the end of the day. Either raise the plan's daily quota, or point the default profile at a model/tier with headroom for the day's real usage plus the many QA/testing routines that also share this same account and model.
- mitigation: 2026-09-16 (neo-hourly-fixer-cloud): an automation run's Activity Log row (`src/components/features/automations/detail/activity-log-item.tsx`) used to render the exact same bare `RunStatusBadge` "Failed" pill for every kind of failure, with the real `error_detail` (e.g. litellm's `RESOURCE_EXHAUSTED` / `RateLimitError` wrapping the Gemini `generate_requests_per_model_per_day` cap) only visible by calling the automation-run API directly. Added `isRateLimitErrorDetail()` (`src/components/features/home/featured-automations/automation-run-health.ts`, matching `RESOURCE_EXHAUSTED|RateLimitError|rate.?limit|quota`, case-insensitive) and, when a FAILED run's `error_detail` matches it, the Activity Log row now renders "Failed — default model hit its daily quota" (new `AUTOMATIONS$DETAIL$RATE_LIMIT_FAILED` key) with the full raw `error_detail` on hover, instead of the generic badge — reusing the same hover-detail pattern `HomeAutomationRunTooltip` already used for the home page's featured-automation cards. Any other failure reason still renders the plain "Failed" badge unchanged. Does not touch AgentOps (`AgentOpsRun` has no raw error-string field piped from the collector yet — a bigger, separate lift) and does not address the underlying daily-quota exhaustion itself, which still needs the user's action below.
- evidence: automation run `error_detail` (via `GET /api/automation/v1/<id>/runs`): `litellm.RateLimitError: geminiException - {"error":{"code":429,"message":"You exceeded your current quota... Quota exceeded for metric: generativelanguage.googleapis.com/generate_requests_per_model_per_day, limit: 250, model: gemini-3.1-pro\nPlease retry in 7h22m19s.","status":"RESOURCE_EXHAUSTED"}}` — reproduced on 2 separate Proactivation runs, ~$0.17-$0.20 cost each despite failing

### INC-5 — CRITICAL — GitHub Enterprise OAuth's shared client secret can still be sent to any attacker-named public host
- status: MITIGATED (the internal/SSRF half only -- an attacker naming an internal address, cloud-metadata IP, or IPv6/IPv4-mapped equivalent is now rejected; naming an ordinary attacker-owned public domain is still accepted, see "needs from user")
- affects: `github-oauth-start`/`github-oauth-callback` (GHES connection flow) -- and, by extension, every user of this deployment, since `GITHUB_ENTERPRISE_OAUTH_CLIENT_ID`/`_SECRET` is one shared credential pair for the whole deployment, not per-user
- first seen: 2026-09-22 (02:20 UTC, neo-hourly-fixer-cloud, auth-data-platform section run)   last confirmed: 2026-09-22 (same run)   confirmed by: neo-hourly-fixer-cloud
- needs from user: a product decision on the GHES connection flow's design -- `enterpriseHost` is free text any signed-in user (an anonymous browser session qualifies) types into Connections settings, and `github-oauth-callback` POSTs the deployment-wide `GITHUB_ENTERPRISE_OAUTH_CLIENT_SECRET` to `https://<enterpriseHost>/login/oauth/access_token`. Before this run's fix, naming an internal address there (e.g. the cloud metadata IP) leaked that secret back in the response body -- that path is now blocked. But naming an ordinary public domain the attacker controls is not distinguishable from a real customer's GHES host with the information this server has, so that variant still works: register `enterpriseHost: "attacker.example.com"`, complete the OAuth dance against a fake authorize page there, and the callback still hands that secret to the attacker's own server. Closing this needs either (a) an admin-configured allowlist of GHES hosts this deployment actually serves (env var, checked before the token POST), or (b) moving to a per-tenant client id/secret registered against each customer's own GHES instance instead of one shared pair. Either is a real behavior/config change beyond what a routine should decide unasked.
- mitigation: 2026-09-22 (neo-hourly-fixer-cloud): added `assertEnterpriseHostAllowed()` (`supabase/functions/_shared/github.ts`, reusing `assertHostAllowed` from `_shared/template.ts`) and call it in both `github-oauth-start` (before the host is ever stored) and `github-oauth-callback` (immediately before the token-exchange POST that actually sends the secret, as defense in depth). Also closed a real bypass in `assertHostAllowed` itself: it only pattern-matched textual IPv4/hostname forms, so an IPv4-mapped IPv6 literal (`::ffff:169.254.169.254`), IPv6 link-local (`fe80::/10`), unique-local (`fc00::/7`), loopback (`::1`) or unspecified (`::`) address reached the exact same blocked networks unblocked. `assertHostAllowed` now numerically decodes any bracketed IPv6 hostname and checks it (and, for IPv4-mapped addresses, re-checks the embedded IPv4 against the existing pattern list) rather than only pattern-matching text. This second fix also protects every other caller of `resolveBaseUrl`/`assertHostAllowed` (the generic connector host-override flow), not just GitHub.
- evidence: `supabase/functions/github-oauth-callback/index.ts` (pre-fix) built `githubTokenUrl(enterpriseHost)` from `github_oauth_state.enterprise_host` (attacker-supplied, stored verbatim by `github-oauth-start`) and POSTed `{client_id, client_secret, code, redirect_uri, code_verifier}` to it with no host validation at all; `_shared/template.ts`'s `BLOCKED_HOST_PATTERNS` (the existing SSRF blocklist reused for the fix) matched only textual dotted-IPv4/hostname patterns, confirmed bypassable via `new URL("http://[::ffff:169.254.169.254]/").hostname === "[::ffff:a9fe:a9fe]"` (no pattern matches that string).

### INC-6 — CRITICAL — Jira webhook receiver's bearer check isn't bound to the URL's registration, so one connected user's webhook JWT can trigger token access for a different user
- status: OPEN -- deliberately NOT code-fixed this run, see below
- affects: `supabase/functions/jira-webhook-receiver/index.ts` -- any Jira-connected user's account
- first seen: 2026-09-22 (02:20 UTC, neo-hourly-fixer-cloud, auth-data-platform section run)   last confirmed: 2026-09-25 (22:11 UTC, neo-hourly-fixer-cloud: cheap re-check of the code itself — `jira-webhook-receiver/index.ts` still authenticates solely via `verifyAtlassianWebhookJwt` (HMAC + `exp` only) and still looks up/decrypts the target user's Jira tokens via `.eq("user_id", registrationId)` with no comparison to any claim in the verified JWT payload; no new information on the real Atlassian claim schema since the prior check, so still holding off per below)   confirmed by: neo-hourly-fixer-cloud
- needs from user: `jira-webhook-receiver/index.ts:129-158` takes `registrationId` from the URL path and uses it directly as `user_id` to look up and decrypt/refresh *that* user's Jira access+refresh tokens and push the fresh one to the agent-server. The only auth check is `verifyAtlassianWebhookJwt` (`_shared/jira.ts:97-135`), which verifies the bearer JWT's HMAC signature against the one deployment-wide `JIRA_CLIENT_SECRET` and its `exp` claim -- it never checks the JWT's payload against `registrationId`/`cloud_id`. Since the client secret is shared across every user's Jira connection, any user who has connected their own Jira (so Atlassian signs a JWT for their own site's webhook events) appears able to replay that same bearer token against `jira-webhook-receiver/<other-user-id>` and have the server decrypt/refresh/push that other user's Jira token instead of their own. This is a real, code-fixable bug in principle -- bind the verified JWT's site/tenant claim to the row looked up by `registrationId` (reject on mismatch) -- but this run deliberately did NOT attempt that fix: Atlassian's OAuth-2.0-app webhook JWT claim schema (which claim actually carries the site/cloudId here, as opposed to Atlassian Connect's differently-shaped `qsh`/`context` claims) is not verifiable from inside this sandbox (no docs access, no captured real-payload fixture anywhere in this repo), and guessing wrong in a path that every production Jira webhook call goes through risks either leaving the hole open (checking the wrong claim) or silently breaking Jira automation for every connected user (rejecting a legitimate claim) on a change that deploys the moment it's pushed. Needs either: confirmed reference for the real claim name (fetch Atlassian's current webhook-JWT docs, or capture one real payload from a test site) before writing the binding check, or a different mitigation the user prefers (e.g. a high-entropy per-registration path segment instead of the guessable `user_id`, so knowing another user's id alone isn't enough).
- mitigation: none yet -- see above for why this run held off rather than guess
- evidence: `jira-webhook-receiver/index.ts:154-158` (`.eq("user_id", registrationId)`) and `:168-172` (same key against `jira_connections`), reached after only `verifyAtlassianWebhookJwt(bearerToken, clientSecret)` (`:147`) passes -- no comparison anywhere between the verified payload and `registrationId`.

### INC-7 — CRITICAL — OAuth `state` is never bound to the browser that must complete it, so an attacker can bind a victim's GitHub/Jira authorization to the attacker's own account
- status: OPEN -- deliberately NOT code-fixed this run, see below
- affects: every OAuth connect flow -- `connections-oauth-start`/`_shared/connections-oauth-complete.ts` (generic flow, github/github-enterprise/jira-cloud/any future registry provider) and the two legacy flows it wraps, `github-oauth-start`/`github-oauth-callback` and `jira-oauth-start`/`jira-oauth-callback` -- i.e. every provider connection in the app
- first seen: 2026-09-23 (03:10 UTC, neo-hourly-fixer-cloud, auth-data-platform section run)   last confirmed: 2026-09-25 (22:11 UTC, neo-hourly-fixer-cloud: cheap re-check of the code itself — `connections-oauth-start/index.ts` still derives `userId`/`orgId` solely from the caller's own session (`getCallerUserId`/`getCallerOrgId`) and stores them in `oauth_states` keyed only by `state`, with no cookie/session binding added since; no new information changing the design-decision blocker, so still holding off per below)   confirmed by: neo-hourly-fixer-cloud
- needs from user: a product/design decision on how the callback should verify the browser completing the flow is the same one that started it. The standard fix is a double-submit cookie: `connections-oauth-start` (and `github-oauth-start`/`jira-oauth-start`) sets a short-lived, `HttpOnly`, `SameSite=Lax` cookie (e.g. a hash of `state`) alongside returning `authorizeUrl`, scoped to the Supabase functions origin so it rides along on the top-level GET redirect back from the provider; the callback (`connections-oauth-complete.ts` and both legacy callbacks) then rejects the request unless that cookie is present and matches the `state` row before doing anything else. This touches every OAuth entry/exit point in the app (6 edge functions plus the frontend's `authorizeUrl` fetch, which would need `credentials: "include"`) and is exactly the kind of core-flow, cross-cutting change this sandbox cannot exercise end-to-end (no real browser completing a real provider round-trip) -- getting the cookie scope/SameSite/CORS-credentials details wrong risks either leaving the hole open or silently breaking every connection attempt the moment it deploys, with no automated test able to catch that class of mistake. Needs a human to implement and manually verify one real connect round-trip (GitHub or Jira) in a live environment before this can safely ship.
- mitigation: none -- see above for why this run held off rather than build it blind
- evidence: `connections-oauth-start/index.ts:22,71-76,100-113` derives `userId`/`orgId` solely from the caller's own authenticated session (`getCallerUserId`/`getCallerOrgId`) and stores them in `oauth_states` keyed only by the returned `state`; `_shared/connections-oauth-complete.ts:70-91,232-255` (and identically, the legacy path, `github-oauth-callback/index.ts:44-146` against `github_oauth_state`) looks up that row by `state` alone and unconditionally trusts `stateRow.user_id`/`org_id` for the resulting token write -- nothing (no cookie, no origin header check, no re-auth prompt) ties the request completing the callback to the session that created the state row. An attacker can call `*-oauth-start` as themselves to obtain a legitimate `authorizeUrl` bound to their own org, send that URL to a victim, and if the victim authorizes it on the real provider consent screen, the resulting GitHub/Jira access token is written into the *attacker's* org's connection, not the victim's.

