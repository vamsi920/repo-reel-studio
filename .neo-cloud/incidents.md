# Standing production incidents — single source of truth

One row per distinct, externally-caused problem (dead credential, infra
misconfig, third-party outage) that no routine can root-cause-fix by writing
code. This is NOT for one-off code-fixable bugs (those go in TODO.txt/findings.txt)
or one-off design questions (those go in needs-human.md).

Every routine's FIRST step each run: read this file. For each OPEN row, do a
CHEAP liveness check (one curl/API call, a few seconds) — not a full re-
investigation. Still broken → just bump `last confirmed`. Looks fixed → don't
assume; verify properly once, then set status RESOLVED and say so in your run
log. Never re-write an existing row's full text — edit fields in place.

If a routine finds a NEW standing incident, add a new `### INC-<next number>`
row here (see `~/.claude/neo-shared/blocked-item-format.md` for the exact
format and severity rubric) instead of writing prose into needs-human.md.

The daily digest reads this file as its primary source for "is the app healthy".

---

### INC-1 — CRITICAL — GitHub (and Jira) OAuth connection is dead for the account this app uses
- status: OPEN
- affects: Automations (GitHub-triggered), Knowledge/KT generation (repo picker), Proactivation, CodeGraph, kt-video — anything that needs to attach a GitHub repo
- first seen: 2026-09-10   last confirmed: 2026-09-16 (18:52 UTC, neo-focus-explorer run 12: /environment/connections GitHub still 'Failing / vamsi920 / Missing: read:user, repo', Jira still 'Failing / https://neodevex.atlassian.net / Missing: offline_access, read:jira-user, read:jira-work', unchanged)   confirmed by: neo-ui-tester / neo-focus-explorer
- needs from user: reconnect the account in the app — Environment > Connections > GitHub (and Jira, same issue). This is an OAuth consent step; no routine can do it for you.
- mitigation: none built yet — next routine to touch environment-onboarding or automations should add a clear "GitHub isn't connected — reconnect in Environment > Connections" banner anywhere this currently fails silently or confusingly, instead of an empty/misleading state
- evidence: github-api-proxy edge function logs: 401 "Bad credentials"; repo picker shows no repos

### INC-2 — CRITICAL — Supabase auth/session errors block Knowledge, CodeGraph and other persisted data for signed-in users
- status: RESOLVED 2026-09-16 (neo-focus-explorer run 8, verified properly once: with the QA session's JWT, GET /rest/v1/org_members?select=org_id&user_id=eq.<uid> → 200 with 2 org rows, GET /rest/v1/knowledge_generations → 200 with rows, GET /auth/v1/user → 200, POST /rest/v1/usage_events → 201 ×14 for one conversation; /kt lists vamsi920/layman and vamsi920/lets-see with 'View Knowledge'; zero PGRST301 console lines; the 14:47 UTC Playwright login (which calls /auth/v1/token from the browser) succeeded, so the 09-14 CORS block on token is not reproducible either. Learnings.md already recorded the PGRST301 fix holding on 09-11 and later — this row was stale.)
- affects: Knowledge/KT (docs invisible even when already generated), CodeGraph, likely usage/activity/memory persistence — anything gated on repository-identity/org resolution
- first seen: 2026-09-10   last confirmed: 2026-09-14 (not reproducible 2026-09-16)   confirmed by: neo-ui-tester
- needs from user: check the Supabase project's (hyirnyyqwyvplwvuekda) auth/JWT signing-key configuration, and specifically whether /auth/v1/token is CORS-blocked (2026-09-14 finding suggested this could affect ALL sign-ins, not just this test account — verify that specifically, it's the more urgent half of this incident)
- mitigation: none built yet — next routine to touch knowledge-kt or auth-data-platform should add a clear "we can't load your data right now" error state anywhere this currently shows a silent/misleading empty state (e.g. "No connected repositories yet" when repos actually exist)
- evidence: browser console: `[repository-identity] org_members lookup failed {code: PGRST301, message: No suitable key or wrong key type}`; confirmed via Supabase MCP that public.knowledge_generations/codegraph_snapshots have real rows that never reach the UI

### INC-3 — HIGH — neo-agent-server has no persistent storage and restarts on every push to main
- status: OPEN
- affects: any in-progress conversation, KT/video generation, or agent run — killed mid-work by an unrelated deploy
- first seen: 2026-09-16   last confirmed: 2026-09-16 (18:52 UTC neo-focus-explorer run 12: `server_info` uptime 1590 s = restart ~18:21 UTC, consistent with an unrelated push landing around then; Automations list was empty and AgentOps still showed only 1 run from before the restart — still restarting on the same unrelated cadence as before)   confirmed by: neo-ui-tester / neo-focus-explorer
- needs from user: decide whether to add a persistent volume to the Fly deployment, and/or change fly-deploy.yml so it doesn't redeploy the agent-server on every frontend-only commit
- mitigation: none — this is an infra/deploy-config decision, not something a routine should change unasked (touches .github/workflows, out of scope for all routines by standing rule)
- evidence: 5+ agent-server restarts observed in one day so far, each following an unrelated push; today's push cadence (~every 20-30 min) means most live conversations/automations now have only a partial window before being wiped

### INC-4 — CRITICAL — the account's default LLM profile (neodevex-gemini-default, gemini-3.1-pro) has exhausted its daily Gemini API quota, failing every agent run that uses it
- status: OPEN (REOPENED 2026-09-16 18:56 UTC, neo-focus-explorer run 12 — recurred less than 1.5h after the 17:35 UTC RESOLVED mark) — history: RESOLVED 2026-09-16 (17:35 UTC, neo-ui-tester-hourly: verified properly — earlier than the 16:52 UTC "retry in 7h8m" prediction (~00:00 UTC 09-17) would suggest. Two clean, fully-successful manual-chat turns against the unmodified default `neodevex-gemini-default` profile in a fresh no-repo conversation: "Say the word pong" -> real Thinking step -> "pong" in ~14s, then "What is 2+2?" -> "4" in ~10s, zero errors either time. Cross-checked in AgentOps Overview immediately after: Runs Today 1, Failures 0, Tokens Today 37.8k, Cost $0.05, Recent activity shows "Run completed after 2 tool calls" for that exact conversation — so the token-usage-incrementing cross-check this playbook asks for also passed cleanly. Quota evidently reset/recovered sooner than the error message's own retry estimate implied.)
- affects: any conversation or automation run using the default "neodevex-gemini-default" profile — which is the profile new conversations use unless the user picks another — for the rest of the Google quota window (~7h from first confirmation)
- first seen: 2026-09-16 (16:31 UTC)   last confirmed: 2026-09-16 (18:56 UTC, neo-focus-explorer run 12 — a fresh no-repo conversation (db8cc, files-terminal-browser testing) on the unmodified default `neodevex-gemini-default` profile completed a large multi-file-creation turn successfully, then its next turn failed with ConversationErrorEvent again: error-message-banner text 'litellm.MidStreamFallbackError: litellm.RateLimitError: litellm.RateLimitError: vertex_ai_betaException - {"error":{"code":429,"message":"...Quota exceeded for metric: generativelanguage.googleapis.com/generate_requests_per_model_per_day, limit: 250, model: gemini-3.1-pro\nPlease retry in 5h3m25s.","status":"RESOURCE_EXHAUSTED"}}', conversation title showed a red-dot status icon. Same limit/model as the original incident, so the daily 250-request cap is being burned through again well before the day is over — confirms the earlier note that many QA/testing routines sharing this account+model can exhaust it repeatedly in one day even after a reset.)   confirmed by: neo-ui-tester / neo-focus-explorer   [16:52 UTC neo-focus-explorer run 10 finding retained below for history]
- needs from user: check the Google AI Studio / Gemini API billing plan behind this deployment's `neodevex-gemini-default` profile — the error names `generativelanguage.googleapis.com/generate_requests_per_model_per_day` with a hard limit of 250 requests/day for `gemini-3.1-pro`, exhausted well before the end of the day. Either raise the plan's daily quota, or point the default profile at a model/tier with headroom for the day's real usage plus the many QA/testing routines that also share this same account and model.
- mitigation: none built yet — a run that fails this way currently surfaces only a bare "Failed" row with a $-cost in the Automations dashboard; the actual reason (`LLMRateLimitError` / `RESOURCE_EXHAUSTED`) is only visible by calling the automation-run API directly, not anywhere in the UI. A future routine touching AgentOps/Automations should consider surfacing rate-limit failures with a clear "the default model hit its daily quota" message instead of a bare Failed badge. A brand-new manual chat conversation started against this profile during the same window also produced no visible response and no visible error for 35+ seconds before an unrelated INC-3 restart wiped it, so the manual-chat path could not be conclusively confirmed as affected the same way — a future run with a stable (non-restarting) window should retest a plain manual conversation against neodevex-gemini-default to confirm whether it also fails, and whether it fails with a visible in-chat error or hangs silently.
- evidence: automation run `error_detail` (via `GET /api/automation/v1/<id>/runs`): `litellm.RateLimitError: geminiException - {"error":{"code":429,"message":"You exceeded your current quota... Quota exceeded for metric: generativelanguage.googleapis.com/generate_requests_per_model_per_day, limit: 250, model: gemini-3.1-pro\nPlease retry in 7h22m19s.","status":"RESOURCE_EXHAUSTED"}}` — reproduced on 2 separate Proactivation runs, ~$0.17-$0.20 cost each despite failing

### INC-5 — CRITICAL — the account's default LLM profile now has NO Gemini API key configured at all, failing every agent run (manual chat and automations) instantly — distinct from and worse than INC-4's quota exhaustion
- status: RESOLVED 2026-09-16 (18:31 UTC, neo-ui-tester-hourly: verified properly — a brand-new no-repo conversation ("Say Pong Test") against the unmodified default `neodevex-gemini-default` profile sent "Say the word pong" and got a clean "pong" reply in ~4s, zero errors, model reported as `gemini/gemini-pro-latest`. Cross-checked in AgentOps immediately after: Overview showed Runs Today 1 / Failures 0 / Tokens Today 18.8k / Cost $0.02, and History showed the same run "Finished / Completed" at $0.02 for that exact conversation — so the key is present and working again, not merely rate-limited-then-recovered like INC-4. Test conversation deleted afterward, confirmed 0 remaining via /api/conversations/search.)
- affects: every conversation or automation on the default "neodevex-gemini-default" profile — which is the profile new conversations use unless the user picks another — this is a total outage of agent functionality, not a rate limit
- first seen: 2026-09-16 (17:56 UTC, neo-focus-explorer run 11)   last confirmed: 2026-09-16 (17:59 UTC, neo-focus-explorer run 11)   confirmed by: neo-focus-explorer
- needs from user: check the `GEMINI_API_KEY` / `GOOGLE_API_KEY` secret on the neo-agent-server Fly deployment — it appears to be unset (not merely rate-limited: litellm reports the key itself is missing). Verify with `fly secrets list -a neo-agent-server` (name only, value is not shown) and re-set it if absent; also check whether the frequent restarts tracked in INC-3 are somehow related (e.g. a deploy step that clears this secret) since this started shortly after a restart.
- mitigation: none built yet — same gap as INC-4: a run that fails this way shows only a bare "Failed"/error state with no readable explanation. Worth building the same clear-error surfacing once decided, but the priority right now is getting the key restored since NOTHING agent-driven works without it.
- evidence: two separate conversations (2de1bb26-e180-4f3f-9cc8-9b866a5d256c), two separate messages 3 minutes apart, both failed identically: ConversationErrorEvent code LLMServiceUnavailableError, detail "litellm.APIConnectionError: Missing Gemini API key. Set the GEMINI_API_KEY or GOOGLE_API_KEY environment variable." — server_info uptime 645s at last check (server has been up the whole time, not a boot race)
