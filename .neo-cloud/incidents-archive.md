# Resolved incidents — archive

Rows moved here from `.neo-cloud/incidents.md` once their status is RESOLVED,
by the daily digest task. One file for all time, newest at the bottom.

---

## archived 2026-09-16

### INC-2 — CRITICAL — Supabase auth/session errors block Knowledge, CodeGraph and other persisted data for signed-in users
- status: RESOLVED 2026-09-16 (neo-focus-explorer run 8, verified properly once: with the QA session's JWT, GET /rest/v1/org_members?select=org_id&user_id=eq.<uid> → 200 with 2 org rows, GET /rest/v1/knowledge_generations → 200 with rows, GET /auth/v1/user → 200, POST /rest/v1/usage_events → 201 ×14 for one conversation; /kt lists vamsi920/layman and vamsi920/lets-see with 'View Knowledge'; zero PGRST301 console lines; the 14:47 UTC Playwright login (which calls /auth/v1/token from the browser) succeeded, so the 09-14 CORS block on token is not reproducible either. Learnings.md already recorded the PGRST301 fix holding on 09-11 and later — this row was stale.)
- affects: Knowledge/KT (docs invisible even when already generated), CodeGraph, likely usage/activity/memory persistence — anything gated on repository-identity/org resolution
- first seen: 2026-09-10   last confirmed: 2026-09-14 (not reproducible 2026-09-16)   confirmed by: neo-ui-tester
- needs from user: check the Supabase project's (hyirnyyqwyvplwvuekda) auth/JWT signing-key configuration, and specifically whether /auth/v1/token is CORS-blocked (2026-09-14 finding suggested this could affect ALL sign-ins, not just this test account — verify that specifically, it's the more urgent half of this incident)
- mitigation: 2026-09-16 (neo-focus-fixer-cloud, knowledge-kt section run): this row's incident is already RESOLVED, but the leftover suggestion below (from before it was marked resolved) was still live and unaddressed, so it was built as this run's knowledge-kt work. `knowledgePersistenceRepository.listGeneratedRepositories()` (src/lib/data-platform/repositories/knowledge-repository.ts) used to swallow a genuine Supabase query error (RLS denial, dead auth session, network failure) into the exact same `[]` result as "this user genuinely has zero generated repositories" — so /kt's `usePersistedRepositories`/`useAllRepositories` (src/routes/kt-list.tsx) rendered the plain "No connected repositories yet" empty state even when real generations existed in Postgres and just couldn't be read, with nothing in the UI pointing at why. The repository method now returns `{ summaries, error }`, and kt-list.tsx surfaces a distinct "We can't load your data right now" (`KT$LOAD_ERROR`) alert state instead of the misleading empty-state copy whenever the lookup itself failed (query error or rejected promise) and no connected/local repos are otherwise known — it does not touch the already-connected-repos-still-shown path, which was already correct. This is the same "silent empty state → readable error state" pattern INC-1's `isGithubDisconnected` mitigation used, applied here to the Supabase-persisted half of /kt instead of the GitHub-connection half. Does not address the underlying auth/session root cause (still needs the user's action above); only makes a future recurrence visible instead of silent.
- evidence: browser console: `[repository-identity] org_members lookup failed {code: PGRST301, message: No suitable key or wrong key type}`; confirmed via Supabase MCP that public.knowledge_generations/codegraph_snapshots have real rows that never reach the UI

### INC-5 — CRITICAL — the account's default LLM profile now has NO Gemini API key configured at all, failing every agent run (manual chat and automations) instantly — distinct from and worse than INC-4's quota exhaustion
- status: RESOLVED 2026-09-16 (18:31 UTC, neo-ui-tester-hourly: verified properly — a brand-new no-repo conversation ("Say Pong Test") against the unmodified default `neodevex-gemini-default` profile sent "Say the word pong" and got a clean "pong" reply in ~4s, zero errors, model reported as `gemini/gemini-pro-latest`. Cross-checked in AgentOps immediately after: Overview showed Runs Today 1 / Failures 0 / Tokens Today 18.8k / Cost $0.02, and History showed the same run "Finished / Completed" at $0.02 for that exact conversation — so the key is present and working again, not merely rate-limited-then-recovered like INC-4. Test conversation deleted afterward, confirmed 0 remaining via /api/conversations/search.)
- affects: every conversation or automation on the default "neodevex-gemini-default" profile — which is the profile new conversations use unless the user picks another — this is a total outage of agent functionality, not a rate limit
- first seen: 2026-09-16 (17:56 UTC, neo-focus-explorer run 11)   last confirmed: 2026-09-16 (17:59 UTC, neo-focus-explorer run 11)   confirmed by: neo-focus-explorer
- needs from user: check the `GEMINI_API_KEY` / `GOOGLE_API_KEY` secret on the neo-agent-server Fly deployment — it appears to be unset (not merely rate-limited: litellm reports the key itself is missing). Verify with `fly secrets list -a neo-agent-server` (name only, value is not shown) and re-set it if absent; also check whether the frequent restarts tracked in INC-3 are somehow related (e.g. a deploy step that clears this secret) since this started shortly after a restart.
- mitigation: none built yet — same gap as INC-4: a run that fails this way shows only a bare "Failed"/error state with no readable explanation. Worth building the same clear-error surfacing once decided, but the priority right now is getting the key restored since NOTHING agent-driven works without it.
- evidence: two separate conversations (2de1bb26-e180-4f3f-9cc8-9b866a5d256c), two separate messages 3 minutes apart, both failed identically: ConversationErrorEvent code LLMServiceUnavailableError, detail "litellm.APIConnectionError: Missing Gemini API key. Set the GEMINI_API_KEY or GOOGLE_API_KEY environment variable." — server_info uptime 645s at last check (server has been up the whole time, not a boot race)
